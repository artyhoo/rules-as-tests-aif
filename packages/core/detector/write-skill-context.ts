// Task 6 (write-side touchpoint 4) — emit AIF skill-context overrides.
// Full implementation lands in Task 6 commit; keep stub so cli.ts compiles.

import type { DetectionResult } from './types.ts';

export function writeSkillContext(_outDir: string, _result: DetectionResult): void {
  throw new Error('writeSkillContext: not yet implemented (Phase 4 Task 6)');
}
