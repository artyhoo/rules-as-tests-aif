{
  "patternId": "react-server-components",
  "appliesTo": ["next"],
  "rule": {
    "title": "Forbid Node-only modules in `'use client'` components",
    "stack": ["react-next"],
    "applies-to": ["src/app/**/*.tsx", "src/components/**/*.tsx"],
    "check": {
      "type": "eslint",
      "rule": "rules-as-tests/no-server-imports-in-client"
    },
    "examples": {
      "bad": "'use client'\nimport fs from 'node:fs';\n\nexport default function ClientWidget() {\n  return <div>{fs.readFileSync('config.json', 'utf8')}</div>;\n}",
      "good": "'use client'\nimport { useState } from 'react';\n\nexport default function ClientWidget({ initialConfig }: { initialConfig: string }) {\n  const [config] = useState(initialConfig);\n  return <div>{config}</div>;\n}"
    }
  },
  "rulesMdTemplate": "## {{id}} — Forbid Node-only modules in `'use client'` components\n\n**Stack:** react-next  \n**Check:** ESLint `rules-as-tests/no-server-imports-in-client` (preset rule, already shipped in `packages/preset-next-15-canonical/eslint-rules/`)  \n**Why:** `'use client'` modules execute in the browser. Node-only APIs (`node:fs`, `node:crypto`, `node:child_process`) crash at runtime. Pass server data through props instead of importing.\n",
  "eslintRuleConfig": {
    "rules-as-tests/no-server-imports-in-client": "error"
  }
}
