# Rules-as-Tests skill (with-drift fixture)

The 5-layer framework MUST be applied. AST > grep MUST be the default.
Paired negative tests MUST exist for every rule.

Mutation testing MUST be enabled.
