# Overview (with-drift fixture)

The 5-layer framework defines five layers, each MUST be present.

AST > grep — every check SHOULD traverse the TSESTree (note: weakened modal vs SKILL.md).

Paired negative tests MUST accompany every positive case.

Mutation testing MUST be enabled (Stryker).

Two-AI review MUST run before merge.
