# Rules-as-Tests skill

The 5-layer framework MUST be applied to every rule. AST > grep — MUST be the
default extraction strategy. Paired negative tests MUST exist for every rule.

Mutation testing MUST be enabled for AST-generation phases. The two-AI review
MUST run before any rule is admitted to the manifest.
