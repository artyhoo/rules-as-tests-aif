# enforcement-liveness-fix — DONE
- Final PR: (pending — branch feature/enforcement-liveness-fix-feef69 ready to PR)
- Closed: 2026-06-26
- Summary: Barrel TS→JS compilation fix (Variant A) — eslint.config.mjs now imports compiled index.js; f17 passes on Node 22; REDUCED WARN visible in pre-push fallback; CI matrix adds Node 20+22.

## Evidence: f17 passes on Node 22

Node version: v22.22.3

```text
✓ Check3 Arm(i): eslint-rules-local/no-unsafe-zod-parse.ts present (shipped rule copied by install.sh)
✓ Check3 Arm(i): eslint-rules-local/index.js present (compiled JS barrel generated by install.sh)
✓ Check3 Arm(ii): planted .parse() violation FLAGGED by shipped eslint-rules-local/no-unsafe-zod-parse.ts
✓ Check3 Arm(ii) neg: audit:exempt → rule skips line (non-vacuous: shipped rule is live + exempt path works)
PASS=4 FAIL=0
```

## S2 gate: ESM import works on Node 22

```text
node --input-type=module -e "const m = await import('./eslint-rules-local/index.js'); console.log('plugin.meta:', JSON.stringify(m.default.meta));"
plugin.meta: {"name":"@rules-as-tests/local-eslint-rules","version":"0.1.0"}
```

## Invariant guard (§8)

- `make self-audit` not available in this environment; principle tests 09, 12, 25 pass (directly verified).
- Principle 17 (no-paid-LLM in CI): f17-node-compat job uses only bash + npm (no API calls). ✓
- Principle 21 (agnosticism): barrel is now pure JavaScript ESM — no TS loader required, all-Node compatible. ✓

## Parked decision (dev-deps strategy)

Per kickoff §4c: tsc auto-install vs opt-in + loud REDUCED-WARN is still operator-gated.
Current implementation: warns loudly when tsc not found (opt-in path). Operator to decide if auto-install needed.
