# KICKOFF — f2-aif-fire-backend-iphase (meta-launch)

> **Type:** I-phase, single-sub-wave (generated by `/pipeline f2-aif-fire-backend` on 2026-06-10).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill. Name `f2-aif-fire-backend` resolved → `f2-aif-fire-backend-iphase` (only dispatchable kickoff; sibling `f2-fire-statusreadback-brainstorm` has no kickoff + is already DN-resolved).
> **Deliverable:** honest dispatch-only `AifFireBackend` wired into `runtime-bridge` (never lies about completion, `awaitDone()` never hangs) + operator `/fire` runbook + `scripts/fire-routine.sh`. REST stays the agnostic default with full `cli/await.ts` parity.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

This is a single-stage I-phase: one Worker, one PR → staging. There is no Stage 2/3.

Read in this order:

1. This kickoff (binding wrapper + stage gate + dispatch instruction).
2. **The umbrella kickoff (BINDING SPEC):** `.claude/orchestrator-prompts/f2-aif-fire-backend-iphase/kickoff.md` — TASK, Hard constraints (4), §Type-decision (a/b), Files, T-traps, VERIFY. This wrapper does NOT restate it; read it verbatim.
3. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-06-10, HEAD `5ec8dc4`). Mechanical `plan-currency-check.sh` rc=0; reverse-currency surfaced 69 untracked merged PRs in §0 (informational, maintainer-owned reconciliation — not blocking this dispatch).
- **In-flight / dup check (manual — injected helpers ran with empty arg):**
  - `packages/runtime-bridge/src/AifFireBackend.ts` — **does not exist** (work undone).
  - `scripts/fire-routine.sh` — **does not exist** (work undone).
  - PR #333 (`worktree-f2-bridge-fire`, merged 2026-06-01) = **docs-only research-patch** (`docs(research-patches): F2 — CC Routines /fire ...`), NOT the implementation. It produced the DN-1=A+D verdict; it did not build the backend.
  - No open PR carries this work. **CLEAR — not a duplicate, not in-flight.**
- **Umbrella kickoff:** `.claude/orchestrator-prompts/f2-aif-fire-backend-iphase/kickoff.md` (binding).
- **Type ground truth (verified for §Type-decision):** `types.ts:44` → `status: 'pending'|'running'|'done'|'error'` (no `blocked_external`); `backend.ts:16` → `name: 'aif-handoff'|'amux'|'manual'` (no `'aif-fire'`). Both confirm the kickoff's §Type-decision framing.

---

## §2 Launch-table

Launch table — f2-aif-fire-backend-iphase (as of HEAD `5ec8dc4`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A | execution-build (runtime-bridge code + script + runbook + SSOT doc) | Mode A (inline session, `isolation: worktree`) | No (single coherent feature; TDD-within-session per kickoff line 40; Phase -1 cold-review covers the review need) | 1 | — (single sub-wave) | M (~80–200 LOC: new backend ≥80 + does-not-hang/presence-gate tests + curl helper + runbook) |

**Why Mode A, not SDD/Mode B:** one backend feature, one PR (umbrella kickoff line 41). No ≥2 disjoint parallel surfaces → no Mode B. SDD's 3-reviewer overhead exceeds value for a single tightly-coupled backend; the mandatory Phase -1 cold-review (§7) is the review channel.

---

## §3 Stage gate (single stage — degenerate)

There is no inter-stage transition. The only gate is **completion**: the F2 PR merges to `staging`.

```bash
gh pr list --search "is:merged head:worktree-f2-aif-fire-backend base:staging" \
  --json number,title,mergedAt,headRefName --limit 5 2>/dev/null \
  || echo "gh unavailable — verify F2 PR merged manually"
```

(Head-branch pattern is the Worker's worktree branch; adjust the `head:` filter to the actual branch the Worker created. `base:staging` filter prevents recycled-branch false-positives — T-MOB-B.)

Empty result before the Worker has opened a PR is expected. After the Worker reports done, this gate must return the merged PR.

---

## §4 Sub-wave dispatch instruction (Stage 1, the only stage)

**Channel: maintainer-paste into a fresh CC tab** (NOT Agent-tool dispatch). Write-task Worker dispatch via the Agent tool with `isolation:"worktree"` silently fails for writes (`#worker-dispatch-via-subagent`, bug #39886). The umbrella kickoff's "inline Agent" wording means a single-focus Opus session; the channel is a pasted tab the maintainer opens. <!-- channel-discipline: allow teaches the anti-pattern (NOT-this-channel guidance + bug #39886) -->

Paste this into a new CC tab:

```text
/orchestrator — execute f2-aif-fire-backend-iphase, Mode A, isolation:worktree.
Read .claude/orchestrator-prompts/f2-aif-fire-backend-iphase/kickoff.md as the BINDING SPEC.
First: resolve §Type-decision (a vs b) explicitly and state it in the PR body (recommend (b) per kickoff line 18 unless callers need a machine-checkable dispatchOnly capability).
TDD: write the does-not-hang + available()-false-when-unconfigured tests FIRST.
Honor all 4 Hard constraints. One PR → staging. Attach the 5 VERIFY evidence lines.
```

**Worktree setup:** first step in the tab is `bash scripts/create-worktree.sh f2-aif-fire-backend` (portable, base-ref auto-resolved) OR `claude -w f2-aif-fire-backend` if the WorktreeCreate hook is wired. Per `parallel-subwave-isolation.md §1`.

---

## §4b §1.7 PR-body authoring mandate (CONDITIONAL — paths-triggered)

The implementation files live under `packages/runtime-bridge/src/**` + `scripts/**` — **these do NOT trigger the §1.7 PR-body mandate** (the trigger set is `.claude/rules/**`, `packages/core/principles/**`, `EXECUTION-PLAN.md`, `prior-art-evaluations.md`, `CLAUDE.md`, `packages/core/templates/**`, `.claude/skills/**`, `agents/**`).

**§1.7 fires ONLY IF** the Worker edits `docs/meta-factory/prior-art-evaluations.md` to record the DN-2 verdict (umbrella kickoff line 27). If so, the PR body MUST carry `### §1.7 Forward-check applied` + `### §1.7 Backward-check applied` (H3 headings, word "applied", ≥40 non-ws chars each, ≥1 `file:line` citation each). Pre-flight grep before `gh pr create`:

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2 (if doc edited)
```

If the DN-2 verdict is recorded as a commit trailer / runbook line only (no `prior-art-evaluations.md` edit), §4b is inert.

**Separate, ALWAYS-required (capability-commit gate, CLAUDE.md):** `AifFireBackend.ts` ≥80 LOC under `packages/` ⇒ capability commit ⇒ the **commit message** MUST carry a `Prior-art:` trailer citing the existing F2 SSOT entry (`prior-art-evaluations.md` F2 / `/fire` cluster). This is enforced by `.husky/pre-push` and is independent of §4b.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue. **Active canonical traps for this umbrella** (carried verbatim from umbrella kickoff lines 32–36):

- **T3** — no prose-only claims; every VERIFY line = command output or file:line.
- **T16** — `/fire`'s name resembles a dispatch backend, but its problem-class (fire-only, no read-back) ≠ a full backend's. Do NOT pattern-match it into a fake `awaitDone`. The honest dispatch-only design IS the countermeasure.
- **T19** — own adversarial cold-QA of the diff before handoff; CI ≠ design review.

**Domain-specific trap (NOT in canonical catalogue):**

- **T-Fire-A** — implementer tempted to make `awaitDone()` poll the `session_url` HTML or infer completion from git side-effects. **Forbidden:** the `/fire` per-routine token has no read access; any such poll is a hack outside the documented contract. `awaitDone` stays honest-fast (resolve immediately with a non-success terminal result carrying `session_url`, or throw a typed `BackendError`).

> Blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation. Enumeration + ≥1 domain trap present above.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (MANDATORY before Worker dispatch)

Per umbrella kickoff line 39, a Phase -1 cold-review over the kickoff is **mandatory before dispatch** (runtime-bridge code = blast radius). One REVISE round maximum (CLAUDE.md meta-orch self-review obligation).

```text
Agent: Phase -1 cold-review for f2-aif-fire-backend-iphase
Role: reviewer (read-only, adversarial) — NOT orchestrator, NOT implementer
Skill: requesting-code-review
Must verify: (1) §Type-decision (a vs b) is actually decided/decidable, not left open;
(2) the does-not-hang test is specified concretely; (3) NO CI path calls /fire (DN-2);
(4) agnostic core intact (packages/core imports nothing from runtime-bridge).
Surface any strategy fork as DECISION-NEEDED (Option A → X / Option B → Y); do NOT pick.
```

---

## §8 Stop conditions

- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Worker reports any Hard-constraint violation it cannot satisfy → halt, surface.
- `awaitDone()` cannot be made non-hanging within the documented `/fire` contract → STOP (this is the core design constraint; a hanging await is a BLOCKER, not a compromise).
- Any temptation to make the backend default, or to call `/fire` from `.github/workflows/*` → DN-2 / Hard-constraint-3 violation; stop.
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write the implementation code in THIS kickoff — it is the Worker's job (single pasted tab).
- Do NOT make `AifFireBackend` the default backend — REST stays default (Hard constraint 3).
- Do NOT add a CI workflow that calls `/fire` (DN-2 DEFER-permanent).
- Do NOT add npm deps (substrate purity; `no-paid-llm-in-ci.md` + BFR-default).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).

---

## §10 See also

- `.claude/orchestrator-prompts/f2-aif-fire-backend-iphase/kickoff.md` — the binding spec.
- `.claude/orchestrator-prompts/f2-fire-statusreadback-brainstorm/prompt.md` — origin brainstorm (DN-1=A+D resolved 2026-06-01).
- `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency source.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
