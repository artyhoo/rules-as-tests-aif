# KICKOFF — slow-test-triage (meta-launch)

> **Type:** Mixed (R-phase already shipped in PR #231; I-phase contingent on 3 maintainer DN verdicts). Generated by `/meta-orchestrator docs/meta-factory/research-patches/2026-05-25-slow-test-triage.md` on 2026-05-25.
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** decision artifacts (R-phase patch + DN verdicts) → conditional Mode A I-phase implementing chosen Option for Group R4 (+ optional Group P11 + testTimeout policy note).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Read in this order:

1. This kickoff (binding scope + stage gates + conditional launch-tables).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot (NOTE: §0 drift advisory below).
3. Umbrella kickoff at `.claude/orchestrator-prompts/slow-test-triage/kickoff.md` — R-phase scope.
4. PR #231 body + the proposed research-patch at `docs/meta-factory/research-patches/2026-05-25-slow-test-triage.md` (lands on `staging` only when PR #231 merges).

**Umbrella shape (read carefully — non-standard):** the R-phase is ALREADY SHIPPED in PR #231 (361-line research-patch, all 22 CI checks green as of 2026-05-25T18:15Z). There is no Stage 1 Worker to dispatch. The remaining work is:

- **Stage 1 (waiting on maintainer):** review + merge PR #231, AND issue verdicts on DN-1 / DN-2 / DN-3 (see §4 below). No Worker dispatch from this meta-orchestrator.
- **Stage 2 (conditional, post-merge):** dispatch a Mode A I-phase Worker per DN-1 verdict (Option A / B / C — different blast radius). DN-2 and DN-3 are either deferred or fold into the same I-phase PR per maintainer call.

---

## §1 Inputs

- **Plan-currency check:** PARTIAL — `wave-sequencing-plan.md §0` is current for the wave-status story but UNTRACKED for 12+ merged PRs since 2026-05-25 reconciliation (PRs #213/#214/#217/#218/#219/#220/#222/#223/#224/#225/#226/#227/#228/#229/#230 + kickoffs from `cross-worktree-sync-research/`, `mutation-discipline-umbrella-meta-launch/`, etc.). Surfaced as observation, NOT blocking this umbrella's dispatch — the slow-test-triage entry is not yet in §0 either (UNTRACKED), but its R-phase scope is self-contained (single PR, no cross-wave dependency on the untracked entries). The drift IS load-bearing for the `wave-sequencing-plan.md §0` integrity invariant — maintainer should reconcile in a separate `docs(wave-plan): reconcile 2026-05-25` PR (parallel to the precedent in PR #220).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/slow-test-triage/kickoff.md` (R-phase audit + recommendation; falsifier-driven; explicitly OUT of scope: I-phase implementation).
- **Stale items:** none for this umbrella; cross-cutting drift listed above.
- **Git HEAD at invocation:** `research/defer-reflex-r-phase-fresh` (different umbrella's branch — orchestrator session pivoted; meta-orchestrator artefacts persist regardless of current branch since `.claude/orchestrator-prompts/` is gitignored).

---

## §2 Launch-table (manual — kickoff has no §2/§3 table for the helper to parse)

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume | Status |
|---|---|---|---|---|---|---|---|
| R | R-phase audit | Mode A inline | No | 1 | — | M (361 LOC patch) | **DONE — PR #231 OPEN, CI green, awaiting maintainer merge + DN verdicts** |
| I-DN1-A | execution-build | Mode A inline | No | 2 | — | S (~10 LOC, 1 file) | CONDITIONAL — fires only if maintainer chooses DN-1 Option A (skip-conditional) |
| I-DN1-B | execution-build | Mode A inline | No | 2 | — | M (~20 LOC, 3 files: `audit-ai-docs.test.ts` + `package.json` + `audit-self.yml`) | CONDITIONAL — fires only if maintainer chooses DN-1 Option B (slow-suite extraction) |
| I-DN1-C | execution-build + light refactor | Mode A inline | No | 2 | — | M (~20 LOC, 2 files: `audit-ai-docs.ts` + test) | CONDITIONAL — fires only if maintainer chooses DN-1 Option C (inject execSync); R-phase verdict prefers C for semantic preservation but does NOT endorse — A/B/C all viable |
| I-DN2-A | none | — | — | — | — | 0 | CONDITIONAL — fires only if maintainer chooses DN-2 Option A (set re-evaluation threshold; documentation-only, can fold into a separate doc PR or skip) |
| I-DN2-B | execution-build | Mode A inline | No | 2 | I-DN1-{A,B,C} | M (~40 LOC, 1 file: `11-build-first-reuse-default.test.ts`) | CONDITIONAL — fires only if maintainer chooses DN-2 Option B (pre-emptive git-log batch optimization) |
| I-DN3-A | doc edit | Mode A inline (fold into DN-1/DN-2 PR) | No | 2 | I-DN1-{A,B,C}, I-DN2-{A,B} | XS (~1 LOC, 1 file: `vitest.config.ts`) | CONDITIONAL — fires only if maintainer chooses DN-3 Option A (explicit 120s ceiling comment) |
| I-DN3-B | none | — | — | — | — | 0 | CONDITIONAL — fires only if maintainer chooses DN-3 Option B (keep current state) |

**Note on Stage column:** Stage 1 was Worker-dispatched directly via `/orchestrator .claude/orchestrator-prompts/slow-test-triage/kickoff.md` (per kickoff §5 "How to start"), BEFORE this meta-orchestrator invocation. There is no Stage 1 to dispatch from here — only Stage 2 contingent on maintainer answers.

**Note on parallelism:** all I-phase sub-waves are Mode A inline (S/M volume, single-file blast radii). No Mode B worktrees needed. If maintainer picks DN-1 Option B (3-file change) + DN-3 Option A (1-line comment), they fold into one PR; the launch-table's "parallel sibling" column accounts for serialization through `vitest.config.ts` + `audit-self.yml` shared state.

---

## §3 Stage gates (real git + maintainer-answer checks)

### Stage 1 → Stage 2

Stage 2 dispatch waits for **two distinct events**:

**(a) PR #231 merged to `staging`:**

```bash
gh pr list --search 'is:merged head:research/slow-test-triage base:staging' \
  --json number,title,mergedAt --limit 5
```

Expected: non-empty JSON listing PR #231 with `mergedAt` non-null.
If empty → halt. Do NOT dispatch any I-phase Worker.

**(b) Maintainer verdicts collected on DN-1, DN-2, DN-3:**

This gate is NOT mechanical — it lives in `state.md §2 Decisions` and must be filled by hand by the orchestrator when the maintainer answers. The §2 table starts empty (rows present, content `<pending>`). No I-phase dispatch until all three rows are populated with explicit Option letters AND a `<rationale>` from the maintainer.

Falsifier (rationalization to refuse): "PR #231 merged so all DNs are implicitly approved" — NO. Merging the R-phase only confirms the *research* is accepted; it does NOT pick between the per-DN options (the patch explicitly does not endorse one per `reviewer-discipline.md §2`). Dispatching Stage 2 without explicit DN answers = `#strategy-decided-by-reviewer` anti-pattern.

### Stage 2 → done

After the chosen-Option Worker PR(s) merge to `staging` AND pre-push runs locally with no timeout regression on Group R4 (verified by maintainer or follow-up Worker on a representative cold-cache machine):

```bash
# Verify chosen-Option PR(s) merged
gh pr list --search 'is:merged head:fix/slow-test-triage-iphase base:staging' \
  --json number,title,mergedAt --limit 5
# (or whatever branch name the I-phase Worker uses; pattern: fix/slow-test-triage-* or refactor/slow-test-triage-*)
```

**Phase -1 cold-review between Stage 1 (PR #231) and Stage 2 (I-phase) is MANDATORY** (see §7 below). Cold-review acts twice in this umbrella:
1. Before maintainer merges PR #231 (R-phase substance review — done at PR review time, not a separate session).
2. Before dispatching Stage 2 I-phase Worker — fresh reviewer reads chosen Option's diff against the patch's `§2 Per-test recommendations` to verify the implementation matches the chosen verdict.

---

## §4 Sub-wave dispatch instructions

### Stage 1 (R-phase): NOTHING TO DISPATCH

PR #231 already exists. The Worker session that produced it has handed off; its output is the open PR. No further Worker action until the PR merges + DN verdicts arrive.

**Maintainer's next action (out-of-band):** open PR #231, read the body's 3 DN items + 3 ATTN observations, leave a review verdict and/or merge, then answer DN-1 / DN-2 / DN-3 in the orchestrator session OR by editing `state.md §2 Decisions` directly.

### Stage 2 (I-phase): dispatch ONE Worker per the chosen DN-1 Option

Once DN-1 verdict arrives, paste ONE of the following 1-liners into a NEW Claude Code tab (Mode A inline; foreground; subscription-billed; no Agent tool from this session — `#worker-dispatch-via-subagent` anti-scope per §8 of `SKILL.md`):

#### Path A — if DN-1 = Option A (skip-conditional)

```text
/orchestrator implement DN-1 Option A from docs/meta-factory/research-patches/2026-05-25-slow-test-triage.md — add `it.skipIf(process.env['SKIP_NPX_TESTS'] === '1')` to the 3 probeR4 slow tests at packages/core/audit-self/audit-ai-docs.test.ts (one at :344 + two probeR4 suite siblings). Set `SKIP_NPX_TESTS=0` explicitly in audit-self.yml so CI always runs them. Verify pre-push runs locally with SKIP_NPX_TESTS=1 set (should skip), unset (should run). Mode: Mode A inline, single PR. Include §1.7 PR-body sections per §4b mandate (paths-triggered: edits a test file under packages/core — IF the I-phase also touches .claude/rules/ or principles, §1.7 is required; otherwise §1.7 Skipped: <≥60 chars> with rationale per `phase-research-coverage §1.7`). Apply T-trap counters T3 / T7 / T15 / T20 + T-SlowTestTriage-I (no testTimeout regression — the fix must eliminate timeout risk on cold-cache, not paper over with a longer timeout).
```

#### Path B — if DN-1 = Option B (move to slow-suite)

```text
/orchestrator implement DN-1 Option B from docs/meta-factory/research-patches/2026-05-25-slow-test-triage.md — extract the 3 probeR4 slow tests to a new `test:slow` script in packages/core/package.json (`vitest run audit-self/audit-ai-docs.test.ts --testNamePattern='probeR4'`). Update pre-push to skip the probeR4 tests (alter the existing `npx vitest run packages/core/audit-self/audit-ai-docs.test.ts` invocation to use `--testNamePattern='^(?!.*probeR4)'` or equivalent). Add a CI job in .github/workflows/audit-self.yml that runs `pnpm --prefix packages/core run test:slow` and add it to the `ci-success` aggregate `needs:` list. Mode: Mode A inline, single PR (3 files: test, package.json, audit-self.yml). §1.7 Skipped: <≥60 chars> rationale if no rule/principle touched. T-trap counters: T3 (measure post-change pre-push duration on warm and cold cache) / T7 (adversarial: what test surface did I miss after the move?) / T10 (re-enumerate pre-push test surface; confirm the move removed exactly 3 tests, not 4) / T15 (move doesn't add a new slow test) / T20 (every dispatch claim cites file:line).
```

#### Path C — if DN-1 = Option C (inject execSync)

```text
/orchestrator implement DN-1 Option C from docs/meta-factory/research-patches/2026-05-25-slow-test-triage.md — refactor packages/core/audit-self/audit-ai-docs.ts probeR4() to accept an optional `execSync` injection parameter (signature: `probeR4(opts?: { execSync?: typeof execSync }) → ...`). Default to real execSync when omitted. In the test file, inject a fast stub for the 2 warm-path tests (`hasTsMorph present path`, `hasTsconfig && !hasTsMorph path`) but KEEP the real execSync for the cold-path contract test at :344. Mode: Mode A inline, single PR (2 files: audit-ai-docs.ts + audit-ai-docs.test.ts). §1.7 Skipped: <≥60 chars> rationale (refactor only; no discipline rule touched). T-trap counters: T3 / T7 (adversarial: does the injection point miss any call site? grep for execSync in audit-ai-docs.ts and verify all paths) / T15 (refactor doesn't add slow tests) / T16 (the «inject execSync» pattern is dependency-injection; verify upstream problem-class match — vitest's vi.mock is the upstream equivalent, with different semantics; see `dual-implementation-discipline §3` if extracting to a util) / T20.
```

#### DN-2 / DN-3 follow-ups (deferred — fold into above OR separate PRs per maintainer preference)

- **DN-2 Option A** (set re-evaluation threshold) → no code change; record threshold in `wave-sequencing-plan.md` under a new "Watch list" subsection OR in a new `docs/meta-factory/watch-list.md`. Doc PR, ~10 LOC.
- **DN-2 Option B** (pre-emptive git-log batch optimization for principle 11) → standalone Mode A inline; ~40 LOC in `11-build-first-reuse-default.test.ts`; needs its own §1.7 PR-body section (touches `packages/core/principles/**` — §4b mandate fires).
- **DN-3 Option A** (1-line ceiling comment in `vitest.config.ts`) → can fold into the DN-1 PR (same file is not touched, but the change is mechanical and atomic with the rest of the slow-test fix).
- **DN-3 Option B** (keep current state) → no action.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- [doc-authority-hierarchy.md](.claude/rules/doc-authority-hierarchy.md) — <what was checked>. file:line evidence: `<path>:<line>`
- [phase-research-coverage.md §1.12](.claude/rules/phase-research-coverage.md) — <what was checked>. file:line evidence: `<path>:<line>`
- [build-first-reuse-default.md](.claude/rules/build-first-reuse-default.md) — <verdict + rationale>
- [no-paid-llm-in-ci.md](.claude/rules/no-paid-llm-in-ci.md) — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the new rule's scope>

- `<existing-artefact-name>` — <how this PR interacts; supersedes / extends / orthogonal>. file:line evidence: `<path>:<line>`
- <repeat for each touched artefact>
```

**Hard rules (each one has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2 (`##`).** Awk regex `^### §1\.7 ...` — H2 silently fails. (Memory: 6× recurrence as of 2026-05-25; PRs #58, #69, #105, #111, #212, #215.)
2. **Word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 file:line citation per section.**
5. **Mechanical-maintenance escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` — must specify WHY no discipline surface applies.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'   # must be 2 (or 1 Skipped line)
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c   # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c   # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'   # ≥2
```

**Path scope for THIS umbrella's I-phase:**

- Path A (skip-conditional, edits `audit-ai-docs.test.ts` only) → §4b does NOT fire (no path match) → `§1.7 Skipped: ...` is sufficient.
- Path B (3-file change including `audit-self.yml`) → §4b does NOT fire (CI yml not on the path list) → `§1.7 Skipped: ...` is sufficient.
- Path C (refactors `audit-ai-docs.ts`) → §4b does NOT fire → `§1.7 Skipped: ...` is sufficient.
- DN-2 Option B (touches `packages/core/principles/11*.test.ts`) → §4b **DOES fire** → Forward + Backward sections mandatory.
- DN-3 Option A (1-line comment in `vitest.config.ts`) → §4b does NOT fire → `§1.7 Skipped: ...` is sufficient.

---

## §4a Worker worktree setup (FIRST step in any worker session — Gap-3 round-2 follow-up)

If the worker is dispatched into a `git worktree add`-created directory (default for parallel sub-waves per `parallel-subwave-isolation.md §1`), reconstitute the `node_modules` plumbing the orchestrator's primary workdir already has. Without it, `npm` / test / build commands fail.

**Run BEFORE any `npm` / test / build command (idempotent — no-op in primary workdir):**

```bash
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY_WORKDIR=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY_WORKDIR/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
  echo "worktree setup OK — node_modules + packages/core/node_modules linked to primary"
fi
```

Note: this umbrella's I-phase sub-waves are all Mode A inline (no Mode B parallel) — worktree-setup script is included for completeness but typically not triggered.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See [`.claude/rules/ai-laziness-traps.md §2`](../../../.claude/rules/ai-laziness-traps.md) for full catalogue.

**Active canonical traps for slow-test-triage:**

- **T3 — Verify durations, don't claim from memory.** Already enforced in R-phase patch §1 (two-run measurement). Carries forward to I-phase: any post-change duration claim must come from a fresh measurement on the same machine, NOT extrapolated from R-phase numbers.
- **T7 — Don't just match-and-move-on (adversarial check).** R-phase ran "what test surface did I miss?" and confirmed population is complete. I-phase must re-run this counter-prompt against ITS chosen Option's implementation: "what call site / config / hook path did I miss when wiring the skip-condition / slow-suite / inject?".
- **T10 — Enumerate full population before recommending.** R-phase enumerated 2 surfaces × 7 tests ≥1s. I-phase enumerate: any other test that calls `execSync('npx ...')` AND is invoked by pre-push? grep before claiming the fix is complete.
- **T15 — Self-application: the slow-test fix doesn't itself introduce a slow test.** Load-bearing. Any test added by the I-phase must measure <100ms warm-cache AND not be subprocess-bound to a cold-cache npx call.
- **T20 — Inline-verdict-without-evidence.** Every "this approach works" claim in the I-phase PR body must cite a file:line + measured duration / CI run.

**Domain-specific traps (slow-test-triage-specific, NOT in canonical catalogue):**

- **T-SlowTestTriage-A — Warm-cache numbers masquerading as representative.** R-phase already documents this (patch §6). Carry forward to I-phase: every post-change duration claim must report BOTH warm-cache AND a cold-cache estimate (or explicitly state the cold-cache case is no longer reachable because the test is skipped / moved / mocked).
- **T-SlowTestTriage-I — testTimeout regression (NEW for I-phase).** Trigger: I-phase still flakes in some environment after the chosen Option ships → AI tempted to bump `testTimeout` further. Counter: if the chosen Option doesn't eliminate the cold-cache risk, the FIX is wrong — not the timeout ceiling. DN-3 Option A (explicit 120s ceiling) is a policy, not an escape hatch. Bumping past the ceiling = surface to maintainer, do NOT bump silently.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation (pattern-matching the prompt instead of reasoning against it). Kickoff authors MUST enumerate T-numbers AND add ≥1 domain-specific trap — satisfied above.

---

## §6 Recursive self-application

This kickoff was generated by `/meta-orchestrator` and must itself pass `principle 12` (AI-laziness-traps format validation, `packages/core/principles/12-ai-laziness-traps.test.ts`). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

Self-check for THIS umbrella: this meta-kickoff itself does not introduce a slow test (it's a markdown file). T15 satisfied.

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage 2 I-phase, invoke (in a separate session, NOT via Agent tool from a write-task channel):

```text
Session: Phase -1 cold-review for slow-test-triage Stage 1 → Stage 2 transition
Role: reviewer
Skill: requesting-code-review
Scope: read the merged PR #231 patch + the chosen-Option Worker's draft PR diff; verify implementation matches the recommendation for DN-1 Option <A/B/C> as written in the patch §2; surface BLOCKER/MAJOR/MINOR.
```

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED («Option A → consequence X / Option B → consequence Y»). Does NOT pick strategy. See [`reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md).

**Cold-review specifically checks:**
1. Chosen Option implementation matches the patch §2 recommendation verbatim (file:line, signature, env-var name, semantic).
2. T-SlowTestTriage-A discipline: PR body cites BOTH warm- and cold-cache post-change durations (or explicitly states why cold-cache no longer applies).
3. T-SlowTestTriage-I discipline: post-change pre-push run produces NO `testTimeout` bump anywhere in the diff.
4. §1.7 PR-body shape if §4b path-scope fires (DN-2 Option B only).

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block whose CONTENT follows the orchestrator-checkpoint substance shape — distinct from the per-turn generic substance the end-of-turn hook already enforces. Substance spec: see SKILL.md §10.3a table.

**Anti-duplication discipline:** block content names sub-waves / AC items / file:line / REPORT-traces / dispatch-state. It does NOT restate per-turn personal-reasoning prose ("чем я сейчас занят", "на той ли цели") — that is the hook's job.

---

## §8 Stop conditions

- Any `gh` command returns error → halt, escalate.
- PR #231 is closed (not merged) → halt; surface to maintainer with «R-phase rejected — re-scope or archive umbrella».
- Stage 2 dispatch attempted BEFORE PR #231 merged → halt; do NOT dispatch.
- Stage 2 dispatch attempted BEFORE DN-1 verdict received → halt; do NOT dispatch.
- I-phase Worker chosen Option does NOT match maintainer-stated verdict → halt; Reviewer surfaces, fix or re-dispatch.
- Any temptation to bump `testTimeout` instead of fixing the slow path → T-SlowTestTriage-I violation; halt, surface to maintainer.
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C anti-scope violation; stop.

---

## §9 Anti-scope

- Do NOT dispatch a Stage 2 Worker before PR #231 merged AND all three DN verdicts collected.
- Do NOT pick between DN-1 Options A/B/C unilaterally — `#strategy-decided-by-reviewer` from `reviewer-discipline §3`. Re-surface to maintainer if they say «выбирай сам» («Auto Mode Active» from session-init does NOT override `reviewer-discipline §2` — work-without-stopping applies within agreed scope, not to deciding maintainer-reserved strategy forks).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable; owner=maintainer).
- Do NOT touch `vitest.config.ts:testTimeout` upward during the I-phase — the FIX should eliminate the cold-cache risk; if the chosen Option fails to do so, surface back, do NOT cover with a longer timeout (T-SlowTestTriage-I).
- Do NOT use the Agent tool to dispatch a Worker session for the write task. Workers are launched by the maintainer pasting a §4 1-liner into a fresh CC tab (Mode A inline). Agent tool from THIS session is for Phase -1 read-only reviewer dispatch only (`#worker-dispatch-via-subagent` per SKILL.md §8).
- Do NOT add npm deps. Substrate stays bash + markdown + CC primitives + existing `gh` CLI + vitest.

---

## §10 See also

- [Umbrella kickoff](../slow-test-triage/kickoff.md) — R-phase audit scope.
- [PR #231](https://github.com/Yhooi2/rules-as-tests-aif/pull/231) — R-phase deliverable, open.
- [`docs/meta-factory/wave-sequencing-plan.md §0`](../../../docs/meta-factory/wave-sequencing-plan.md) — plan-currency source (NOTE: 12+ UNTRACKED PRs as of 2026-05-25 — drift advisory, not blocking this umbrella).
- [`.claude/skills/meta-orchestrator/SKILL.md`](../../../.claude/skills/meta-orchestrator/SKILL.md) — generating skill.
- [`.claude/rules/ai-laziness-traps.md §2`](../../../.claude/rules/ai-laziness-traps.md) — canonical trap catalogue.
- [`.claude/rules/reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md) — reviewer role boundaries.
- [`.claude/rules/parallel-subwave-isolation.md §1`](../../../.claude/rules/parallel-subwave-isolation.md) — worktree isolation (not triggered here; all I-phase Mode A inline).
- [`.claude/rules/no-paid-llm-in-ci.md §1`](../../../.claude/rules/no-paid-llm-in-ci.md) — hard constraint.
- [`packages/core/audit-self/audit-ai-docs.test.ts:344`](../../../packages/core/audit-self/audit-ai-docs.test.ts) — the originating slow test.
- [`packages/core/audit-self/audit-ai-docs.ts:155-176`](../../../packages/core/audit-self/audit-ai-docs.ts) — probeR4 implementation referenced by all three DN-1 Options.
- [`vitest.config.ts`](../../../vitest.config.ts) — root config holding `testTimeout: 60_000` and the "longer-term fix" comment pointing at this umbrella's research-patch.
