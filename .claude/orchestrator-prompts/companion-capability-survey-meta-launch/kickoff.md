# KICKOFF — companion-capability-survey (meta-launch)

> **Type:** R-phase (research → research-patch) — generated by `/meta-orchestrator companion-capability-survey` on 2026-06-01.
> **Origin:** plan-currency check + §2.5 dedup (CLEAR) + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** `docs/meta-factory/research-patches/2026-06-01-companion-capability-survey.md` — capability×stage×BFR-verdict survey of Superpowers / Superset / aif-handoff for `/meta-orchestrator` pipeline quality, + SSOT additive rows.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

This is a **single-session R-phase** — one Opus Mode-A session producing one research-patch. There are no parallel sub-waves and no inter-stage gates. Read in this order:

1. This meta-kickoff (binding scope + dispatch + traps).
2. **The actual umbrella kickoff** `.claude/orchestrator-prompts/companion-capability-survey/kickoff.md` — the real method (§1 lens, §2 seed candidates, §3 method, §4 output shape). **It is the source of truth for the research content;** this meta-kickoff only wraps dispatch discipline around it.
3. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

---

## §1 Inputs

- **Plan-currency check:** План актуален (as of 2026-06-01 `86d2153`). Branch `meta-orchestrator-survey-2` is current with `origin/staging` (0 ahead / 0 behind). Zero open PRs. Umbrella kickoff EXISTS.
- **Dedup (§2.5):** `OK: companion-capability-survey no dup-detect signal` — CLEAR. No deliverable on staging yet; no INFLIGHT (zero open PRs).
- **Scope-overlap (load-bearing — NOT a dup, but a re-survey hazard):** three prior companion surveys already landed deliverables on staging:
  - `companion-reuse-deep-dive` (#248–251 + #307 done.md) → `2026-05-26-companion-reuse-{aif-handoff-autoqueue,devin-replan,omo-deep-dive,superpowers-scope}.md`
  - `companion-integration-analysis` → `2026-05-20-companion-integration-analysis.md`
  - `companion-target-comparison` → `2026-05-16-companion-target-comparison.md`
  - **The Worker MUST extend/cite these, not re-survey** (kickoff §3.1 SSOT-first + §6.5 reconcile). A candidate already verdicted in `prior-art-evaluations.md` is **cited, not re-evaluated** (T18-adjacent: don't redo settled work).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/companion-capability-survey/kickoff.md`
- **Stale items:** None blocking. (`wave-sequencing-plan.md §0` last reconciled 2026-05-29; PRs #271–#320 not yet folded in — that refresh is a separate umbrella's scope, does not gate this R-phase.)

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A (the survey) | R-phase (research→patch) | Mode A inline (Opus) | No | 1 | — | M |

Single sub-wave. SDD=No (research, not a ≥3-task build). Mode A inline per §5 dispatch tree row «R-phase, single». Volume M (one research-patch, MCP-heavy: DeepWiki ≥3 phrasings × 3 companions + WebSearch + context7).

---

## §3 Stage gates

**No inter-stage gates** — single-session R-phase. The only gate is the **pre-handoff self-QA (T19)**: before the Worker hands the research-patch back / opens the PR, it runs its own adversarial cold-review of the patch (claims grounded in file:line / DeepWiki link / command output; no prose-only verdicts per T3). CI ≠ design review.

Verification that the deliverable landed (run after the PR merges, NOT a dispatch precondition):

```bash
gh pr list --search "is:merged head:research/companion-capability-survey base:staging" --json number,title,mergedAt,headRefName --limit 5
```

---

## §4 Sub-wave dispatch instructions

**Primary (recommended): Mode A inline, fresh CC tab.** Paste this 1-liner into a new Claude Code session on this repo:

```text
/orchestrator companion-capability-survey §dispatch — Mode A R-phase, single Opus session.
Read .claude/orchestrator-prompts/companion-capability-survey/kickoff.md (the real method) +
companion-capability-survey-meta-launch/kickoff.md (§1 scope-overlap + §4b §1.7 mandate). Output the
research-patch at docs/meta-factory/research-patches/2026-06-01-companion-capability-survey.md +
SSOT additive rows; open PR head=research/companion-capability-survey base=staging.
```

**Why Mode A and not autonomous aif-handoff:** the runtime-bridge is up (`RUNTIME_BRIDGE_MODE` set, responds 404 at root = server live), so autonomous dispatch is *available* (`tsx packages/runtime-bridge/src/cli/dispatch.ts <this-kickoff>`). It is **not recommended for this umbrella**: this is an MCP-heavy research task (DeepWiki / WebSearch / context7 + live BFR-verdict judgment), and aif-handoff is an *implementation* agent whose headless MCP access is unreliable (memory `project_aif_autonomous_fully_configured` + workflow MCP caveat). T16: aif's problem-class = «implement a spec with post-hoc review»; ours = «survey + judge external capabilities with live MCP» — **mismatch**. If the maintainer still wants autonomous, §4c below is MANDATORY first.

---

## §4b §1.7 PR-body authoring mandate (APPLIES — target touches `docs/meta-factory/prior-art-evaluations.md`)

The research-patch adds SSOT additive rows to `docs/meta-factory/prior-art-evaluations.md` (kickoff §4 item §5) — that path is on the §1.7 trigger list. The PR body MUST carry:

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars: which existing disciplines were checked>

- `build-first-reuse-default.md §3` — survey ran the 6-layer mechanism before any ADOPT verdict. file:line evidence: `<patch>:<line>`
- `phase-research-coverage.md §1.7` — negative-existence claims carry the 6-item search check. file:line evidence: `<patch>:<line>`
- `prior-art-evaluations.md §3` — new rows are append-only, carry Verdict + Rationale + Trigger. file:line evidence: `<patch>:<line>`
- `no-paid-llm-in-ci.md` — verdict: research is session-bound, zero CI LLM cost.

### §1.7 Backward-check applied

<≥40 non-whitespace chars: the sweep of prior companion surveys this patch interacts with>

- `2026-05-26-companion-reuse-*.md` — extends / cites, does not re-survey. file:line evidence: `<patch>:<line>`
- `2026-05-20-companion-integration-analysis.md` — <supersedes / orthogonal>. file:line evidence: `<patch>:<line>`
```

**Hard rules** (each has historically failed CI): heading depth = **H3 `###`** (not H2); word **«applied»** required; **≥40 non-whitespace chars** per section; **≥1 `file:line` citation per section** (regex `[^[:space:]]+\.[a-z]+:[0-9]+`). Escape hatch only if the PR genuinely has no discipline surface: `### §1.7 Skipped: <rationale ≥60 chars>` — but this PR DOES (SSOT rows), so do not skip.

Pre-flight grep before `gh pr create`:

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'   # must be 2
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'                  # must be ≥2
```

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess (INERT unless maintainer chooses autonomous)

This umbrella's recommended path is Mode A paste-tab (§4). §4c is live **only** if the maintainer overrides to `tsx packages/runtime-bridge/src/cli/dispatch.ts`. If so, set before dispatch:

```bash
export AGENT_MAX_REVIEW_ITERATIONS=1
export AGENT_AUTO_REVIEW_STRATEGY=closure_first
export AGENT_SKIP_REVIEW=false
```

And the dispatched kickoff carries verbatim, addressed to the aif agent:

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork (two defensible BFR verdicts for one capability, an ambiguous problem-class match, a seed claim you cannot source-confirm) — **do NOT pick.** Park it (`manualReviewRequired`, fork stated «Option A → consequence X / Option B → consequence Y») and stop that task. Proceed only on the unambiguous parts. Guessing a fork to keep moving is the failure this loop exists to prevent.

Pre-dispatch gate: `grep -q 'park it' <this-kickoff>` AND `echo "$AGENT_MAX_REVIEW_ITERATIONS"` non-empty → else STOP.

---

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §2`)

**Active canonical traps** (carried verbatim from the umbrella kickoff §5):

- **T3** — every claim = source file:line / DeepWiki link / command output. No prose-only findings.
- **T5** — research-patch only, NO adoption code. If you open `Edit` on a source file, stop.
- **T11 / T12** — BFR 6-layer + WebSearch before any «adopt» verdict; do not survey from training-data memory.
- **T13** — ADOPTED-from-companion ≠ zero-work; verify the upstream's own evidence.
- **T16** — problem-class match per candidate («their problem: X; our pipeline need: Y; match? evidence»). The headline trap: a skill «sounds useful» but must map to a real pipeline gap.
- **T19** — own cold-QA on the patch before handoff (CI ≠ design review).
- **T20** — no verdict without a tool call in the same turn.

**Domain-specific traps (companion-capability-survey-specific):**

- **T-CCS-A** — tempted to ADOPT a Superpowers skill because it's elegant, without a concrete pipeline gap. Counter: §1 lens requires naming the stage + the gap; no gap → REJECT/drop, not ADOPT.
- **T-CCS-B** — trusting the §2 seed list as fact. Counter: §2 is unverified single-session DeepWiki (wrong once already); re-confirm each against source before it enters the table.
- **T-CCS-C (meta-launch addition)** — re-surveying a capability already verdicted in `prior-art-evaluations.md` or in the three prior companion surveys (§1 above). Counter: SSOT-first (kickoff §3.1) — cite the existing row, extend its `Trigger to revisit`; do not produce a fresh verdict for settled rows.

> Blanket «see ai-laziness-traps.md» without this enumeration = T7 violation. The enumeration above + T-CCS-A/B/C satisfies §3 of the rule.

---

## §6 Recursive self-application

This meta-kickoff was generated by `/meta-orchestrator` and must pass principle 12 (AI-laziness-traps format). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer

Single-session R-phase has no inter-stage transition, so the between-stages Phase -1 dispatch is N/A. The equivalent discipline is **T19 self-QA** (§3): the Worker runs its own adversarial cold-review of the research-patch before handoff. If the maintainer wants an independent second opinion, dispatch a read-only reviewer via Agent tool on the merged patch (reviewer surfaces strategy forks as DECISION-NEEDED, does not pick — `reviewer-discipline.md §2`).

---

## §8 Stop conditions

- DeepWiki / WebSearch returns nothing for a load-bearing seed claim → mark `INCONCLUSIVE-needs-human`, do not fabricate (T3).
- Any temptation to start adopting a capability in this R-phase → STOP (T5 / kickoff §4 «do NOT start adopting»).
- A genuine «adopt A or B» strategy fork → surface as DECISION-NEEDED, maintainer decides (reviewer-discipline §2); do not pick.
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write adoption / implementation code — this is research (T5).
- Do NOT re-survey capabilities already verdicted (cite + extend — T-CCS-C).
- Do NOT finalize adoption strategy — verdicts are recommendations; maintainer decides.
- Do NOT add npm deps; do NOT modify `~/.claude/skills/orchestrator/`.

---

## §10 See also

- `.claude/orchestrator-prompts/companion-capability-survey/kickoff.md` — the real R-phase method (source of truth).
- `docs/meta-factory/prior-art-evaluations.md` — SSOT (extend, don't dup).
- `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency source.
- `.claude/rules/ai-laziness-traps.md §2` · `build-first-reuse-default.md §3` · `phase-research-coverage.md §1.7` · `reviewer-discipline.md §2`.

## 🟢 Простыми словами

Это умбрелла-обёртка для одной исследовательской сессии: «что в наших компаньонах (Superpowers / Superset / aif-handoff) мы ещё не используем, но что улучшило бы пайплайн `/meta-orchestrator` — и стоит ли это брать по build-first-reuse». Я проверил: план актуален, дублей нет (dup-detect = OK), но **три похожих обзора уже сделаны раньше** — Worker обязан их цитировать и расширять, а не переделывать. Это R-phase: один Opus-сеанс пишет один research-patch, никакого кода. Мост aif поднят, но автономный режим тут не советую — задача завязана на DeepWiki/WebSearch, а aif — агент для реализации, не для research с живыми MCP. §1.7-мандат в PR обязателен (патч трогает `prior-art-evaluations.md`). Дальше — твой ход: вставь 1-liner из §4 в новый CC-таб.
