# KICKOFF — aif-init-passport-gen (meta-launch)

> **Type:** R-phase / design-first (single session) (generated by `/pipeline aif-init-passport-gen` on 2026-06-17).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill.
> **Deliverable:** a design decision + prior-art SSOT entries + research-patch resolving the 4 open design questions (delivery channel · run-timing · deterministic-vs-LLM split · review gate) so a subsequent I-phase can implement the AI-driven project-passport generator (GH #547 Point 1).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.
3. Per-umbrella kickoff at `.claude/orchestrator-prompts/aif-init-passport-gen/kickoff.md` (the substantive brief — Why / Goal / 4 design questions / prior-art consult / AI-traps).

---

## §1 Inputs

- **Plan-currency check:** Plan is current (as of 2026-06-17 e57fdbb).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/aif-init-passport-gen/kickoff.md`
- **Stale items:** None.
- **In-flight ledger:** CLEAR — no open PR, no branch, no ahead-commits carrying this umbrella's slug (umbrella kickoff itself merged via #599; that is authoring, not in-flight work).
- **Classification:** design-first feature, scope NOT decided (4 open design questions, brainstorm mandated) → routes to **RESEARCH / Mode A inline**. Single design session, no parallel siblings.

---

## §2 Launch-table

Launch table — aif-init-passport-gen (as of e57fdbb):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A | R-phase (design-first) | Mode A inline | No | 1 | — | M |

**Why single sub-wave / RESEARCH:** the umbrella kickoff explicitly mandates `superpowers:brainstorming` at session start and forbids pre-deciding the 4 design questions. The design space (delivery channel, run-timing, deterministic-vs-LLM boundary, review gate) is open, so `scope_decided=FALSE` → RESEARCH. Implementation is a **separate future dispatch** (re-invoke `/pipeline aif-init-passport-gen` once the design PR lands and scope is decided).

**Why Mode A interactive (NOT autonomous aif):** the 4 design questions require maintainer input; brainstorming is interactive by nature. aif-handoff has no mid-implementation pause-and-ask primitive (it guesses forks silently — design §1, `coordinator.ts:398-476`), so a design-first session is the wrong fit for autonomous dispatch regardless of bridge state. Bridge is also currently unset (`RUNTIME_BRIDGE_MODE` empty) → paste-tab only.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 (design) → Stage 2 (implementation)

Stage 2 (implementation) MUST NOT be dispatched until the Stage 1 design deliverable is merged to `staging` — i.e. a PR that (a) records prior-art verdicts in `docs/meta-factory/prior-art-evaluations.md`, (b) resolves the 4 design questions, and (c) lands a research-patch / design doc under `docs/meta-factory/research-patches/` (or `docs/superpowers/specs/`):

```bash
gh pr list --search "is:merged head:aif-init-passport-gen-design base:staging" --json number,title,mergedAt,headRefName --limit 10 2>/dev/null || echo "gh unavailable — cannot verify stage gate"
```

Replace `aif-init-passport-gen-design` with the actual head branch the design session opened. Expected: non-empty JSON (the merged design PR). If empty → halt; do NOT dispatch the implementation I-phase. Re-running `/pipeline aif-init-passport-gen` after the design PR merges will re-classify (scope_decided=TRUE) and emit the implementation launch-table.

**Phase -1 cold-review before the implementation dispatch is MANDATORY** (see §7 below). For the design session itself there is no preceding stage to gate.

---

## §4 Sub-wave dispatch instructions

**Sub-wave A — design / R-phase (Mode A inline, interactive):**

Open a fresh CC session (maintainer pastes the §10 1-liner). The session MUST:

1. **Brainstorm first** — invoke `superpowers:brainstorming` on the 4 design questions in the umbrella kickoff §"Design questions". Do NOT pre-decide; surface genuine forks to the maintainer via `AskUserQuestion`.
2. **Prior-art consult (mandatory, before any "I propose…")** — per `build-first-reuse-default.md §3`: DeepWiki `ask_question` ≥3 phrasings + WebSearch ≥3 phrasings on "AI/LLM repo-aware project-context / passport generation" (`aider --init`/repo-map, Cursor `.cursorrules` generators, `gitingest`/repo-summarisers, CC `/init` CLAUDE.md generation). **Own-stack-first:** evaluate ADOPT/ADAPT/REFERENCE of CC `/init` and the existing `tool-bootstrapping` skill (already reads `package.json` deps) before proposing bespoke generation. Record verdicts in `docs/meta-factory/prior-art-evaluations.md` (SSOT).
3. **Decide & document** — resolve the 4 design questions; capture the decision + rationale in a research-patch under `docs/meta-factory/research-patches/2026-06-<dd>-aif-init-passport-gen-design.md` (or a spec under `docs/superpowers/specs/`). Define the deterministic-detection core (testable) vs LLM-synthesis boundary, and the "draft, please review" signalling.
4. **Acceptance for the design session:** the 4 design questions are answered with maintainer-confirmed verdicts, prior-art SSOT rows exist, and the implementation scope (delivery channel + run-timing + detection contract + ≥2 fixture-shapes for the acceptance test) is decided enough that the next `/pipeline` invocation classifies it as a scoped I-phase.

This is a design + research session — it produces a Markdown decision/patch, NOT implementation code (T5). If you reach for `Edit` on a generator source file, stop — that is the I-phase.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to the design PR + the eventual implementation PR — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

The design PR touches `docs/meta-factory/prior-art-evaluations.md` → §1.7 applies. The implementation PR will likely touch `.claude/skills/**` / `agents/**` / `packages/core/templates/**` → §1.7 applies.

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- [doc-authority-hierarchy.md](.claude/rules/doc-authority-hierarchy.md) — <what was checked>. file:line evidence: <path>:<line>
- [phase-research-coverage.md §1.12](.claude/rules/phase-research-coverage.md) — <what was checked>. file:line evidence: <path>:<line>
- [build-first-reuse-default.md](.claude/rules/build-first-reuse-default.md) — <verdict + rationale>
- [no-paid-llm-in-ci.md](.claude/rules/no-paid-llm-in-ci.md) — <verdict>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the new scope>

- <existing-artefact-name> — <how this PR interacts; supersedes / extends / orthogonal>. file:line evidence: <path>:<line>
- <repeat for each touched artefact>
```

**Hard rules (each has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2.** CI awk regex matches `^### §1\.7 ...`.
2. **Word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 file:line citation per section** matching `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **Mechanical-maintenance escape hatch:** `### §1.7 Skipped: <rationale ≥60 chars>` — only for purely structural PRs with no discipline surface.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2
```

**If a sub-wave target file does NOT match the path list above:** ignore §4b. The mandate is path-scoped.

---

## §4c Autonomous aif-handoff dispatch — park-don't-guess contract (LIVE)

> **Operator elected autonomous aif dispatch** (overriding the §2 interactive recommendation, 2026-06-17). This section governs. Because this is a design-first feature with 4 genuine design-question forks, the park-don't-guess levers below are **non-negotiable** — without them aif guesses the forks silently (`coordinator.ts:398-476`; review auto-closes on "no blockers", not "a human is sure").

**Lever 1 — conservative aif config (set on the env BEFORE dispatch):**

```bash
export AGENT_MAX_REVIEW_ITERATIONS=1
export AGENT_AUTO_REVIEW_STRATEGY=closure_first
export AGENT_SKIP_REVIEW=false
```

**Lever 2 — park-don't-guess instruction (addressed to the aif agent, verbatim):**

> **aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity — the 4 design questions in `.claude/orchestrator-prompts/aif-init-passport-gen/kickoff.md` (delivery channel · run-timing · deterministic-vs-LLM split · review gate), two defensible implementations, or any undecided design choice that changes behaviour — **do NOT pick** — park it as a question (set the task to `manualReviewRequired` / `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence Y») and **stop that task.** Proceed only on the unambiguous, deterministic work: the prior-art consult (DeepWiki + WebSearch ≥3 phrasings, own-stack-first CC `/init` + `tool-bootstrapping`) and recording verdicts in `docs/meta-factory/prior-art-evaluations.md`. Guessing a design fork to "keep moving" is the exact failure this contract exists to prevent.

**Lever 3 — resolve loop reviews aif's DECISIONS, not only open questions:** the operator Approves / Requests-changes each completed task; "questions" = open forks + aif's autonomous decisions awaiting verification.

**Egress gate (mandatory after `status=done` / `status=verified`):** aif does NOT push or open PRs by design.

```bash
npx tsx packages/runtime-bridge/src/cli/harvest.ts <taskId> --base staging
```

Anti-pattern `#autonomous-done-no-harvest`: skipping leaves the work permanently in the container.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for aif-init-passport-gen: T2, T11, T12, T13, T15, T16.**

- **T2** — designing the generator ≠ running it. The design session decides; acceptance for the eventual generator requires placeholder-free output on real fixtures. Do not declare the feature done at the design stage.
- **T11 / T12** — repo-aware doc generation is an active area; do the WebSearch + DeepWiki sweep at proposal time, not from training-data memory. ≥3 phrasings each.
- **T13 / T16** — if ADOPTing CC `/init` or the `tool-bootstrapping` skill, write «Upstream problem class: X. Our problem class: Y. Match? evidence: …» explicitly. "generate CLAUDE.md" vs "generate AIF passport" may not transfer; do not assume the name carries the capability.
- **T15** — self-application: the generator (once designed) should be able to produce *this* repo's own passport, or the design must explain why N/A.

**Domain-specific traps (aif-init-passport-gen-specific, NOT in canonical catalogue):**

- **T-Passport-A** — the AI is tempted to hardcode the timeliner reference stack (Hono / Drizzle / Expo / Vitest) into the generator so it "works on the demo" but fails on any other consumer. Counter: detection MUST derive from arbitrary repo signals; the ≥2-differently-shaped-fixtures acceptance test (Hono+Drizzle monorepo AND Next.js+Prisma flat repo) is the falsifier and must be designed in, not bolted on.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation. T-numbers are enumerated and a domain-specific trap is added per `ai-laziness-traps.md §3`.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation, `packages/core/principles/12-ai-laziness-traps.test.ts`). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

T15 also applies to the deliverable: the passport generator's own self-application is a design acceptance dimension (can it generate *this* repo's passport?).

---

## §7 Phase -1 reviewer dispatch (before the implementation stage)

Before dispatching the implementation I-phase (Stage 2), invoke:

```text
Agent: Phase -1 cold-review for aif-init-passport-gen design deliverable
Role: reviewer
Skill: requesting-code-review
```

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y). Does NOT pick strategy. See `reviewer-discipline.md §2`. The reviewer checks that the design genuinely resolves the 4 questions and that the deterministic-detection contract + ≥2-fixture acceptance test are specified before implementation begins.

---

## §8 Stop conditions

- Any `gh` command returns error and a stage gate cannot be verified → halt, escalate.
- Stage 1→2 gate command returns empty AND the design PR was expected merged → halt, report; do NOT dispatch implementation.
- `superpowers:brainstorming` surfaces a genuine strategy fork → surface to maintainer via `AskUserQuestion`; do NOT pick.
- Prior-art consult surfaces a production-grade analog that subsumes the feature → surface as ADOPT/ADAPT verdict before any BUILD; do NOT build past it.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write generator implementation code in the design session (§4 step 4 / T5).
- Do NOT re-do #547 Point 2 (auto-wire R2 — already shipped via #553).
- Do NOT pull in #551 upgrade-path (separate `consumer-upgrade-path` kickoff).
- Do NOT add any paid-LLM-in-CI path — generation is operator-session only (`no-paid-llm-in-ci.md`).
- Do NOT finalize project strategy — surface genuine forks to maintainer.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per no-paid-llm-in-ci.md + BFR-default).

---

## §10 See also

- `.claude/orchestrator-prompts/aif-init-passport-gen/kickoff.md` — substantive umbrella brief.
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/skills/pipeline/SKILL.md` — generating skill.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/build-first-reuse-default.md §3` — prior-art consult mechanism (own-stack-first).
- `.claude/rules/dual-implementation-discipline.md §3` — delivery-channel triage (consumer-facing ⇒ portable markdown).
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
