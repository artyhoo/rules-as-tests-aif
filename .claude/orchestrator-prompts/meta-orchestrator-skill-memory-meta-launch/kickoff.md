# KICKOFF — meta-orchestrator-skill-memory (meta-launch)

> **Type:** I-phase build (generated by `/meta-orchestrator meta-orchestrator-skill-memory` on 2026-05-25 at `0aa250e`).
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** Cache file template + SKILL.md §1/§10 `!shell` injection + helper writer + wave-plan Track-P row (ADAPT Cline Memory Bank, per-skill committed-or-gitignored memory cache). 4 atomic deliverables, single Mode A inline session.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. [`docs/meta-factory/wave-sequencing-plan.md §0`](../../../docs/meta-factory/wave-sequencing-plan.md) — plan-currency snapshot.
3. Umbrella kickoff at [`.claude/orchestrator-prompts/meta-orchestrator-skill-memory/kickoff.md`](../meta-orchestrator-skill-memory/kickoff.md) — **load-bearing SSOT** for scope; this meta-launch wrapper only adds dispatch mandates.

---

## §1 Inputs

- **Plan-currency check:** **DRIFT** (5 items + 12 UNTRACKED merged PRs) as of 2026-05-25 `0aa250e`. Mechanical state preferred over §0; `wave-sequencing-plan.md` Track P row stale (claims L3-L5 I-phase 🔲 but #222/#223/#225 merged 2026-05-25; memory `project_planner_completeness_done` confirms DONE).
- **Umbrella kickoff:** `.claude/orchestrator-prompts/meta-orchestrator-skill-memory/kickoff.md` (302 LOC; §-1 RE-VERIFY block mandatory before §1 execution per the umbrella's own discipline).
- **Stale items (informational, NOT blocking this umbrella):**
  - Track P row L3-L5 status (PRs #222/#223/#225 say DONE; row says 🔲).
  - `defer-reflex-detection` Stage 1 R-phase #229 merged but no §0 trackrow.
  - `meta-orchestrator-plan-memory` umbrella entries #227/#230 absent from §0.
  - mutation-discipline Stage 1 audit #219 merged; §0 «Mutation discipline umbrella PLANNED» (from memory) not updated.
  - 12+ UNTRACKED merged PRs (#218/#220/#224/#226 most recent and warrant trackrow).

> Reconciliation of wave-sequencing-plan §0 is **out of scope for this umbrella** — surface to maintainer as separate Track-0 action (per [CLAUDE.md «PR strategy»](../../../CLAUDE.md) drive-by-scope discipline). The session executing this umbrella does NOT edit §0 except for the single Track-P row append per umbrella §1.4.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A (cache file template) | I-phase build | Mode A | No | 1 | sequential within session | S (~5k) |
| B (SKILL.md §1+§10 edits, single SSOT) | I-phase build | Mode A | No | 1 | sequential within session | M (~8k) |
| C (helper script + paired-negative test) | I-phase build | Mode A | No | 1 | sequential within session | M (~15-25k) |
| D (wave-plan Track-P row) | I-phase build | Mode A | No | 1 | sequential within session | S (~3k) |

**Mode decision rationale:**
- Single Mode A inline Opus session (NOT parallel sub-waves), per umbrella kickoff §2 verbatim: «Single session, sequential edits, NO Worker dispatch».
- SDD?=No across the board — 4 small file edits with file-locks (cache template + helper + SKILL.md single-SSOT + wave-plan row); SDD overhead > catch-rate value at this size per [§3 launch-table SDD threshold](../../../.claude/skills/meta-orchestrator/SKILL.md).
- All deliverables share Stage 1 — there is no Stage 2 in this umbrella.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → (none — single-stage umbrella)

There is no Stage 2. The umbrella ships as ONE PR per umbrella kickoff §3 AC item 6: «All edits land in ONE PR titled `feat(meta-orchestrator): skill memory — cache + injection + writer`». Completion is verified by:

```bash
gh pr list --search 'is:merged head:feat/meta-orchestrator-skill-memory base:staging' \
  --json number,title,mergedAt,headRefName --limit 5
```

Expected after Stage 1 completes: non-empty JSON with one PR matching the title pattern.

**No Phase -1 reviewer dispatch between stages** — single-stage umbrella. **However**, the umbrella kickoff's own §-1 RE-VERIFY mandate requires a Phase -1 cold-reviewer subagent BEFORE Stage 1 execution + AC item 9 mandates own cold-QA BEFORE `gh pr create`. Both are dispatched by the implementing session, NOT by this meta-launch wrapper.

---

## §4 Sub-wave dispatch instructions

**Stage 1 = single Mode A inline Opus session.** Maintainer pastes the dispatch 1-liner from §10 «Dispatch 1-liner» (added round-3 amendment — see §10 below) into a fresh CC tab. The session:

1. Reads `.claude/orchestrator-prompts/meta-orchestrator-skill-memory/kickoff.md` cold (§-1 RE-VERIFY).
2. Dispatches Phase -1 cold-reviewer subagent per umbrella §-1 step 3 → GO before any Edit/Write.
3. Sets up worktree per umbrella §2 (idempotent block — `ln -sfn`, see §4a below for the canonical reusable form):
   ```bash
   git worktree add ../rules-as-tests-aif-skill-memory staging
   cd ../rules-as-tests-aif-skill-memory
   git checkout -b feat/meta-orchestrator-skill-memory
   # then run the idempotent node_modules block from §4a
   ```
4. Executes deliverables A → B → C → D sequentially.
5. Runs own cold-QA per umbrella §3 AC item 9 BEFORE `gh pr create`.
6. PR body includes §1.7 forward+backward checks per §4b below.
7. After PR merged to staging — removes worktree per umbrella §2 cleanup block.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Workers below — self-evaluate)

Workers whose target file matches ANY of the following paths MUST include in the **PR body** (not just commit message):

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**This umbrella triggers the mandate** — touches `.claude/skills/meta-orchestrator/SKILL.md` (Deliverable B), single SSOT per [install.sh:233-235](../../../install.sh) (no `skills/meta-orchestrator/` mirror).

**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `no-paid-llm-in-ci.md` — helper is deterministic bash, zero API-billed calls. file:line evidence: `.claude/skills/meta-orchestrator/helpers/update-cache.sh:<line>`
- `dual-implementation-discipline.md §5` — `@dual-pair: meta-orchestrator-plan-cache` markers in helper + SKILL.md. file:line evidence: `.claude/skills/meta-orchestrator/SKILL.md:<line>`
- `build-first-reuse-default.md` — ADAPT Cline Memory Bank per umbrella §0 BFR verdict
- `doc-authority-hierarchy.md` — Class field on helper inline comment header

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the new feature's scope>

- `.claude/skills/meta-orchestrator/SKILL.md §1` — extended (not superseded); cache becomes 5th `!shell` block atop existing 4. file:line evidence: `.claude/skills/meta-orchestrator/SKILL.md:<line>`
- `.claude/skills/meta-orchestrator/SKILL.md §10` — extended (item 5 added); existing items 1-4 preserved verbatim. file:line evidence: `.claude/skills/meta-orchestrator/SKILL.md:<line>`
- `docs/meta-factory/wave-sequencing-plan.md` Track P row — appended (existing schema preserved)
- No existing artefact deleted or rewritten — additive change only
```

**Hard rules (each one has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2 (`##`).** The CI gate's awk regex matches `^### §1\.7 ...` — H2 silently fails. (6× recurrence as of 2026-05-25: #58, #69, #105, #111, #212, #215.)
2. **Word «applied» is required.** Headings without «applied» fail the regex.
3. **≥40 non-whitespace chars in EACH section body** (awk-counted, post-strip).
4. **≥1 file:line citation per section** matching regex `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. **No mechanical-maintenance escape hatch for THIS PR** — the umbrella adds new disciplines (cache spec, helper contract); §1.7 substance check applies.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2 (one per section minimum)
```

If any check fails → fix the body before pushing. CI catches the same failure but burns wall-clock + a maintainer's attention; pre-flight is free.

---

## §4a Worker worktree setup (FIRST step in any worker session — Gap-3 round-2 follow-up)

If the worker is dispatched into a `git worktree add`-created directory (default per umbrella §2 setup block), reconstitute the `node_modules` plumbing the orchestrator's primary workdir already has.

**Run BEFORE any `npm` / test / build command (idempotent — no-op in primary workdir):**

```bash
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY_WORKDIR=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY_WORKDIR/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
  echo "worktree setup OK — node_modules + packages/core/node_modules linked to primary"
fi
```

(Round-3 amendment: umbrella kickoff §2 now embeds this exact idempotent block; the duplication is intentional — `§4a` keeps the canonical reusable form available for ALL workers, not just this umbrella's. Falls through cleanly in primary workdir, no-op on re-invocation, no-op when umbrella §2 already executed it.)

---

## §5 AI-traps active (per [`ai-laziness-traps.md §3`](../../../.claude/rules/ai-laziness-traps.md))

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for meta-orchestrator-skill-memory** (mirrored from umbrella §5 verbatim — kickoff-author obligation per ai-laziness-traps.md §3):

- **T1** — sampling-shallow: when verifying §-1 claims, read ALL 4 cited files, not 1-2.
- **T3** — plausible-finding-without-verification: every claim must have file:line or command output backing. INCONCLUSIVE if unverifiable.
- **T11** — BUILD-without-prior-art-check: re-verify BFR survey is still current (3 phrasings DeepWiki + WebSearch on «cross-session memory cache markdown file»); ADOPT if a mature tool surfaced post-2026-05-25.
- **T13** — adopted-as-zero-work: ADAPT Cline Memory Bank means we own the adaptation; audit the 1-file-vs-6-file scope reduction explicitly.
- **T15** — self-application skipped: this umbrella's own discipline (no Worker dispatch for writes; mechanical state always wins; cache supplementary not load-bearing) MUST apply to its execution. Kickoff explicitly forbids Worker dispatch for this implementation.
- **T16** — pattern-matching-on-name: «memory cache» sounds like Cline Memory Bank but Cline's problem class = «cross-session project context» while ours = «cross-invocation continuity within /meta-orchestrator specifically». Verify match is real, not nominal.
- **T17** — destructive-delegation-without-preserving: if a prior `_plan-cache.md` exists with hand-edits, do NOT clobber. Helper's «if cache missing» branch is «create template» only; existing files require `--force` or `git mv` to `.broken.<timestamp>.md`.
- **T19** — handoff-without-own-cold-QA: before `gh pr create`, run an independent Opus subagent over the diff. CI ≠ design review.
- **T20** — inline-verdict-without-evidence: in PR description, any «this works because X» / «we chose ADAPT because Y» must be backed by a same-turn tool call. No vibes-only verdicts in PR body.

**Domain-specific traps (meta-orchestrator-skill-memory specific, NOT in canonical catalogue):**

- **T-mem-A** — **«cache-as-source-of-truth drift»**: temptation to read `_plan-cache.md` and TRUST what it says about merged PRs instead of re-running `gh pr list`. Cache is session-memo, NOT a state register. Every «PR #X merged» claim from cache must be re-verified via `gh pr list` (which `plan-currency-check.sh` already does — cache precedes but does NOT replace). Counter: SKILL.md §1 Step 2 item 6 (cache-reconciliation rule, added by Deliverable B).

- **T-mem-B** — **«cache writer becomes feature factory»**: once helper exists, temptation to add more sections («last 5 invocations», «kill-rate trend», «favorite umbrella»). Counter: umbrella §6 lists exactly the sections that exist; any new section requires a separate kickoff. No drive-by feature creep in this implementation session.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation (pattern-matching the prompt instead of reasoning against it). The umbrella's own §5 already enumerates; this meta-launch wrapper mirrors verbatim per kickoff-author obligation.

---

## §6 Recursive self-application

This kickoff was generated by `/meta-orchestrator` and must itself pass `principle 12` (AI-laziness-traps format validation, [`packages/core/principles/12-ai-laziness-traps.test.ts`](../../../packages/core/principles/12-ai-laziness-traps.test.ts)). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

**Recursive note:** the umbrella itself implements memory-for-the-meta-orchestrator. The meta-launch wrapper for it ALSO uses meta-orchestrator discipline. T15 self-application thus applies twice — both this meta-launch kickoff AND the umbrella's own kickoff implement the discipline they describe.

---

## §7 Phase -1 reviewer dispatch (between stages)

**Not applicable between Stage 1 and Stage 2** — single-stage umbrella.

**Applicable INSIDE Stage 1** per umbrella §-1 step 3 + AC item 9:

- **Pre-execution Phase -1** (umbrella §-1 step 3): cold reviewer over THIS kickoff + umbrella kickoff before any Edit/Write. GO/REVISE. Focus: T3, T11, T15, T20.
- **Pre-handoff Phase -1** (umbrella AC item 9): cold reviewer over the diff before `gh pr create`. GO before push.

Both dispatched by the implementing session via the Agent tool with role=reviewer + skill=requesting-code-review (SP SSOT, REFERENCE per [R-phase patch §3](../../../docs/meta-factory/research-patches/2026-05-23-meta-orchestrator-prior-art.md)).

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (Option A → consequence X / Option B → consequence Y). Does NOT pick strategy. Does NOT edit files. See [reviewer-discipline.md §2](../../../.claude/rules/reviewer-discipline.md).

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block whose CONTENT follows the orchestrator-checkpoint substance shape per [SKILL.md §10.3a](../../../.claude/skills/meta-orchestrator/SKILL.md) — names sub-waves / AC items / file:line / REPORT-traces, NOT per-turn personal-reasoning prose (that's the end-of-turn hook's job).

Umbrella kickoff §7 already specifies the final block content shape (verbatim). Mid-execution checkpoints follow the same discipline.

---

## §8 Stop conditions

- Any `gh` command returns error during the final PR-merge gate (§3) → halt, escalate.
- Phase -1 reviewer returns REVISE 3× → escalate to maintainer per umbrella §4.
- Phase -1 reviewer returns STOP / BLOCKER → halt, surface to maintainer.
- Cache format collision with another in-flight skill's cache → STOP, surface as DECISION-NEEDED.
- Helper test fails after 3 fix attempts → STOP, do NOT mark `.skip` (`#discipline-theatre`).
- Sub-deliverable spawns Worker subagent for write task → STOP — umbrella's own discipline prohibits this (T15 self-application).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; STOP.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this meta-launch wrapper. The umbrella kickoff at [`../meta-orchestrator-skill-memory/kickoff.md`](../meta-orchestrator-skill-memory/kickoff.md) is the implementation SSOT.
- Do NOT finalize project strategy.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per [no-paid-llm-in-ci.md](../../../.claude/rules/no-paid-llm-in-ci.md) + BFR-default).
- Do NOT reconcile `wave-sequencing-plan.md §0` drift beyond the single Track-P row append (Deliverable D). Other DRIFT items from §1 are separate Track-0 work.
- Do NOT cross-pollinate with `defer-reflex-detection` or `mutation-discipline` umbrellas — they are queued separately per §2 priority scoring.

---

## §10 Dispatch 1-liner (round-3 amendment)

Maintainer pastes this into a fresh CC tab (or current session if Mode A inline) to launch the implementing session for THIS umbrella:

```
/orchestrator meta-orchestrator-skill-memory — Mode A inline Opus, role=implementer, autonomous=<NO|YES>, base=staging, worktree=../rules-as-tests-aif-skill-memory. Read .claude/orchestrator-prompts/meta-orchestrator-skill-memory/kickoff.md cold first (§-1 RE-VERIFY mandatory); execute §1.1→§1.5 sequentially after §-1 cold-reviewer returns GO; cold-QA before gh pr create per AC item 9.
```

**Variants:**
- For just §-1 (verify-and-halt-before-edits): replace «role=implementer» with «role=implementer, scope=§-1-only».
- For autonomous run (maintainer not monitoring): set `autonomous=YES`.
- For maintainer-monitored (default for discipline-bearing PRs): set `autonomous=NO`.

**Stop conditions auto-apply** per §8 + umbrella §4 — these don't need to be repeated in the 1-liner.

---

## §11 See also

- [`.claude/orchestrator-prompts/meta-orchestrator-skill-memory/kickoff.md`](../meta-orchestrator-skill-memory/kickoff.md) — umbrella SSOT.
- [`docs/meta-factory/wave-sequencing-plan.md`](../../../docs/meta-factory/wave-sequencing-plan.md) — plan-currency source.
- [`.claude/skills/meta-orchestrator/SKILL.md`](../../../.claude/skills/meta-orchestrator/SKILL.md) — generating skill.
- [`.claude/rules/ai-laziness-traps.md §2`](../../../.claude/rules/ai-laziness-traps.md) — canonical trap catalogue.
- [`.claude/rules/parallel-subwave-isolation.md §1`](../../../.claude/rules/parallel-subwave-isolation.md) — worktree isolation.
- [`.claude/rules/reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md) — reviewer role boundaries.
- [`.claude/rules/dual-implementation-discipline.md §5`](../../../.claude/rules/dual-implementation-discipline.md) — `@dual-pair` markers.
- [`docs/meta-factory/research-patches/2026-05-23-meta-orchestrator-prior-art.md`](../../../docs/meta-factory/research-patches/2026-05-23-meta-orchestrator-prior-art.md) — meta-orchestrator R-phase verdict.
