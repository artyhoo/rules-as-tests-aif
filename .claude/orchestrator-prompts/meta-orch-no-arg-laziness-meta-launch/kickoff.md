# KICKOFF — meta-orch-no-arg-laziness (meta-launch)

> **Type:** R-phase only (generated by `/meta-orchestrator meta-orch-no-arg-laziness` on 2026-05-27).
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** one research-patch at `docs/meta-factory/research-patches/2026-MM-DD-meta-orch-no-arg-laziness.md` with verdict (F.1 / F.2 / F.3 / F.4 / F.5 / DEFER / NEW-OPTION-F.6) covering Bugs #2/#3/#4 — three classifier-incompatible inline `!shell` surfaces in SKILL.md.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Single R-phase sub-wave. Read in this order:

1. This meta-kickoff (binding scope + dispatch instructions).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.
3. Umbrella kickoff at `.claude/orchestrator-prompts/meta-orch-no-arg-laziness/kickoff.md` — the binding spec (§0 problem statement, §1 six required searches, §2 candidate matrix F.1–F.5, §3 out of scope, §4 output spec, §5 AI-traps, §6 verdict criteria).

---

## §1 Inputs

- **Plan-currency check:** «План актуален с одним POTENTIAL_DUPE — false-positive vs PR #260 (kickoff line 5 explicitly names #260 as prerequisite, not duplicate; line 13: #260 had to land first as iteration substrate)» (as of 2026-05-27 a468e1e).
- **Umbrella kickoff:** [`.claude/orchestrator-prompts/meta-orch-no-arg-laziness/kickoff.md`](../meta-orch-no-arg-laziness/kickoff.md)
- **Stale items:** none for this umbrella — kickoff exists, all internal cross-refs resolve, prerequisite PR #260 already merged.
- **Fresh empirical reinforcement (this invocation):** invoking `/meta-orchestrator meta-orch-no-arg-laziness` (with argument) produced `umbrella=''` in §1 plan-currency-check injection, `(launch-table-generator: no umbrella …)` in §3, and `jq: invalid JSON text passed to --argjson` in §10 step 5b — live reproductions of Bugs #2/#3/#4 the R-phase targets. The umbrella is current and relevant.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume | ALIAS |
|---|---|---|---|---|---|---|---|
| (single) | R-phase | Mode A | No | 1 | — | M | RESEARCH |

Rationale:
- **R-phase, single → Mode A inline** per SKILL.md §5 Dispatch tree row 1.
- **SDD=No** — research-patch output is markdown, not buildable code; spec/code/quality reviewer split does not apply.
- **Volume=M** — kickoff is 162 LOC; research-patch §4 mandates §0–§6 with 6 searches (§1.1–§1.6) + 4 probes (§1.5a/b/c/d) with file:line evidence ≈ 400–700 LOC output.
- **Stage=1** only — no Stage 2 in this umbrella; verdict lands as one research-patch PR, then a separate I-phase umbrella is opened (NOT part of this R-phase scope per kickoff §3).

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 (this R-phase) — prerequisite check

Prerequisite already merged (verified at preflight):

```bash
gh pr list --search "is:merged head:fix/meta-orch-2.5-loop base:staging" \
  --json number,title,mergedAt,headRefName --limit 5
```

Expected: PR #260 «fix(meta-orchestrator): §2.5 Step 3 — extract Loop to classify-each-candidate.sh» (merged 2026-05-27T18:59:49Z). **PASS** at preflight.

### Stage 1 → Stage 2

**N/A — only Stage 1 in this umbrella.** R-phase output = one research-patch PR. The I-phase that lands the verdict is a separate umbrella (per kickoff §3 «NOT in this R-phase: any code change to SKILL.md, helpers, or principle tests»).

**Phase -1 cold-review is still MANDATORY** before the research-patch is considered done (see §7 below) — even though there's no Stage 2 to gate, the patch itself goes through Phase -1 review before maintainer merge.

---

## §4 Sub-wave dispatch instructions

**Single sub-wave (the R-phase itself), Mode A inline.**

Dispatch protocol — maintainer pastes this 1-liner into a fresh Opus session in the primary workdir (no worktree needed for R-phase; output is markdown, no `npm`/build commands):

```text
/orchestrator meta-orch-no-arg-laziness §1-§6 — Mode A, role=R-phase researcher, autonomous=yes, остальное в kickoff
```

Worker session reads:
1. `.claude/orchestrator-prompts/meta-orch-no-arg-laziness/kickoff.md` (binding spec — §0 problem, §1 six searches with §1.5 probes a/b/c/d, §2 candidate matrix, §4 output spec, §5 AI-traps T1/T3/T4/T7/T11/T12/T13/T15/T16/T19/T20 + T-N1/T-N2/T-N3, §6 verdict criteria).
2. Produces `docs/meta-factory/research-patches/2026-MM-DD-meta-orch-no-arg-laziness.md` (date = invocation date) with §0–§6 per kickoff §4.
3. Self-runs cold-QA per T19 + opens PR base=`staging`.

**Worktree NOT required:** R-phase output is a single markdown file under `docs/meta-factory/research-patches/`; no parallel siblings; no shared-dir-parallel risk per [parallel-subwave-isolation.md §1](../../../.claude/rules/parallel-subwave-isolation.md). Primary workdir is fine.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered)

**Applies if** the research-patch PR's diff touches ANY of:

- `.claude/rules/**`
- `packages/core/principles/**`
- `docs/meta-factory/EXECUTION-PLAN.md`
- `docs/meta-factory/prior-art-evaluations.md`
- `CLAUDE.md`
- `packages/core/templates/**`
- `.claude/skills/**`
- `agents/**`

**Likely scope for this R-phase:** the PR is expected to touch only `docs/meta-factory/research-patches/2026-MM-DD-meta-orch-no-arg-laziness.md` (the new file) and possibly `docs/meta-factory/prior-art-evaluations.md` (if new SSOT entries surface in §1 searches — kickoff §4 «§3 SSOT entries to add»). **The latter triggers §1.7.**

If §1.7 fires, PR body must contain (H3 headings, word «applied», ≥40 non-whitespace chars per section, ≥1 file:line citation per section):

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `phase-research-coverage.md §1` — 6-item checklist run per kickoff §1; file:line evidence: `docs/meta-factory/research-patches/2026-MM-DD-meta-orch-no-arg-laziness.md:<line of each search section>`
- `build-first-reuse-default.md §1` — BFR posture (ADOPT / ADAPT / REFERENCE / BUILD / REJECT) declared per F.X candidate; file:line evidence: `docs/meta-factory/research-patches/2026-MM-DD-meta-orch-no-arg-laziness.md:<verdict section line>`
- `no-paid-llm-in-ci.md` — verdict's mechanism uses no API-billed call (helper bash + skill-internal substitution only)
- `dual-implementation-discipline.md` — if verdict introduces a new helper, declare `@dual-pair` or `@cc-only-rationale` per §3 categorisation

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing artefacts under the verdict's scope>

- `SKILL.md §3 launch-table-generator inline block` — current §3 `!shell` invocation pattern; how the verdict supersedes/extends; file:line evidence: `.claude/skills/meta-orchestrator/SKILL.md:<line>`
- `SKILL.md §10 step 5b delta-write block` — current jq inline pattern; verdict's effect; file:line evidence: `.claude/skills/meta-orchestrator/SKILL.md:<line>`
- `helpers/launch-table-generator.sh` + `helpers/update-delta.sh` — existing helper boundaries; how F.1/F.3 candidates extend or collapse them; file:line evidence: `.claude/skills/meta-orchestrator/helpers/<name>.sh:<line>`
- Prior-art-evaluations.md rows considered in §1: SSOT #9 / #68 / #77 (and any new rows surfaced)
```

**Pre-flight grep (Worker MUST run BEFORE `gh pr create` if §1.7 triggers):**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # ≥2
```

**If §1.7 does NOT fire** (PR only touches `docs/meta-factory/research-patches/` and no other triggered path): skip §4b. The path scope is what triggers the gate.

**Mechanical-maintenance escape hatch** (if Worker concludes the R-phase produced only the new research-patch file with no rule/SSOT/principle touch): `### §1.7 Skipped: <rationale ≥60 chars>` shortcuts the gate. Rationale must specify *why* this PR has no discipline surface.

---

## §4a Worker worktree setup

**Skip — not applicable.** This R-phase runs in the primary workdir (markdown-only output, no `npm`/test/build commands, no parallel siblings requiring `.git/index` isolation). The §4a worktree node_modules setup applies only to worktree dispatches.

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See [`.claude/rules/ai-laziness-traps.md §2`](../../../.claude/rules/ai-laziness-traps.md) for full catalogue.

**Active canonical traps for meta-orch-no-arg-laziness** (sourced from kickoff §5):

- **T1** — sampling floor=5 for upstream candidates per problem-class (don't stop at SSOT #77 alone)
- **T3** — file:line evidence for every claim, especially §1.5 probes; no prose-only findings
- **T4** — don't close before all 6 searches; the §1 list is the floor, not the ceiling
- **T7** — run adversarial counter-prompts at category level («what candidate solution did I miss?»), NOT pattern-match the F.1–F.5 list
- **T11** — prior-art check BEFORE proposing any novel mechanism (F.4/F.5 are «novel-shape» candidates needing especially careful prior-art sweep)
- **T12** — WebSearch at proposing-moment, not from training data; CC harness behavior may have changed post-cutoff
- **T13** — audit ADOPTED-MECHANISM items (#77 Cline Memory Bank) for upstream evidence depth, don't trust SSOT entry as terminal
- **T15** — self-application: does THIS research-patch apply its own framing? (does its own design choice for winner-passing pass §1.5b classifier check?)
- **T16** — problem-class match on each upstream candidate verbatim («upstream X = problem class Y; ours = Z; match?»); SSOT #77 = cross-SESSION committed-markdown; ours = within-INVOCATION state — partial mismatch
- **T19** — own cold-QA pre-handoff: re-review the patch as an independent reviewer before declaring done; verify §6 DECISION-NEEDED items are real decisions, not punted judgment
- **T20** — no inline verdicts without an evidence-bearing tool call same turn; every «F.X = ADOPT/BUILD/REJECT» must be preceded by Bash/Read/Grep/WebFetch/WebSearch in the same turn

**Domain-specific traps (this R-phase, NOT in canonical catalogue — sourced from kickoff §5):**

- **T-N1 — assuming `!shell` block timing without verifying CC docs.** AI tempted to claim «blocks execute lazily» or «eagerly» without WebFetch of `code.claude.com/docs/en/hooks.md` or experimental probe. **Counter:** §1.5a probe is MANDATORY — synthetic two-block skill writing+reading `/tmp/probe-N.txt`. If §1.5a can't run experimentally, mark verdict PROVISIONAL with that caveat.
- **T-N2 — assuming classifier behavior on `${var}` and placeholders without verification.** §0 evidence verifies: `${umbrella}` BLOCKED, `${umbrella:-no-arg}` BLOCKED (`:-` default does NOT save), `<placeholder>` BLOCKED, `${CLAUDE_SKILL_DIR}` OK. Still UNKNOWN: arbitrary undefined `${X}`, `$(command-substitution)`, seam-prefix patterns. **Counter:** §1.5b MUST include synthetic probes for each variable shape used in candidate solutions F.1–F.5; verdicts depending on classifier behavior cite probe output verbatim.
- **T-N3 — assuming Bug #2/#3/#4 are one problem-class without §1.5d evidence.** Scope-merge in §0.5 is provisional pending §1.5d probe (does `<current_ids_json_array>` reconstruct deterministically, or require post-routing-tree judgment?). **Counter:** §1.5d MUST be among first probes; if judgment-dependency surfaces, Bug #4 needs F.1+ extension (persist routing-tree output to state file in §2.5 itself).
- **T-N4 (this meta-launch, fresh) — assuming the current invocation's NULL-arg behaviour is an isolated case.** This very `/meta-orchestrator meta-orch-no-arg-laziness` call (with argument) produced `umbrella=''` in §1 injection. AI tempted to dismiss as «just this one invocation» or «classifier weirdness». **Counter:** capture verbatim transcript snippets from this invocation in the research-patch §0 «New evidence» as a third incident (Incident C); confirms Bug #3 reproduces on argv path too, not only on §3-after-§2 in no-arg mode.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation (pattern-matching the prompt instead of reasoning against it). Principle 12 test validates §5 contents mechanically.

---

## §6 Recursive self-application

This meta-kickoff was generated by `/meta-orchestrator` and must pass `principle 12` (AI-laziness-traps format validation). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

Additionally — T15 in the dispatched R-phase: the Worker's research-patch verdict (whichever F.X wins) MUST itself comply with its own design — e.g., if F.1 wins, the patch's own inline blocks (if any) must use only `${CLAUDE_SKILL_DIR}` per the verdict's own constraint. Catching this is a §1.5 mandate (probe vs propose).

---

## §7 Phase -1 reviewer dispatch

After the Worker session ships the research-patch PR (before maintainer merge), invoke:

```text
Agent: Phase -1 cold-review for meta-orch-no-arg-laziness research-patch
Role: reviewer
Skill: requesting-code-review
```

Reviewer reads:
1. The research-patch (whole file).
2. The umbrella kickoff §1 six-search mandate + §1.5 probe mandate + §6 verdict criteria.
3. Verifies: every claim has file:line evidence; every §1.5 probe has verbatim output; the F.X verdict is supported by the cited evidence; §1.7 forward/backward checks substantively name existing artefacts.

Reviewer emits GO / REVISE / STOP. For any strategy fork («F.1 vs F.3 — both viable», «scope-merge wrong if §1.5d ambiguous»): emit «DECISION-NEEDED: <one-line>. Option A → consequence X. Option B → consequence Y. Maintainer decides» — does NOT pick. Per [`reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md).

**CI ≠ design review** (T19): even if CI is green on the new patch file, the reviewer's substance check is mandatory.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block whose CONTENT follows the orchestrator-checkpoint substance shape per SKILL.md §10.3a — names sub-waves / AC items / file:line / REPORT-traces / dispatch-state. Does NOT restate per-turn personal-reasoning prose (that is the end-of-turn-reminder hook's job).

Skip allowed only for: one-line tool acknowledgements, pure gh/git outputs handed back without orchestrator commentary, reviewer-side blocked-by-hook short responses.

---

## §8 Stop conditions

- Any `gh` command returns error and the R-phase needs PR state → halt, escalate.
- Any §1.5 probe (a/b/c/d) returns INCONCLUSIVE and the candidate matrix F.1/F.3 depends on it → mark verdict PROVISIONAL or DEFER per §6 criteria; do NOT fabricate a verdict.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- BLOCKER finding from cold-QA → halt; surface before declaring R-phase done.
- Any temptation to edit `~/.claude/skills/orchestrator/` (global skill, agent-uncommittable) → T-MOB-C violation; stop.
- Any temptation to BEGIN implementing the verdict in this same PR → §9 anti-scope violation; the I-phase is a SEPARATE umbrella.

---

## §9 Anti-scope

- Do NOT write SKILL.md / helper code changes in this R-phase. The deliverable is a research-patch (markdown). Per umbrella kickoff §3.
- Do NOT pick between F.1/F.2/F.3/F.4/F.5 silently when evidence is ambiguous — surface as «DECISION-NEEDED» per [`reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per [`no-paid-llm-in-ci.md`](../../../.claude/rules/no-paid-llm-in-ci.md) + BFR-default).
- Do NOT skip §1.5d probe and assume scope-merge — the kickoff §0.5 falsifier is explicit; T-N3 names this trap.
- Do NOT extrapolate classifier behaviour to undocumented `${var}` shapes — every shape in F.X candidates needs its own §1.5b probe per T-N2.

---

## §10 See also

- [`.claude/orchestrator-prompts/meta-orch-no-arg-laziness/kickoff.md`](../meta-orch-no-arg-laziness/kickoff.md) — binding umbrella spec.
- [`docs/meta-factory/wave-sequencing-plan.md`](../../../docs/meta-factory/wave-sequencing-plan.md) — plan-currency source.
- [`.claude/skills/meta-orchestrator/SKILL.md`](../../../.claude/skills/meta-orchestrator/SKILL.md) — generating skill.
- [`.claude/rules/ai-laziness-traps.md §2`](../../../.claude/rules/ai-laziness-traps.md) — canonical trap catalogue.
- [`.claude/rules/parallel-subwave-isolation.md §1`](../../../.claude/rules/parallel-subwave-isolation.md) — worktree isolation reference (N/A here, but linked for completeness).
- [`.claude/rules/reviewer-discipline.md §2`](../../../.claude/rules/reviewer-discipline.md) — reviewer role boundaries.
- [`.claude/rules/phase-research-coverage.md §1`](../../../.claude/rules/phase-research-coverage.md) — 6-item search-coverage checklist.
- [`.claude/rules/build-first-reuse-default.md §1`](../../../.claude/rules/build-first-reuse-default.md) — BFR posture vocabulary used in §6 verdict.
- PR #260 — `fix(meta-orchestrator): §2.5 Step 3 — extract Loop to classify-each-candidate.sh` — prerequisite (merged 2026-05-27).
