# KICKOFF — bg-helper-completion-barrier (meta-launch)

> **Type:** I-phase (build + wiring), 2 sequential stages (generated by `/meta-orchestrator bg-helper-completion-barrier` on 2026-06-01).
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill. Binding scope = `.claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md`.
> **Deliverable:** make background-helper incompleteness/crash structurally detectable — `run-helper.sh` wrapper that always appends an `END rc=` trailer + a Layer-1 read-rule so a header-only/trailer-absent file reads as «still running», never «zero results».
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Read in this order:

1. This meta-kickoff (stage gates + dispatch).
2. **`.claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md`** — the BINDING umbrella kickoff (§1 Inputs, §2 Design, §3 sub-wave decomposition, §5 traps, §6 recursive self-application). This meta-kickoff does NOT restate its design — read the binding kickoff.
3. `.claude/skills/meta-orchestrator/SKILL.md §2 / §2.5 / §10` — the call-sites Stage 2 reroutes.
4. `.claude/skills/meta-orchestrator/helpers/classify-each-candidate.sh:51-52` — the `awk '… && !/^=== /'` filter that makes the trailer parse-safe.

---

## §1 Inputs

- **Plan-currency check:** План актуален for this umbrella — fresh incident-driven umbrella (created this session, not in `wave-sequencing-plan.md`; expected). No DRIFT against its own scope. (as of 2026-06-01 `d516ebe`)
- **Currency note (load-bearing for worktree base):** local HEAD `d516ebe` is **4 commits behind** `origin/staging` (`a294b1e`). Stage 1 worktree MUST base off `origin/staging` (refreshed), NOT local HEAD — use `bash scripts/create-worktree.sh <name>` (auto-refreshes origin/HEAD) or `claude -w <name>`.
- **Dup/in-flight:** CLEAR — `gh pr list` shows no open or merged PR on `feat/run-helper-wrapper` or `feat/wire-helper-barrier`; no dupe matching run-helper/completion-barrier. Genuinely new work.
- **Safety pre-verified this session (do not re-litigate):**
  - `classify-each-candidate.sh:52` = `awk 'NF>=2 && /kickoff=/ && !/^=== /'` — strips `=== ` lines. Trailer `=== <name>: END rc=… ===` matches existing divider shape at `priority-score.sh:83,207` → parse-safe.
  - `run-helper.sh` confirmed ABSENT → Stage 1 builds it (capability commit).
  - 3 consumed helpers exist: `priority-score.sh`, `dup-detect.sh`, `inflight-check.sh`.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md`
- **Stale items:** none.

---

## §2 Launch-table

Launch table — bg-helper-completion-barrier (as of `d516ebe`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A | I-phase-small build (TDD) — `run-helper.sh` + paired-negative test | Mode A (SOLO) | No | 1 | — | S |
| B | wiring — reroute call-sites + Layer-1 read-rule + regression-verify | Mode A (SOLO) | No | 2 | — | S |

**Routing rationale:** both sub-waves are single-task, sequential (Stage 1 → Stage 2 hard gate). No SDD (1 task each, not ≥3 independent tasks — review overhead ≈ catch-rate). No parallelism (B depends on A's merged wrapper). Mode A inline per binding kickoff §3. The mandatory Phase -1 cold-review is the **between-stage gate** (§7), not a per-sub-wave PAIR.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2

Stage 2 dispatch waits for Sub-wave A's PR merged to `staging`:

```bash
gh pr list --search "is:merged head:feat/run-helper-wrapper base:staging" \
  --json number,title,mergedAt,headRefName --limit 5
```

Expected: non-empty JSON. Empty → **HALT**, do not dispatch Stage 2.

### Stage 2 → done

```bash
gh pr list --search "is:merged head:feat/wire-helper-barrier base:staging" \
  --json number,title,mergedAt,headRefName --limit 5
```

**Phase -1 cold-review between Stage 1 and Stage 2 is MANDATORY** (see §7).

---

## §4 Sub-wave dispatch instructions

### Stage 1 — Sub-wave A (dispatch now)

Open a fresh session/worktree based on `origin/staging`:

```text
/orchestrator bg-helper-completion-barrier §3-Stage-1 — Mode A inline TDD; build helpers/run-helper.sh + paired-negative test; capability commit w/ Prior-art: trailer (§1.5). Binding scope + design in .claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md §1.5/§2/§3-Stage-1. Branch feat/run-helper-wrapper off origin/staging.
```

Worker MUST:
1. Write test FIRST (principle 02 paired-negative): positive (wrapper appends `END rc=0`, passes stdout verbatim, preserves exit 0) AND paired-negative (appends `END rc=<n>` + propagates non-zero exit when child crashes/`exit 1`; trailer present even when child dies mid-output).
2. Then implement `run-helper.sh` (~10-15 LOC: `"$@"; rc=$?; echo "=== <name>: END rc=$rc (lines=…) ==="; exit $rc`-shape). **Hard constraint:** capture `rc=$?` BEFORE echo, then `exit $rc` — must NOT alter child exit status (tests assert `r.status).toBe(0)`).
3. Carry a `Prior-art:` trailer (§1.5 — escape-hatch acceptable: «trivial bash exec-wrapper, no upstream analog worth a dep»). Still run the `phase-research-coverage §1` negative-existence check + `prior-art-evaluations.md` consult.
4. §4b PR-body mandate APPLIES (touches `helpers/`) — see §4b.

### Stage 2 — Sub-wave B (dispatch ONLY after Stage 1 gate green + Phase -1 GO)

```text
/orchestrator bg-helper-completion-barrier §3-Stage-2 — Mode A inline wiring; reroute SKILL.md §2/§2.5/§10 bg-helper call-sites through run-helper.sh + add Layer-1 read-rule prose; regression-verify 5 tests green (paste output). Binding scope in .claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md §2/§3-Stage-2. Branch feat/wire-helper-barrier off origin/staging.
```

Worker MUST:
1. Reroute `SKILL.md §2 Step 1` (`priority-score.sh`), `§2.5 Step 2` (`dup-detect.sh` + `inflight-check.sh`), and any §10 background helper through `run-helper.sh`.
2. Add Layer-1 read-rule prose: *background-helper output is parsed ONLY after that helper's own `task-notification` (matched by task-id) OR the presence of its `END rc=` trailer; a header-only / trailer-absent file = «still running», NOT «zero results»; never conflate one task's completion notification with another's.*
3. **Regression-verify (mandatory, paste actual output):**
   ```bash
   npm --prefix packages/core test -- priority-score-branch-matcher done-md-completion-filter classify-each-candidate planner-discovery delta-diff 2>&1 | tail -15
   ```
   All 5 must pass. Confirm exit-code preserved + `!/^=== /` filter still strips the new trailer.
4. §4b PR-body mandate APPLIES (touches `.claude/skills/**`).

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to BOTH stages — self-evaluate)

Both stages touch `.claude/skills/**` (and Stage 1 touches `helpers/`). Both PRs MUST carry in the **PR body** (not just commit message):

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars: which existing disciplines were checked>

- `build-first-reuse-default.md` — BUILD verdict pre-reasoned (§1.5); trivial bash exec-wrapper, no upstream dep. file:line evidence: `.claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md:32`
- `no-paid-llm-in-ci.md` — pure bash, no API call. Verdict: compliant.

### §1.7 Backward-check applied

<≥40 non-whitespace chars: sweep of existing artefacts under scope>

- `classify-each-candidate.sh` — trailer is `=== `-prefixed, already stripped by `:52` filter; orthogonal. file:line evidence: `.claude/skills/meta-orchestrator/helpers/classify-each-candidate.sh:52`
```

**Hard rules (each historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2.** CI awk regex matches `^### §1\.7 ...`.
2. **Word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 file:line citation per section** matching `[^[:space:]]+\.[a-z]+:[0-9]+`.
5. Escape hatch: `### §1.7 Skipped: <rationale ≥60 chars>` — but these PRs have a real discipline surface (capability commit + skill edit), so do NOT skip.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'                  # must be ≥2
```

---

## §5 AI-traps active (per `ai-laziness-traps.md §2`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue. **Active canonical traps for this umbrella:**

- **T3** — plausible finding without verification: every «test passes» / «parse-safe» claim needs command output, not prose. (This umbrella exists *because* of an unverified «zero candidates» claim.)
- **T11** — designing custom solution without prior-art check: the §1.5 BFR consult is mandatory before the wrapper lands, even though BUILD is pre-reasoned.
- **T15** — self-application: verify the fix on the orchestrator's **real** `priority-score.sh` call-path (the one that failed this session), not a toy helper.
- **T19** — CI ≠ design review: green regression tests do not substitute for the Phase -1 cold-review between stages.

**Domain-specific trap (NOT in canonical catalogue):**

- **T-BGB-A — «trailer present ⇒ output complete» misread.** The `END rc=0` trailer proves the *child finished*, not that *its work was correct* (e.g. `gh` rate-limited mid-run, exited 0, emitted partial set). Counter: the trailer guarantees *completion + exit-status*, not *semantic completeness*; candidate-count sanity / `gh` error detection stay separate.

> Blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation.

---

## §6 Recursive self-application

This umbrella fixes the meta-orchestrator's own read-discipline. Verify end-to-end on the **real** `/meta-orchestrator` no-arg path: after wiring, a no-arg invocation must wait for the `priority-score.sh` notification/trailer before scoring, or visibly emit «helper still running — re-reading» — never «zero candidates» off a header-only file. This meta-kickoff must itself pass `principle 12` (AI-laziness-traps format — §5 above has explicit T-enumeration + 1 domain trap).

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between Stage 1 and Stage 2 — MANDATORY)

After Stage 1 PR merges to staging, before Stage 2 dispatch:

```text
Agent: Phase -1 cold-review for bg-helper-completion-barrier Stage 1
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer reads `git diff staging...feat/run-helper-wrapper` + binding kickoff §2/§3-Stage-1 AC. Emits GO / REVISE / STOP with BLOCKER/MAJOR/MINOR. Surfaces strategy forks as «DECISION-NEEDED: … Option A → X / Option B → Y» — does NOT pick. Does NOT skip surface review because «CI green» (T19).

- **GO** → dispatch Stage 2.
- **REVISE** → Worker fixes; repeat Phase -1.
- **STOP** → escalate to maintainer; halt.

---

## §7a Reporting format (plain-language tail)

Any session message produced FROM this kickoff (Worker REPORT, Reviewer verdict, status, handoff) ends with a `## 🟢 Простыми словами` block naming sub-waves / AC items / file:line / REPORT-traces — not per-turn personal reasoning.

---

## §8 Stop conditions

- `run-helper.sh` changes the child's exit code (breaks `toBe(0)` tests) → STOP, fix exit propagation.
- Any of the 5 regression tests fail after wiring → STOP, do not merge Stage 2.
- Phase -1 reviewer returns STOP → escalate to maintainer.
- Temptation to foreground the helpers «to make it simpler» → STOP (out of scope; background is correct).
- Stage gate `gh pr list` empty AND Stage N expected merged → halt, report.

---

## §9 Anti-scope

- Do NOT foreground the slow helpers (background is correct — the fix is the read, not the launch).
- Do NOT rewrite `priority-score.sh` / `dup-detect.sh` internals beyond routing through the wrapper.
- Do NOT keep a per-script `echo END` (Layer 2) alongside the wrapper — dropped-as-subsumed (binding §2).
- Do NOT add an npm dep (trivial bash; BFR BUILD).
- Do NOT implement sub-wave code in this kickoff — meta-orchestrator dispatches, never implements.
- Do NOT finalize project strategy — surface genuine forks to maintainer.

---

## §10 See also

- `.claude/orchestrator-prompts/bg-helper-completion-barrier/kickoff.md` — BINDING umbrella kickoff.
- `.claude/skills/meta-orchestrator/SKILL.md §2 / §2.5 / §10` — call-sites.
- `.claude/skills/meta-orchestrator/helpers/classify-each-candidate.sh:51-52` — parse-safe filter.
- `.claude/rules/ai-laziness-traps.md §2` (T3/T10/T15) — T10 «report completeness based on what you LOOKED at, not what EXISTS» is the canonical form of this incident.
- `CLAUDE.md` «What is a capability commit?» — Stage 1 Prior-art trailer requirement.
