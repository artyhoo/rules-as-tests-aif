# KICKOFF — recommendation-laziness-discipline-i-phase (meta-launch)

> **Type:** I-phase build (generated by `/meta-orchestrator recommendation-laziness-discipline-i-phase` on 2026-05-25).
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** ship mechanism layer for parent rule `phase-research-coverage.md §1.12` — Option D = A+C only per benchmark verdict (B explicitly dropped, `narrow-b-benchmark.md §1.5` FP=84.2%). Three file-disjoint sub-waves: A (H1 wording append), C (new T-trap), D (new Class C rule file).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + dispatch channel).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot (no DRIFT relative to this umbrella).
3. Per-sub-wave kickoff at `.claude/orchestrator-prompts/recommendation-laziness-discipline-i-phase/kickoff.md`.
4. R-phase SSOT: `docs/meta-factory/research-patches/2026-05-24-recommendation-laziness-discipline.md` (+ PR #207 amend).
5. Benchmark SSOT: `docs/meta-factory/research-patches/2026-05-25-narrow-b-benchmark.md` (Option D = A+C verdict).

---

## §1 Inputs

- **Plan-currency check:** «План актуален» (as of 2026-05-25 `3b95f08`). One observation: local branch is `fix/meta-orchestrator-item10-b5` which is the head of merged PR #209 — Workers MUST `git worktree add` from `origin/staging`, NOT from this stale branch. Wave-sequencing-plan.md does not track this umbrella as an explicit wave row (it is downstream of N4b H10); no row to amend, no DRIFT.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/recommendation-laziness-discipline-i-phase/kickoff.md` (383 lines, GO recorded in §-1 amendments log iter-2).
- **Stale items:** none in the plan. The dispatch environment has a stale checked-out branch (see above) — Workers correct this by branching from `origin/staging`.
- **Re-verified preconditions (this meta-orchestrator invocation):**
  - `HIGHEST_T=19` (matches kickoff draft-time; no T20 in file) → Sub-wave C falls into **DECISION-NEEDED row** of §1 Sub-wave C state table.
  - Memory `project_stryker_mutation_hardening_done.md` reserves T20 for Stryker «equivalence-claim-without-evidence» (queued, not shipped) → S2 stop condition triggers for Sub-wave C unless maintainer pre-resolves.
  - R-phase §1.4 pre-resolved to **(b) = T21** (reserve T20 for Stryker; ship this trap as T21). Maintainer may override.
  - `inject-session-bootstrap.sh:11` H1 line text verified at this run (matches kickoff §1 Sub-wave A target).

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A | I-phase build (1-line hook edit) | Mode A inline + `isolation: "worktree"` | No | 1 | C, D | S (~1 line) |
| C | I-phase build (rule §2+§3+§5 edits) | Mode A inline + `isolation: "worktree"` | No | 1 | A, D | S–M (~40 LOC) |
| D | I-phase build (new Class C rule file) | Mode A inline + `isolation: "worktree"` | No | 1 | A, C | M (~120 LOC) |

**No Stage 2** — umbrella closes after Stage 1 (3 parallel PRs merged + Phase -1 cold-review on the merged set). Single-stage umbrella; the AC list at §3 is umbrella-level only.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 (Sub-waves A, C, D) — no upstream dependency

Stage 1 sub-waves dispatch immediately (in parallel). No upstream wait — all branches fork from `origin/staging`.

### Stage 1 → umbrella close (no Stage 2)

Before declaring umbrella complete, run:

```bash
gh pr list \
  --search "is:merged base:staging head:fix/recommendation-laziness-iphase-a created:>=2026-05-25" \
  --json number,title,headRefName --jq '.[] | "  A: PR#\(.number) \(.title)"' && \
gh pr list \
  --search "is:merged base:staging head:fix/recommendation-laziness-iphase-c created:>=2026-05-25" \
  --json number,title,headRefName --jq '.[] | "  C: PR#\(.number) \(.title)"' && \
gh pr list \
  --search "is:merged base:staging head:fix/recommendation-laziness-iphase-d created:>=2026-05-25" \
  --json number,title,headRefName --jq '.[] | "  D: PR#\(.number) \(.title)"'
```

Expected: 3 lines (one per sub-wave). If any empty → that sub-wave's PR is not yet merged; do NOT declare umbrella closed.

**Phase -1 cold-review on the merged set is MANDATORY** (see §7 below). CI green ≠ design review (T19).

---

## §4 Sub-wave dispatch instructions

**Dispatch channel:** per `meta-orchestrator/SKILL.md §5` anti-pattern `#worker-dispatch-via-subagent`, write-task Workers belong in **fresh CC sessions opened by maintainer pasting a §10 1-liner block** — NOT dispatched via the Agent tool from this meta-orchestrator session. <!-- channel-discipline: allow teaches the anti-pattern (NOT-this-channel guidance) -->

> **Discrepancy with umbrella kickoff §2:** the umbrella kickoff at `.claude/orchestrator-prompts/recommendation-laziness-discipline-i-phase/kickoff.md §2` says «Dispatch as 3 concurrent `Agent` tool calls in one message». That instruction predates the `#worker-dispatch-via-subagent` anti-pattern codification (PR #186 meta-orchestrator skill, 2026-05-23). The skill's anti-pattern is binding here. If the maintainer wants the older Agent-tool channel anyway, surface this explicitly — but the default is paste-blocks.

**Recommended dispatch order:** all three Sub-waves Stage 1, fully parallel. Maintainer opens 3 fresh CC tabs and pastes one 1-liner block per tab (see §1-liner blocks at the inline session report). Workers self-verify the §-1 preconditions of the umbrella kickoff before editing.

---

## §4a Worker worktree setup (FIRST step in any worker session)

Each Worker MUST run this BEFORE any `npm` / test / build command (idempotent — no-op in primary workdir):

```bash
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY_WORKDIR=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY_WORKDIR/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
  echo "worktree setup OK — node_modules + packages/core/node_modules linked to primary"
fi
```

`packages/core` consumes hoisted modules via a workspaces symlink that `git worktree add` does NOT copy — without this step `npm test` fails.

---

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §2`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for recommendation-laziness-discipline-i-phase:**

- **T3** — every finding requires file:line + actual content, NOT prose-only. Applies to all 3 sub-waves. Worker REPORT must quote command output for every claim («line 11 contains X», «T19 is highest», «benchmark FP_rate = 84.2%»).
- **T7** — pattern-matching the prompt instead of reasoning adversarially. Applies to Sub-wave C: Worker may tick the §1 Sub-wave C bash determination block as «done» without actually re-running it at execution time (or run it and ignore DECISION-NEEDED to pick a T-number autonomously). Counter: T-RLD-IPhase-A below mandates explicit re-run + halt-on-DECISION-NEEDED.
- **T12** — skipping the literature/source check because «I already know». Applies to Sub-wave A: do NOT rely on the H1 wording quoted in this kickoff or in the umbrella kickoff — Read `.claude/hooks/inject-session-bootstrap.sh:11` directly at edit-time (drift possible between kickoff draft and execution).
- **T15** — recursive self-application gate. Applies to all 3 sub-waves: each Worker is itself a candidate violator of the new T-trap. Worker MUST run at least one verification command per recommendation in their REPORT (e.g., before claiming «Sub-wave A complete», run `grep` to verify the line changed as intended; quote the output).
- **T19** — own cold-QA pre-handoff. Applies to umbrella close: orchestrator runs own diff-review across all 3 sub-waves before opening / merging. CI ≠ design review.

**Domain-specific traps (recommendation-laziness-i-phase-specific, NOT in canonical catalogue):**

- **T-RLD-IPhase-A — «Sub-wave C T-number race»:** Worker assigned to Sub-wave C may, under time pressure, autonomously pick a T-number without re-running the §1 Sub-wave C bash block at execution time, OR may proceed past DECISION-NEEDED without halting. Counter: Worker prompt MUST include the bash determination block verbatim AND the «halt on DECISION-NEEDED» instruction; orchestrator verifies T-number choice in REPORT against expected state (HIGHEST_T at time of Worker execution, not kickoff-draft-time).
- **T-RLD-IPhase-B — «B-drop forgotten under time pressure»:** Worker assigned to Sub-wave D may, when authoring §4 Enforcement-channel section, omit the explicit «DROPPED per benchmark §1.5» rationale for layer (B), defaulting instead to A+C-only without explanation. Counter: Sub-wave D §4 acceptance criterion includes «B-drop rationale explicitly traceable to benchmark.md» (`grep -c 'narrow-b-benchmark.md' .claude/rules/recommendation-laziness-discipline.md ≥ 1`).

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation (pattern-matching the prompt instead of reasoning against it). The 5 canonical T-numbers + 2 domain-specific T-traps above are mandatory.

---

## §6 Recursive self-application

This kickoff was generated by `/meta-orchestrator` and must itself pass `principle 12` (AI-laziness-traps format validation, `packages/core/principles/12-ai-laziness-traps.test.ts`). Verify:

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between stages)

Single-stage umbrella → Phase -1 cold-review fires AFTER all 3 Sub-wave PRs merge to staging, BEFORE declaring umbrella closed (NOT between stages, because there is no Stage 2). Invoke:

```text
Agent: Phase -1 cold-review for recommendation-laziness-discipline-i-phase (post-merge set)
Role: reviewer (read-only, no edits)
Skill: requesting-code-review
Scope: git diff staging~3...staging on the merged Sub-wave A+C+D commits
```

Reviewer discipline: surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y). Does NOT pick strategy. See `.claude/rules/reviewer-discipline.md §2` for the pattern.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block whose CONTENT follows the orchestrator-checkpoint substance shape — distinct from the per-turn generic substance the end-of-turn hook already enforces. Substance spec: see SKILL.md §10.3a table (3 checkpoint moments with required content shape).

**Anti-duplication discipline:** the block content names sub-waves / AC items / file:line / REPORT-traces / dispatch-state. It does NOT restate per-turn personal-reasoning prose («чем я сейчас занят», «на той ли цели») — that is the hook's job (Branch A/C). If your block content is verbatim-copyable from `end-of-turn-reminder.sh` reminder text, you have drifted into `#two-prompts-drift` per `.claude/rules/dual-implementation-discipline.md §4`.

---

## §8 Stop conditions

- Any `gh` command returns error and umbrella has parallel sub-waves → halt, escalate.
- Stage gate command returns empty AND a sub-wave was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any sub-wave returns BLOCKER finding → halt that sub-wave; surface before proceeding.
- **S1 (umbrella kickoff §5):** Sub-wave A discovers post-edit that `.claude/settings.json` registration change is needed → deliver snippet in PR body, halt merge until maintainer applies.
- **S2 (umbrella kickoff §5) — TRIGGERS AT THIS RUN:** Sub-wave C `HIGHEST_T=19` AND Stryker T20 queued → DECISION-NEEDED unresolved. Maintainer must pick T20 vs T21 before Sub-wave C executes. Sub-waves A and D may proceed in parallel (they do not depend on the T-number choice). Sub-wave D's cross-reference to `T<N>` resolves only after Sub-wave C commits — Sub-wave D may stage with a placeholder and amend, OR also halt to avoid double-commit churn.
- **S3 (umbrella kickoff §5):** Sub-wave C or D Worker designs a post-hoc grep mechanism claiming a catch-rate without manual classification sample → drop the claim; reframe as «logging-only, manual classification required, NOT a gate». Per benchmark §1.3 + §T19 recall caveat (recall=1.0 on n=10 is speculative; true recall likely low).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this kickoff (this is the meta-kickoff; Worker sessions do the edits).
- Do NOT finalize project strategy — surface genuine forks to maintainer.
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per `no-paid-llm-in-ci.md` + BFR-default).
- Do NOT edit `.claude/settings.json` directly (agent-self-protected; snippet-for-maintainer only).
- Do NOT touch `.claude/skills/meta-orchestrator/**` (F.3 scope, parallel-safe boundary with this umbrella).
- Do NOT re-introduce Sub-wave B (Stop-hook scan) — explicitly dropped per `narrow-b-benchmark.md §1.5` (FP=84.2%).

---

## §10 See also

- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/skills/meta-orchestrator/SKILL.md` — generating skill.
- `.claude/orchestrator-prompts/recommendation-laziness-discipline-i-phase/kickoff.md` — umbrella kickoff (binding scope SSOT, 383 lines).
- `docs/meta-factory/research-patches/2026-05-24-recommendation-laziness-discipline.md` — R-phase design (+ PR #207 amend).
- `docs/meta-factory/research-patches/2026-05-25-narrow-b-benchmark.md` — benchmark patch (Option D verdict + recall caveat).
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue (Sub-wave C target).
- `.claude/rules/phase-research-coverage.md §1.12` — parent rule (prose discipline SSOT).
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation discipline.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
