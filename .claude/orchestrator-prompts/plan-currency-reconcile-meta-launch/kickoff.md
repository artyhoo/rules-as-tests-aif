# KICKOFF — plan-currency-reconcile (meta-launch)

> **Type:** meta-launch dispatch plan (generated by `/pipeline plan-currency-reconcile` on 2026-06-28).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill. Wave-0 umbrella **U1** of the `getff-to-prod` program.
> **Deliverable:** plan-docs (trackers, `ACTION-PLAN.md`, public README) stop lying about status — every "not done" already on `main` is flipped; license = FSL; "4 stacks" + principle-test count verified against `main` via `git`; remaining DONE-umbrellas skepticism-passed. **No product code (R/doc, no-build).**
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-wave mode decisions are in §2 launch-table. Read in this order:

1. **This kickoff** (launch-table + stage gates + per-stage dispatch). This file ADDS orchestration; it does NOT restate the umbrella scope.
2. **Substantive SSOT — the umbrella kickoff:** `.claude/orchestrator-prompts/plan-currency-reconcile/kickoff.md` (on staging, #785) — §1 goal, §2 fixed decisions, §3 stages, §4 S1 detail, §6 AI-traps, §7 stage gates, §9 carve-outs.
3. `.claude/orchestrator-prompts/getff-to-prod-meta-launch/kickoff.md §2` — program sequencing (U1 ∥ U2 in Wave 0; U1 does NOT depend on U2).
4. `docs/meta-factory/wave-sequencing-plan.md §0` — project plan snapshot. **NOTE:** drifted (last reconcile 2026-06-13, HEAD now #786). Orthogonal to this umbrella; not load-bearing here.

---

## §1 Inputs

- **Plan-currency check (skill §1):** umbrella kickoff CURRENT (#785, 2026-06-28); getff `§0` reconciled vs `origin/main` 2026-06-26; `wave-sequencing-plan.md §0` **STALE** (last reconcile 2026-06-13) — surfaced as a separate observation, **NOT a blocker** for this umbrella.
- **In-flight probe (CLAUDE.md pre-dispatch):** **CLEAR**. dup-detect `OK: plan-currency-reconcile` (no merged dupe); no open PR carrying the slug. The worktree `chore-plan-currency-reconcile-v2` (branch @ `89c129436`, **2026-05-25**) + remotes `chore/plan-currency-reconcile-2026-05-25` / `fix/plan-currency-check-tests` are STALE leftovers of the May `plan-currency-check` *tool* work — NOT an active U1 session. Stale-worktree cleanup = out-of-scope observation.
- **REUSE (umbrella §5):** `.claude/orchestrator-prompts/_plan-currency-raw.txt` regenerated fresh **2026-06-28 03:31** (~40 KB `gh` snapshot) — S1 consults it before manual `git`-checks.
- **Operator-notes reachability:** trackers + ПЛАН-ПОЛНЫЙ live at `/Users/art/Claude/Projects/rules-as-tests/` (`СЛЕДУЮЩИЕ-РАБОТЫ.md`, `ПЛАН-ПОЛНЫЙ-getff-2026-06-26.md`, `roadmap-state.md` confirmed present 2026-06-28). On THIS machine S1/S2 are dispatchable — NOT park. A filesystem-isolated worker (aif container) without that path MUST PARK E1/E2 per umbrella §9.
- **S3 ground-truth (pre-verified vs `origin/main`):** principle-test files = **28** (slots 01–26, 20/21 split); presets = **3** (`preset-next-15-canonical`, `preset-react-spa`, `preset-react-native`) + ts-server from shared `templates/` = "4 stacks"; staging README badge already = **FSL-1.1-ALv2** (the "Proprietary" residue is on the lagging `plug-packaging` public branch, NOT staging — S3 must work the actual public-bound branch).

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| S1 | R/enumerate (claim→git table) | Mode A | No | 1 | — | S |
| S2 | doc-apply E1+E2 (trackers + `ACTION-PLAN` Apache→FSL) | Mode A | No | 2 | S3, S4 | S |
| S3 | doc-apply+verify (README sync, 4-stacks, principle-count) | Mode A | No | 2 | S2, S4 | S |
| S4 | audit (skepticism-pass migration-ast / plugin / generator) | Mode A | No | 2 | S2, S3 | S |

Order: **S1 → (S2 ∥ S3 ∥ S4)**. All Mode A (Volume-S doc tasks; no execution-parallel-build → worktrees not mandatory). If S2/S3/S4 run as concurrent CC tabs, isolate S3+S4 (both touch the repo tree) via `bash scripts/create-worktree.sh <name>` per `parallel-subwave-isolation.md §1`; S2 edits operator-notes outside the repo (no repo contamination). Sequential is acceptable given Volume S.

---

## §3 Stage gates (real checks — no-build umbrella: deliverable-presence, not only merged-PR)

### Stage 1 → Stage 2

S2/S3/S4 dispatch waits for S1's discrepancy table to land:

```bash
# S1 deliverable on staging (research-patch form), OR S1 PR merged:
git ls-tree origin/staging -- docs/meta-factory/research-patches/2026-06-28-plan-currency-reconcile-s1.md 2>/dev/null \
  || gh pr list --search "is:merged head:chore/plan-currency-reconcile-s1 base:staging" --json number,mergedAt --limit 5
```

Empty AND no merged S1 PR → **HALT**, do NOT dispatch Stage 2. (Soft-gate alternative for this no-build umbrella: operator explicitly accepts the S1 report in-session.)

### Stage 2 → closure

```bash
gh pr list --search "is:merged base:staging" --json number,title,headRefName --limit 20 | grep -iE "plan-currency-reconcile-(s2|s3|s4)"
```

**Phase -1 cold-review is MANDATORY before closure:** S4 skepticism findings + S3 git-verifications reviewed; reviewer surfaces strategy forks as DECISION-NEEDED, does not pick (`reviewer-discipline.md §2`).

---

## §4 Sub-wave dispatch instructions

(per stage, 1-liner + scope pointer; substance in umbrella kickoff §4 / §S-sections)

- **S1** — enumerate every "not done / TODO / in-work" claim across the 3 trackers (`/Users/art/Claude/Projects/rules-as-tests/{СЛЕДУЮЩИЕ-РАБОТЫ,roadmap-state}.md` + `ПЛАН-ПОЛНЫЙ-getff-2026-06-26.md`) → for each, a concrete `git -C <repo> ls-tree origin/main -- <path>` / `git log origin/main` check (command + output) → output table `claim | tracker says | git says | action (flip/keep)`. Consult `_plan-currency-raw.txt` first (umbrella §5). Change nothing (T5). Gate §7-S1: zero prose-only rows.
- **S2** — apply E1 (flip stale "not done" checkmarks in trackers) + E2 (`ACTION-PLAN.md` Apache-2.0 → FSL) per the S1 table. Gate §7-S2: `grep -rnE "Apache-2.0 на месте|CLA под Apache" ACTION-PLAN.md` → 0. **Operator-notes writes** — if the session lacks `/Users/art/Claude/Projects/rules-as-tests/` access, PARK and surface (umbrella §9), do not skip silently.
- **S3** — README sync on the actual public-bound branch (the lagging `plug-packaging` still carries "Proprietary"): badge = FSL, `grep -c Proprietary` → 0, "4 stacks" = `ls -d packages/preset-*` + shared ts-server (3 presets + ts-server confirmed on main), principle-count = `ls packages/core/principles/*.test.ts | wc -l` (= 28 on main) matches text, mutation-overclaim closed. Precondition role for U16 (confirms react-spa/native e2e gaps real vs phantom).
- **S4** — skepticism-pass `migration-ast` / plugin (#673) / generator: per target a written verdict `verified-done | finding-filed` with command / `file:line` (no "looks ok"). Bugs found → research-output → SEPARATE umbrella (T5, do not bundle).

**§4b §1.7 PR-body mandate** applies only when a sub-wave PR touches `.claude/rules/**`, `packages/core/principles/**`, `CLAUDE.md`, `packages/core/templates/**`, `.claude/skills/**`, `agents/**`. This umbrella edits README + operator-notes + research-patches → §4b generally NOT triggered. If S4 files a finding under those paths, §4b applies (H3 headers `### §1.7 Forward/Backward-check applied`, ≥40 non-ws chars each, ≥1 `file:line` per section).

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue.

**Active canonical traps for plan-currency-reconcile:**

- **T2** — design ≠ run: actually execute the `git`-checks, not "my method would catch it".
- **T3** — every "on main / not on main" = command + output; no prose-only findings.
- **T5** — no fixes during S1/S4 research: findings → research-output, fix → separate umbrella.
- **T14** — clean skepticism-pass ≠ "umbrella DONE" at low coverage: distinguish verified-clean from coverage-insufficient.
- **T15** — self-application: this umbrella's own output table must not itself drift from `main`.

**Domain-specific traps (plan-currency-reconcile-specific, NOT in canonical catalogue):**

- **T-PCR-A** — "trust the tracker over `git`". The exact bug class: read "not done" in a tracker and believe the text instead of checking `git` → the reconcile AMPLIFIES the lie it exists to remove. Counter: every status row backed by a `git` command (§7-S1 gate).

> **Anti-pattern warning:** blanket "see ai-laziness-traps.md" without enumeration above = T7 violation. The enumeration above is binding.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between stages)

Before dispatching Stage 2 (if S1 surfaces a strategy fork, e.g. "is claim X a v2-future spec or a v1-active requirement?"), and again before closure:

```text
Agent: Phase -1 cold-review for plan-currency-reconcile Stage <N>
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer surfaces strategy forks as DECISION-NEEDED (option A → consequence X / option B → consequence Y); does NOT pick (`reviewer-discipline.md §2`).

---

## §8 Stop conditions

- Operator-notes path unreachable in an S1/S2 session → PARK + surface, do not skip E1/E2 (umbrella §9).
- S1 gate empty (no table) → HALT, do not dispatch Stage 2.
- S4 finds a real bug in a DONE-umbrella → finding to research-output, STOP (no in-umbrella fix, T5).
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any temptation to edit product code → R/doc umbrella, no-build (umbrella §2.1) — STOP.

---

## §9 Anti-scope

- No product code (R/doc, no-build, umbrella §2.1).
- No bundling of S4-found bug-fixes (T5, separate umbrella).
- No de-bitrot of `plan-currency-check` tests (branch `fix/plan-currency-check-tests`) — separate concern (umbrella §9).
- No closing of react-spa/native e2e gaps — that is U16; S3 only confirms gaps exist (precondition).
- No editing `~/.claude/skills/orchestrator/` (agent-uncommittable).

---

## §10 See also

- `.claude/orchestrator-prompts/plan-currency-reconcile/kickoff.md` — substantive umbrella SSOT (#785).
- `.claude/orchestrator-prompts/getff-to-prod-meta-launch/kickoff.md` — program sequencing (U1 ∥ U2).
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
- `.claude/skills/pipeline/SKILL.md` — generating skill.
