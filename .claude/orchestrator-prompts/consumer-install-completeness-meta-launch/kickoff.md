# KICKOFF — consumer-install-completeness (meta-launch)

> **Type:** multi-stage I-phase (fixes) + 3 design forks (generated by `/pipeline consumer-install-completeness` on 2026-06-13).
> **Origin:** plan-currency check + launch-table from `/pipeline` skill. Substance SSOT = the umbrella kickoff at `.claude/orchestrator-prompts/consumer-install-completeness/kickoff.md` (§0–§8); this meta-launch adds only the launch-table, real stage-gate commands, principle-12 §5 AI-traps, and per-stage dispatch. Do NOT duplicate the umbrella kickoff's substance — read it.
> **Deliverable:** close 3 OPEN issues — #487 (R11 paywall docs-honesty), #482 (consumer-mode skill honesty), #483 (one-button completeness) — each with an executable acceptance, on a real consumer shape.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Read in this order:

1. This kickoff (launch-table + stage gates).
2. `.claude/orchestrator-prompts/consumer-install-completeness/kickoff.md` — **the binding umbrella kickoff** (§0 pre-seeded findings with file:line evidence, §3 scope fence, §5 per-stage acceptance, §6 DECISION-NEEDED with orchestrator leans).
3. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-06-13 `b6096914`). staging tip `b6096914` (#494) == the umbrella kickoff's §0 evidence base; all 3 issues OPEN as claimed; #478 CLOSED (correctly excluded). Zero drift, zero stale-ref.
- **In-flight probe:** CLEAR — no open/merged `cic-` PR, no `cic` branch or worktree.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/consumer-install-completeness/kickoff.md`
- **Stale items:** none.
- **Load-bearing observation (NOT this umbrella's scope):** `consumer-install-hardening` was closed (#493) + promoted to main (#488) **without ever running its own S4 real-consumer re-verify** (`cih-s4*` = never merged). That S4 (`.../consumer-install-hardening/kickoff-s4.md`) re-verifies the original **13 findings** on flat-Hono + monorepo. This umbrella's S4 re-verifies only the **3 new issues**. The two S4 re-verifies share method (fresh install on flat + monorepo) — folding them is a maintainer call, surfaced in §10, not auto-decided here.

---

## §2 Launch-table

| Sub-wave | Issue | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|---|
| S1 docs-honesty | #487 | I-phase-small (docs-only) | Mode A inline | No | 1 | — (disjoint from install.sh; could run alongside S2/S3 but those are gated) | S |
| S2 consumer-mode honesty | #482 | I-phase-small | Mode A inline | No | 2 | — (file-locks install.sh w/ S3 → sequential) | S–M |
| S3 one-button completeness | #483 | I-phase-small/med | Mode A inline | No | 3 | — (file-locks install.sh w/ S2) | M |
| S4 re-verify + close | all 3 | manual-liveness / verify | Mode A inline | No | 4 | — | M |

**Mode rationale:** every stage is a single-concern execution-build (≤2 tasks) → Mode A inline, no SDD (review overhead ≥ catch-rate at this size; the Phase -1 stage-gate cold-review supplies the independent-review layer). S1 is the ONLY fork-free stage — dispatchable now. S2/S3 are BLOCKED on §6 DN-A / DN-B / DN-C. S4 depends on S3 merged.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2
```bash
gh pr list --search "is:merged head:cic-s1-r11-paywall base:staging" --json number,title,mergedAt --limit 10
```
Expected: non-empty (S1 PR merged). Empty → HALT, do NOT dispatch S2.

### Stage 2 → Stage 3
```bash
gh pr list --search "is:merged head:cic-s2-consumer-skip base:staging" --json number,title,mergedAt --limit 10
```
Empty → HALT. (S2/S3 both edit `install.sh` — run sequential to avoid same-file merge race.)

### Stage 3 → Stage 4
```bash
gh pr list --search "is:merged head:cic-s3-one-button base:staging" --json number,title,mergedAt --limit 10
```
Empty → HALT. S4 (`cic-s4-verify`) is the last stage → writes `done.md` + closes #482/#483/#487 with citation comments.

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7). **Re-run the pre-dispatch in-flight probe immediately after each Phase -1 completes, before the actual dispatch** (all 3 historical collisions materialised inside the Phase -1 window — CLAUDE.md §Operational conventions).

---

## §4 Sub-wave dispatch instructions

Each stage runs in a fresh Mode A session (maintainer pastes the §10 1-liner into a new CC tab) OR autonomously via the runtime-bridge if up (see §10 autonomous-offer). Base off the **live staging tip** (`origin/staging` lags locally — confirm via `gh api repos/Yhooi2/rules-as-tests-aif/commits/staging`). Git transport may be tunnel-blocked → push via `harvest-via-api.sh --base staging` / gh Git Data API.

- **S1 (#487, READY NOW — fork-free):** branch `cic-s1-r11-paywall`. Docs-only caveat in two shipped surfaces — `packages/preset-next-15-canonical/RULES.md` §R11 (`:266-274`) and `templates/ts-server/github-actions-workflow-integrity.yml` `::warning::` text (`:41,66-67`): GitHub-Free-private makes R11 branch-protection **permanently** warn-only (not "not yet adopted"). **Tri-state logic stays byte-identical** (T-CIC-A). Acceptance = grep/docs-audit shows caveat in BOTH shipped surfaces; diff is prose-only. **§4b applies** (RULES.md is a preset watched-path).
- **S2 (#482, BLOCKED on DN-A):** branch `cic-s2-consumer-skip`. Shape depends on DN-A verdict (don't-ship vs self-skip-guard vs parameterize). Acceptance per DN-A. Paired-negative test: framework layout present → `/pipeline` still operates (T-CIC-B).
- **S3 (#483, BLOCKED on DN-B/DN-C):** branch `cic-s3-one-button`. Scope per DN-B; opt-in shape per DN-C. Acceptance = executable post-install `node_modules` + commit-succeeds smoke (T-CIC-C: declare≠install), not "the manifest lists it".
- **S4 (re-verify+close):** branch `cic-s4-verify`. Fresh install on flat + monorepo from staging tip → all 3 acceptances PASS (or explicitly ROUTED); `make self-audit` + full principle suite green; `done.md` per CLAUDE.md schema; close the 3 issues.

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — applies to S1 + any stage touching watched paths)

Stages whose target matches `packages/preset-next-15-canonical/**`, `packages/core/templates/**`, `.claude/skills/**`, `.claude/rules/**`, `CLAUDE.md`, `agents/**` MUST include in the **PR body** (H3 headings, word "applied", ≥40 non-ws chars + ≥1 `file:line` citation per section):

```markdown
### §1.7 Forward-check applied

<≥40 chars: which existing disciplines were checked>
- `doc-authority-hierarchy.md` — <what>. file:line: `<path>:<line>`
- `build-first-reuse-default.md` — <verdict>

### §1.7 Backward-check applied

<≥40 chars: sweep of existing artefacts under scope>
- `<artefact>` — supersedes / extends / orthogonal. file:line: `<path>:<line>`
```

S1 touches `RULES.md` (watched) → §4b is **required** for S1. Pre-flight grep before `gh pr create` (the CI gate is strict; H2 silently fails). Mechanical-maintenance escape hatch: `### §1.7 Skipped: <rationale ≥60 chars>` only if purely structural. Stages whose target does NOT match the path list ignore §4b.

---

## §5 AI-traps active (per `ai-laziness-traps.md §2`)

**Active canonical traps for consumer-install-completeness:** T3, T5, T13, T14, T16, T19.

- **T3** — every fix carries an executable acceptance (command + output), not prose "fixed". #487 is docs-only but still grep-verifiable.
- **T5** — this is the I-phase; research (the 3 issues + §0 confirmation) is done. Do NOT re-audit the whole install surface; fix per §2/§4 stage descriptions.
- **T13/T16** — the shipped templates/skills ARE the subjects; "fixed" must be evidenced in a LANDED consumer install, not the source package (`#pattern-matching-on-name`: `/pipeline` *sounds* consumer-useful but its problem-class is framework-internal wave orchestration — confirm match before ship-vs-skip in DN-A).
- **T14** — "fixed" requires the per-stage acceptance to PASS; partial → say so explicitly (e.g. S3 minimum-credible done but branch-model deferred).
- **T19** — S4 own cold-QA on a real consumer shape before close; CI green ≠ shield/honesty live.

**Domain-specific traps (consumer-install-completeness-specific, NOT in canonical catalogue):**

- **T-CIC-A — «honesty fix that quietly changes behaviour».** #487 is docs-only, graceful-degradation-is-fine. The S1 diff must touch only prose/`::warning::` text — the tri-state logic (`workflow-integrity.yml:48-68`) stays byte-identical. Editing the `jq`/branch logic = out of scope, STOP.
- **T-CIC-B — «consumer-mode guard that breaks the framework's own use».** S2 self-skips in a *consumer* but keeps operating in *this* repo. The skip condition keys on a layout marker absent in consumers (`docs/meta-factory/wave-sequencing-plan.md` / no `packages/core/principles/`), with a paired-negative test that the framework-layout arm still runs.
- **T-CIC-C — «declare-vs-install confusion».** #483's root: install **declares** devDeps (`install.sh:597-604`) but never **installs** them; a fix that only edits the declaration repeats the bug. The S3 acceptance is an executable post-install `node_modules` + commit-succeeds smoke, not "the manifest lists it".

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation. The enumeration + ≥1 domain trap above satisfies principle 12.

---

## §6 DECISION-NEEDED (maintainer GO required — gates S2/S3 dispatch)

Full statement + orchestrator leans + "lean is wrong if…" falsifiers live in the umbrella kickoff §6. Summary:

- **DN-A (#482) — consumer-mode for framework-internal skills:** (A) don't-ship `/pipeline`+runtime-bridge to consumers; (B) ship + self-skip with explicit "requires framework layout — skipped in consumer mode"; (C) parameterize. **Orchestrator lean: B** (consistent with `/aif-*` honesty precedent #480/#485; least-destructive; keeps the skill readable as an example). Wrong if `/pipeline` has zero consumer value → then A.
- **DN-B (#483) — scope of "one-button":** (A) auto-install deps only (detect PM + run dev-dep install + fix hook/dep ordering); (B) + scaffolding (branch model, `.mcp.json`, plugin wiring); (C) honest-checklist only (keep manual, make accurate). **Orchestrator lean: A now, B deferred** (A is high-value/low-controversy + was the install-ordering root cause; B is opinionated + previously scoped OUT). Wrong if maintainer wants full one-button now → then S3 needs an R-phase/brainstorm first.
- **DN-C (#483 follow-on, only if DN-B≠C) — opt-in shape:** default-on dep-install vs `--with-deps` flag? **Orchestrator lean: `--with-deps` flag** (non-destructive default; explicit opt-in to a mutating install).

---

## §7 Phase -1 reviewer dispatch (between stages)

After each stage lands (all stage PRs merged, §3 gate green) and before the next stage dispatch:

```text
Agent: Phase -1 cold-review for consumer-install-completeness Stage N
Role: reviewer (read-only — not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer reads `git diff staging...<stage-N-head>` + the stage acceptance; emits GO / REVISE / STOP with BLOCKER/MAJOR/MINOR. Surfaces strategy forks as «DECISION-NEEDED: Option A → X / Option B → Y. Maintainer decides» — does NOT pick strategy, does NOT edit files, does NOT skip surface review because "CI is green" (T19). See `reviewer-discipline.md §2`.

---

## §7a Reporting format (mandatory plain-language tail)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/pipeline/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message FROM this kickoff (Worker REPORT, Reviewer verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block naming sub-waves / AC items / file:line / REPORT-traces — NOT per-turn personal reasoning (that's the hook's job; verbatim-copyable = `#two-prompts-drift`).

---

## §8 Stop conditions

- Any `gh` command errors and a stage gate cannot be verified → halt, escalate.
- Stage gate command returns empty AND Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any sub-wave returns BLOCKER finding → halt Stage; surface before proceeding.
- DN-A/DN-B/DN-C unanswered → S2/S3 dispatch is FORBIDDEN (S1 + artifact authoring proceed).
- Any temptation to edit `~/.claude/skills/orchestrator/` → STOP.

---

## §9 Anti-scope

- Do NOT reopen/redo #478 (CLOSED) or any FQA/hardening finding (F1–F15).
- Do NOT edit the `timeliner` consumer repo (it is the polygon/evidence, not a deliverable).
- Do NOT deliver full one-button scaffolding (branch model + `.mcp.json` + plugin) unless DN-C authorises it.
- Do NOT write sub-wave implementation code in this kickoff.
- Do NOT finalize project strategy — surface DN-A/B/C to maintainer.
- Do NOT add npm deps to the framework (S3 shelling out to the consumer's own PM is not a new framework capability).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).

---

## §10 See also

- `.claude/orchestrator-prompts/consumer-install-completeness/kickoff.md` — binding umbrella kickoff (substance SSOT).
- `.claude/orchestrator-prompts/consumer-install-hardening/kickoff-s4.md` — the never-run hardening S4 (13-finding re-verify) — the §1 observation.
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
- `CLAUDE.md §Operational conventions` — pre-dispatch in-flight probe.
