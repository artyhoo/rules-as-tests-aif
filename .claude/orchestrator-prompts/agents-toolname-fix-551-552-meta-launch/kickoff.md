# KICKOFF — agents-toolname-fix-551-552 (meta-launch)

> **Type:** single-PR fix dispatch (generated by `/pipeline agents-toolname-fix-551-552` on 2026-06-16).
> **Origin:** plan-currency check + in-flight probe + §2.5 routing from the `/pipeline` skill.
> **Deliverable:** one PR to `staging` fixing shipped sub-agent `tools:` names (#551) + excluding the authoring-only prober from the consumer payload (#552).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

This is a **single-stage DIRECT fix** (TYPE=fix, Mode A inline, volume S). There is **no sub-wave decomposition and no inter-stage gate** — the orchestration value here is preflight + routing + the §4b §1.7 PR-body mandate, not multi-stage sequencing. Read in this order:

1. This kickoff (routing record + §1.7 mandate + stop conditions).
2. **Binding worker spec → `.claude/orchestrator-prompts/agents-toolname-fix-551-552/kickoff.md`** — the self-contained fix kickoff (problem + GO verdict + exact 3-step fix plan + acceptance + reproduce-and-verify checklist). Do NOT re-derive the plan; execute it.
3. `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue (T3/T15/T19/T-551-A active, §5 below).

---

## §1 Inputs

- **Plan-currency check:** CURRENT (as of 2026-06-16, HEAD d6af6b0 == staging tip; `plan-currency-check.sh` rc=0).
- **In-flight probe:** CLEAR — no open PR / worktree carries this fix; #558 (merged) was the docs-only kickoff commit; issues #551 + #552 both still OPEN (fix not yet done).
- **Umbrella kickoff (binding spec):** `.claude/orchestrator-prompts/agents-toolname-fix-551-552/kickoff.md`
- **Routing:** TYPE=fix · sibling_count=0 · bundle_opt_in=FALSE → **DIRECT** (Mode A inline). load_bearing=TRUE (`agents/**` + `packages/core/principles/**`) → §4b §1.7 mandate applies.
- **Stale items:** none.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| (single) | fix | Mode A inline (DIRECT) | No | 1 | — | S |

Single atomic fix — no parallel siblings, no SDD (1 coherent change, review-overhead > catch-rate per §3 threshold).

---

## §3 Stage gates

**N/A — single stage.** There is no Stage 1 → Stage 2 transition to gate. The only gate is the umbrella's own acceptance criteria (umbrella kickoff §Acceptance) + the §4b §1.7 PR-body gate below. The merge gate is the standard auto-merge into `staging` after CI green:

```bash
gh pr list --search "is:merged head:<this-fix-branch> base:staging" --json number,title,mergedAt --limit 5
```

(Non-empty after the PR merges = done; this is the umbrella-close signal, not an inter-stage gate.)

---

## §4 Worker dispatch instructions

Open ONE fresh CC session (Mode A inline) and execute the binding worker spec verbatim:

> Read `.claude/orchestrator-prompts/agents-toolname-fix-551-552/kickoff.md` and execute its **Exact fix plan** (3 steps) from clean `staging`. Treat the previous-session draft (worktree `claude/festive-mirzakhani-46e078`) as **untrusted** — reproduce + verify, do not assume it was correct. Run the full Acceptance gate before opening the PR. Base the PR on `staging`.

The fix touches: `agents/*.md` (6 frontmatter `tools:` lines + 3 prose mentions), `install.sh` (glob case-skip + `SHIPPED_DOCS` swap), `packages/core/principles/09-doc-authority-hierarchy.ts` (`REQUIRED_HEADER_DOCS` swap — keeps count 18 + set-equal, the line-139 gate).

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — APPLIES to this fix; self-evaluate)

This fix touches `agents/**` AND `packages/core/principles/**` → both in the §1.7 path list. The PR body (not just the commit message) MUST carry:

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars: which existing disciplines were checked>

- `doc-authority-hierarchy.md` — REQUIRED_HEADER_DOCS swap keeps count 18 + set-equal. file:line evidence: `packages/core/principles/09-doc-authority-hierarchy.ts:139`
- `build-first-reuse-default.md` — fix = match upstream AI Factory CC names (ADOPT, not BUILD). file:line evidence: `/opt/homebrew/lib/node_modules/ai-factory/subagents/*.md:4`
- `no-paid-llm-in-ci.md` — no API-billed call introduced; verdict OK

### §1.7 Backward-check applied

<≥40 non-whitespace chars: sweep of existing artefacts under this change's scope>

- `agents/manual-rule-liveness-prober.md` — excluded from consumer payload, KEPT in source as authoring tool. file:line evidence: `install.sh:<glob-case-line>`
- `agents/orchestrator-worker-discipline.md` — added to SHIPPED_DOCS (was missing = bug #552-C). file:line evidence: `install.sh:<SHIPPED_DOCS-line>`
```

**Hard rules (each has historically failed the CI gate):**

1. Heading depth = **H3 (`###`)**, NOT H2. The awk regex matches `^### §1\.7 ...`.
2. The word **«applied»** is required in each heading.
3. **≥40 non-whitespace chars** in EACH section body.
4. **≥1 `file:line` citation** per section (regex `[^[:space:]]+\.[a-z]+:[0-9]+`).

**Pre-flight grep (run BEFORE `gh pr create` — compose `$PR_BODY` first):**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'   # must be 2
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'                   # must be ≥2
```

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue.

**Active canonical traps for agents-toolname-fix-551-552:** T3, T15, T19.

- **T3** — every fix backed by command/`file:line`, no prose-only edits.
- **T15** — self-application: the fix's own validity is **test-run** (`09-doc-authority-hierarchy.test.ts` + principle-12 + `tests/install-sh/*` + `check:all`), not asserted.
- **T19** — own cold-QA of the diff before handoff; CI ≠ design review.

**Domain-specific trap (NOT in canonical catalogue):**

- **T-551-A** — «fixing the frontmatter `tools:` but leaving the broken names (`read_file`/`list_files`/`run_command`) in the prose body» → looks fixed, stays inconsistent. **Counter:** after the frontmatter edit, grep agent bodies for `read_file|list_files|run_command`; only the (unshipped) prober's source may legitimately retain them.

> Blanket «see ai-laziness-traps.md» without the enumeration above = T7 anti-pattern.

---

## §6 Recursive self-application

This kickoff was generated by `/pipeline` and must itself pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Stop conditions

- Acceptance gate (`09-doc-authority-hierarchy.test.ts` line-139 sync) red → HALT, surface; do NOT force the swap.
- Cleanroom `install.sh` still copies `manual-rule-liveness-prober.md` → #552 not satisfied; HALT.
- Any temptation to TRUST the throwaway-worktree draft instead of reproducing → STOP (kickoff ⚠ section).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T-MOB-C violation; stop.

---

## §8 Anti-scope

- Do NOT widen beyond #551 + #552 (the prober's consumer-facing rule-liveness story is the separate `pressure-scenario-automation` track — do not pull it in).
- Do NOT ship the prober to consumers (the whole point of #552).
- Do NOT add npm deps.

---

## §9 See also

- `.claude/orchestrator-prompts/agents-toolname-fix-551-552/kickoff.md` — binding worker spec.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `packages/core/principles/09-doc-authority-hierarchy.test.ts:139` — the 18/set-equal gate.
- GH #551, #552, #550; SSOT #115 (`docs/meta-factory/prior-art-evaluations.md:188`).
