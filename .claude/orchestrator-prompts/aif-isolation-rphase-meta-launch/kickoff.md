<!-- bridge: skip -->
<!-- This R-phase investigates aif's OWN broken task-isolation (Finding A). Running it
     autonomously via the aif bridge is circular (agent runs in the shared checkout under
     study; aif has no mid-flight park per Finding F → guesses verdict forks). Channel =
     Mode A paste-tab only. See §4 channel constraint. Marker prevents auto re-dispatch. -->

# KICKOFF — aif-isolation-rphase (meta-launch)

> **Type:** R-phase, single (generated by `/meta-orchestrator aif-isolation-rphase` on 2026-06-01).
> **Origin:** plan-currency check + classify/route from `/meta-orchestrator` skill. Underlying R-phase born from `qloop-ux-probe` 2026-06-01 live e2e run (Findings A + F).
> **Deliverable:** ONE research-patch at `docs/meta-factory/research-patches/2026-06-0X-aif-task-isolation.md` with a verdict per Finding A + F (config-fix / aif-upstream-issue / our-side-guard) + reproduction + recommended fix path.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

This is a **single-sub-wave R-phase** — there are no inter-stage gates. Read in this order:

1. This meta-kickoff (dispatch context + classification).
2. **The real kickoff (binding scope):** `.claude/orchestrator-prompts/aif-isolation-rphase/kickoff.md` — §0 evidence, §1 research questions A1–A4/F1–F2, §2 method+constraints, §3 AI-traps, §4 acceptance, §5 stop conditions.
3. `.claude/rules/build-first-reuse-default.md §3` — the 6-layer mechanism (DeepWiki on aif-handoff FIRST, before any own-mechanism proposal).

---

## §1 Inputs

- **Plan-currency check:** План актуален (as of 2026-06-01 c56e900). This umbrella is an ad-hoc R-phase born from `qloop-ux-probe`'s live run; it is intentionally NOT in `wave-sequencing-plan.md §0` (N0–N8 tracks). The plan-cache explicitly predicted it ("Finding A scoped as R-phase"). No drift against this umbrella.
- **Dedup (§2.5):** CLEAR. No existing research-patch carries a verdict on Finding A's root cause (in-place-on-shared-checkout despite `AIF_TASK_WORKTREES_ENABLED=true`). The already-resolved `dirty_worktree` cause (scaffolding-untracked, PR #314) is a *different* root cause. No open/inflight PR with an isolation slug.
- **Underlying kickoff:** `.claude/orchestrator-prompts/aif-isolation-rphase/kickoff.md`
- **Stale items:** none.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| (single) | R-phase | Mode A inline | No | 1 | — | M (one research-patch + live reproduction + DeepWiki sweep) |

**Classification rationale:** TYPE=R-phase, single → `RESEARCH` mode → Mode A inline (one Opus session). Per §5 dispatch tree row "R-phase, single". SDD=No (research produces a doc, not ≥3 independent build tasks).

---

## §3 Stage gates

**None — single sub-wave, single stage.** There is no Stage 1→2 transition. The only "gate" is the acceptance check in the underlying kickoff §4 (research-patch exists with reproduction + per-finding verdict + recommended fix path) and the principle-10/13 annotations (`<!-- scope:aif-task-isolation -->` + §1.7 self-reflection).

Landing PR (research-patch only, no production code) merge-check, for reference:

```bash
gh pr list --search "is:merged head:rphase/aif-task-isolation base:staging" --json number,title,mergedAt --limit 5
```

---

## §4 Sub-wave dispatch instructions

**Dispatch as Mode A inline** — a fresh Opus CC session opened by the maintainer, paste the 1-liner from the meta-orchestrator §10 output. The session:

1. Reads `.claude/orchestrator-prompts/aif-isolation-rphase/kickoff.md` (binding scope).
2. Runs DeepWiki `ask_question` on the aif-handoff repo (≥3 phrasings) for worktree-isolation + resume-from-review lifecycle BEFORE proposing any own mechanism (T11/T12, BFR §3).
3. Reproduces Finding A & F against the live aif container across ≥2 dispatch cycles (T-aif-A domain trap).
4. Writes the research-patch with a verdict per finding + recommended fix path + §1.7.

**Channel constraint (load-bearing):** do NOT dispatch this R-phase autonomously via the aif-handoff runtime bridge (`dispatch.ts`). It is circular — the R-phase investigates aif's own broken isolation, and the bridge falls back to `ManualBackend` precisely on the `dirty_worktree` 409 under study. Maintainer-paste Mode A only.

**§4b §1.7 PR-body mandate:** the deliverable is a research-patch under `docs/meta-factory/research-patches/**` — that path is NOT in the §4b path list (`.claude/rules/**`, `packages/core/principles/**`, `CLAUDE.md`, etc.). §4b does NOT apply. (Principle 10 scope-annotation + principle 13 §1.7-in-patch still apply, per underlying kickoff §4 — those are the research-patch's own gates, not the §4b PR-body gate.)

**§4c autonomous park-don't-guess:** INAPPLICABLE — no autonomous aif dispatch (see channel constraint above).

---

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §2`)

See `.claude/rules/ai-laziness-traps.md §2` for the full catalogue.

**Active canonical traps for aif-isolation-rphase:**

- **T11** — propose NO own mechanism without a prior-art / DeepWiki check on aif-handoff first.
- **T12** — do NOT skip the upstream sweep because "I know how worktrees work"; the env flag's actual behaviour is repo-specific, verify don't recall.
- **T16** (pattern-matching-on-name) — `AIF_TASK_WORKTREES_ENABLED=true` SOUNDS like it isolates; verify it actually creates worktrees (`git worktree list`).
- **T2 / T4** — designing the fix ≠ doing the R-phase; deliverable is a verdict-bearing research-patch with reproduction, not "here's what I would investigate".
- **T15** (self-application) — include a §1.7 self-reflection in the research-patch.

**Domain-specific (aif-isolation-rphase-specific, NOT in canonical catalogue):**

- **T-aif-A** — «assuming the container state seen once is stable». The shared checkout's branch/dirty state changes per task; reproduce across ≥2 dispatch cycles before concluding the mechanism.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without enumeration above = T7 violation. T-numbers + ≥1 domain trap enumerated above per `ai-laziness-traps.md §3`.

---

## §6 Recursive self-application

This meta-kickoff was generated by `/meta-orchestrator` and must pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

---

## §7 Phase -1 reviewer dispatch (between stages)

**N/A — single stage.** There is no inter-stage transition requiring a Phase -1 cold-review. The maintainer reviews the research-patch landing PR directly (research-patches are append-only per the Artifact Ownership Contract; the session that discovered the gap owns the patch). The session SHOULD still run its own adversarial cold-QA of the patch before handoff (T19 — `own_qa_before_handoff`).

---

## §8 Stop conditions

- aif container unreachable / docker absent → STOP, report (can't reproduce).
- Tempted to edit aif internals or the container to "fix" mid-research → STOP (R-phase = research only; `feedback_stop_surface_not_hack_on_dispatch_fail`).
- Any temptation to dispatch this R-phase via the aif bridge → STOP (circular, see §4 channel constraint).
- Any temptation to edit `~/.claude/skills/orchestrator/` → stop.

---

## §9 Anti-scope

- Do NOT write production code (R-phase = research-patch only).
- Do NOT finalize the fix — propose verdicts, dispatch a separate I-phase later.
- Do NOT add npm deps.
- Do NOT mutate the aif container to "prove" a fix.

---

## §10 See also

- `.claude/orchestrator-prompts/aif-isolation-rphase/kickoff.md` — binding scope (the real kickoff).
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source (this umbrella is ad-hoc, not tracked there).
- `.claude/rules/build-first-reuse-default.md §3` — DeepWiki-first 6-layer mechanism.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries.
