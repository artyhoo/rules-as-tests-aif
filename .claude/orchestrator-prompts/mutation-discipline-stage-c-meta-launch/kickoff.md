# KICKOFF — mutation-discipline Stage 3 C (Tests-of-tests) — meta-launch

> **Type:** I-phase build, single (Mode A). Generated by `/meta-orchestrator mutation-discipline-umbrella` 2026-06-02.
> **Origin:** mutation-discipline umbrella, Stage 3 C. Stage-2→3 gate GREEN (B merged: #366 bash-hook mutation testing + #305 B.1 prior-art + #373 B.2 follow-up).
> **Base branch:** staging (NOT main).
> **Deliverable:** content-level paired-negative discipline — `02-paired-negative-test.test.ts` no longer passes on a test that merely has the ❌+✅ shape but asserts nothing real.

## §0 Cold-start context

Stage 1 audit (A, #219) + Stage 2 bash-mutator (B, #366/#305/#373) are merged. This is Stage 3 C.
Full umbrella spec: `.claude/orchestrator-prompts/mutation-discipline-umbrella/kickoff.md` §Stage-3-C.

## §1 Goal (the work)

Extend `packages/core/principles/02-paired-negative-test.test.ts` from a **structural** shape check (presence of ❌-negative + ✅-positive blocks) to **content-level** assertions:

1. Each paired-negative test must assert an **exit code OR `stdout`/`stderr` content** — not merely `.toBeDefined()` / shape-only.
2. Each must carry a **mutation-sanity docstring** comment (forced documentation that a mutation was tried at write-time; documentation, not automated proof).
3. For `.sh` targets: wire the **bash mutator from B (#366, universalmutator ADAPT, local/on-demand)** to register an automated mutation kill-rate where feasible.

Keep scope to principle 02 + its inputs. Do NOT refactor unrelated principles.

## §2 Acceptance

- `02-paired-negative-test.test.ts` FAILS on a test that has the ❌+✅ shape but asserts only `.toBeDefined()` (content-level gate proven by a paired-negative on the gate itself).
- Existing real paired-negative tests still PASS (no false-positive cascade — F4 below).
- `npm --prefix packages/core run test:principles` green.

## §3 Stage gate (already passed — do not re-dispatch upstream)

Stage 2→3: `gh pr list --search "is:merged mutation base:staging"` → #366 present. ✅ Confirmed 2026-06-02.

## §4 §1.7 PR-body mandate (paths-triggered — APPLIES: target is `packages/core/principles/**`)

This sub-wave edits `packages/core/principles/02-paired-negative-test.test.ts` → matches the §4b path list. The PR body MUST carry `### §1.7 Forward-check applied` + `### §1.7 Backward-check applied` (H3 headings, word «applied», ≥40 non-ws chars each, ≥1 `file:line` citation each), OR the `### §1.7 Skipped: <≥60-char rationale>` escape hatch. Run the pre-flight grep before `gh pr create`. (Recurrence-prone: #58/#69/#105/#111/#212/#215.)

## §4c Autonomous aif dispatch — park-don't-guess contract (MANDATORY — this kickoff is bridge-dispatched)

**aif agent — fork discipline (non-negotiable):** On ANY genuine fork or ambiguity (two defensible ways to express the content-level assertion; an undecided choice about how strict the gate should be; a mutation-sanity-docstring format that changes behaviour) — **do NOT pick.** Park it: set the task to `manualReviewRequired` / `blocked_external` with the fork stated as «Option A → consequence X / Option B → consequence Y», and **stop that task.** Proceed only on the unambiguous parts. Guessing a fork to "keep moving" is the exact failure this loop prevents. (`ask-question-reminder.sh:50-54` discipline.)

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §2`)

See `.claude/rules/ai-laziness-traps.md §2`. **Active traps for Stage 3 C:**
- **T3** — every content-level assertion must reference real exit/stdout/stderr from the actual test target, not a plausible-looking guess. file:line evidence in the PR.
- **T4** — closing C before the gate's OWN paired-negative proves it FAILS on a shape-only test (the content-gate must itself be falsifiable).
- **T16** — the bash-mutator from B is a real tool; verify its output format matches what principle 02 expects before wiring (don't assume by name).
- **T20** — no «this assertion is enough» verdict without running the test (red on shape-only, green on real).
- **Domain-specific (T-C-A):** «structural check already passes, so content is fine» — the whole point is structural-pass ≠ content-real; do NOT conflate.

## §6 Stop conditions
- F4: principle 02 extension false-positive-cascades on existing tests → revert, narrow scope, retry.
- aif container unreachable → STOP, report.
- Genuine fork on gate strictness / assertion shape → PARK (per §4c), do not guess.
