# KICKOFF — aif-operator-asset-access (meta-launch)

> **Type:** R-phase-first umbrella (generated by `/meta-orchestrator aif-operator-asset-access` on 2026-06-03).
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** Stage 1 = `docs/meta-factory/research-patches/2026-06-03-aif-operator-asset-access.md` — per-candidate verdict (A–E) + recommended composition + DN-1/2/3 surfaced. Stage 2 = I-phase implementing the chosen composition (gated on Stage-1 verdict + DN-1 maintainer resolution).
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This kickoff (binding scope + stage gates).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot.
3. The umbrella kickoff: `.claude/orchestrator-prompts/aif-operator-asset-access/kickoff.md` (the real scope — candidates A–E, DN-1/2/3, AI-traps).

---

## §1 Inputs

- **Plan-currency check:** План актуален (as of 2026-06-03 `4e829ec`). HEAD = #387 (latest merged); 0 open PRs; 0 commits on origin/staging not in HEAD.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/aif-operator-asset-access/kickoff.md` (CANON-backed: `~/.claude-coordination/rules-as-tests-aif/aif-operator-asset-access/kickoff.md`).
- **Stale items:** None blocking. `wave-sequencing-plan.md §0` reconciled 2026-06-02; the `UNTRACKED-KICKOFF` / `UNTRACKED-PR` reverse-currency lines are by-design (§0 tracks *waves*, not micro-umbrellas).

---

## §2 Launch-table

Launch table — aif-operator-asset-access (as of `4e829ec`):

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| R | R-phase (solution-space survey + per-candidate verdict + DN scoping) | **Mode A — operator-side CC** (NOT aif; see §2.1) | No | 1 | — | M |
| I | I-phase (implement chosen composition; likely combo B+D+C, A operator-only) | **TBD-by-verdict** (classified after Stage 1 lands) | TBD | 2 | — | M–L |

### §2.1 Why Stage-1 R-phase is operator-side, NOT autonomous-aif (load-bearing)

The runtime-bridge IS reachable (`/tasks` 200, `/health` 200, 2026-06-03). The `/` 404 is a liveness probe, not a readiness verdict — do not read it as "bridge dead." **Nonetheless this R-phase must NOT be dispatched to aif**, because:

- The R-phase's core task (DN-2) is to inspect the **operator's global skills** — `~/.claude/skills/orchestrator/`, `~/.claude/skills/reviewer/`, Superpowers `~/.claude/plugins/` — to decide *what discipline needs bridging*.
- The aif agent runs in a container with **only the repo clone bind-mounted** — it is blind to operator-home (`~/.claude/`). That blindness is the exact gap this umbrella studies.
- Dispatching the R-phase to aif → it researches its own subject while blind to it. `#autonomous-dispatch-without-park` is not even the issue; the issue is **input-blindness**: aif cannot read the assets it must evaluate.

→ Stage 1 = fresh **operator-side** CC session (paste-tab). Self-application note (T15): once Stage 2 ships the in-repo B/D artifacts, *those* ride the clone and a later aif dispatch CAN see them — that is the umbrella's own success test.

### §2.2 Stage 2 deferred classification (honest)

Stage 2 TYPE = `I-phase-large, NOT scope_decided` until Stage 1 verdict exists → routing tree would resolve to RESEARCH today, which is circular. Correct handling: **do not classify Stage 2 now.** After Stage 1 lands a verdict, re-run `/meta-orchestrator aif-operator-asset-access` (or classify the chosen composition's pieces individually) — each piece is then `scope_decided` and routes properly (B/D in-repo agnostic artifacts → likely PAIR/SDD with paired-negative tests; C `dispatch.ts` → PAIR with paired test; A docker-compose mount → operator-runbook, outside-repo, gated on DN-3).

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2

Stage 2 (I-phase) dispatch waits for BOTH:

**(a) The R-phase research-patch PR merged to `staging`:**

```bash
gh pr list --search "is:merged head:research/aif-operator-asset-access base:staging" \
  --json number,title,mergedAt,headRefName --limit 10 2>/dev/null \
  || echo "gh unavailable — cannot verify stage gate"
```

Expected: non-empty JSON (the research-patch PR). If empty → halt. Do NOT dispatch Stage 2.

**(b) DN-1 resolved by maintainer** (operator-axis vs shipped-axis split — see §4 below). This is a doctrine fork the R-phase *surfaces*; the maintainer *resolves* it. Stage 2 scope (which candidates become shipped capabilities vs operator-only convenience) is undefined until DN-1 has a content answer. Auto-proceeding on an unresolved DN-1 = `#strategy-decided-by-reviewer`.

### Stage 2 → (done)

No Stage 3. After Stage 2 lands, write `done.md` per CLAUDE.md umbrella-closure convention and verify liveness (a real aif dispatch sees the bridged asset — §4 I-phase liveness requirement).

**Phase -1 cold-review between Stage 1 and Stage 2 is MANDATORY** (see §7 below). CI ≠ design review (T19).

---

## §4 Sub-wave dispatch instructions

### Stage 1 — R-phase (operator-side CC session)

Open a fresh CC session (paste-tab). Dispatch prompt:

> `/orchestrator` — R-phase for `aif-operator-asset-access`. Read `.claude/orchestrator-prompts/aif-operator-asset-access/kickoff.md` (the real scope). Mode A, single Opus R-phase, operator-side (you have `~/.claude/` access — aif does NOT, which is the whole point).
>
> **Task:** evaluate candidates A–E (§2 of the umbrella kickoff) against `build-first-reuse-default.md` (ADOPT/ADAPT/BUILD/REJECT each), accounting for the existing in-repo mechanisms (`packages/core/templates/shared/skill-context/`, `agents/*.md`, `link-coordination.sh`, `.ai-factory/skill-context`) — do NOT reinvent (BFR §3 6-layer + DeepWiki/WebSearch ≥3 phrasings on any negative-existence claim). Re-confirm the §1 gap table with fresh evidence (T3 — the meta-orchestrator-refactor audit was overstated in places). Surface DN-1/2/3 as decision-needed with both options' consequences (reviewer-discipline.md §2) — do NOT silently pick the operator-vs-shipped split.
>
> **Deliverable:** `docs/meta-factory/research-patches/2026-06-03-aif-operator-asset-access.md` (per-candidate verdict + recommended composition + DN surfacing). If it adds/edits an SSOT row in `prior-art-evaluations.md`, §4b applies (see below). No implementation — R-phase produces the patch + verdict ONLY (umbrella §6 anti-scope).
>
> Own cold-QA before handoff (T19). End with the `## 🟢 Простыми словами` checkpoint tail.

### Stage 2 — I-phase (deferred; re-dispatch after Stage 1 verdict + DN-1)

Do NOT pre-author. After Stage 1 lands: re-invoke `/meta-orchestrator aif-operator-asset-access` to classify the chosen composition's pieces, or dispatch each atomic piece per the verdict. Each in-repo discipline artifact (B/D) carries Class + Authoritative-for header (`doc-authority-hierarchy.md`) + paired-negative test where mechanizable. `dispatch.ts` changes (C) carry a paired test. Verify a real aif dispatch then sees the bridged asset (liveness, not just "wired").

---

## §4b §1.7 PR-body authoring mandate (paths-triggered — self-evaluate per sub-wave)

A Worker whose target file matches ANY of `.claude/rules/**`, `packages/core/principles/**`, `docs/meta-factory/EXECUTION-PLAN.md`, `docs/meta-factory/prior-art-evaluations.md`, `CLAUDE.md`, `packages/core/templates/**`, `.claude/skills/**`, `agents/**` MUST include the `### §1.7 Forward-check applied` + `### §1.7 Backward-check applied` H3 sections in the **PR body** (≥40 non-whitespace chars each; ≥1 `path:line` citation each; heading depth = H3 `###`, word "applied" required).

- **Stage 1 (R-phase):** applies ONLY if the research-patch also edits `prior-art-evaluations.md` (an SSOT row for aif-handoff). The research-patch file itself under `research-patches/` is NOT in the path list. Mechanical-maintenance escape hatch `### §1.7 Skipped: <≥60-char rationale>` if no discipline surface.
- **Stage 2 (I-phase):** WILL hit `.claude/rules/**` and/or `agents/**` and/or `templates/**` → §4b is live. Run the pre-flight grep (4 checks) before `gh pr create`.

Full mandate text + pre-flight grep: see the `/meta-orchestrator` SKILL.md §4b (path-scoped, H3-depth, "applied" keyword, 6× historical recurrence).

---

## §5 AI-traps active (per `ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue.

**Active canonical traps for aif-operator-asset-access** (mirrors umbrella kickoff §5):

- **T3** — re-confirm the §1 gap table with fresh evidence; do NOT trust the meta-orchestrator-refactor audit (overstated in places).
- **T11 / T12** — BFR search (DeepWiki/WebSearch ≥3 phrasings + SSOT consult) BEFORE any BUILD verdict; the `skill-context/` + `agents/*.md` mechanisms already exist.
- **T13 / T16** — do NOT assume existing `skill-context/aif-*` covers orchestrator/reviewer discipline; verify upstream-vs-our problem-class match explicitly.
- **T15** — self-application: the fix must work when THIS umbrella is later dispatched via aif (the in-repo B/D artifacts are the test).
- **T19** — own cold-QA before handoff (CI ≠ design review).
- **T20** — every verdict (ADOPT/ADAPT/BUILD/REJECT) needs ≥1 evidence-bearing tool call in the same turn.

**Domain-specific trap (aif-operator-asset-access-specific, NOT in canonical catalogue):**

- **T-AOA-A** — "solve it by committing the global orchestrator skill into the repo." **FORBIDDEN** (CLAUDE.md: `~/.claude/skills/orchestrator/` is agent-uncommittable, owner=maintainer). The agnostic equivalent is an `agents/*.md` or skill-context artifact authored fresh, NOT a copy of the global skill.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation. The enumeration + T-AOA-A is mandatory.

---

## §6 Recursive self-application

This kickoff was generated by `/meta-orchestrator` and must pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 2>/dev/null | tail -5
```

Deeper self-application (the umbrella's own thesis): once Stage 2 ships, dispatch THIS umbrella's kickoff via aif and confirm the agent now sees the bridged orchestrator/reviewer discipline. That liveness run is the umbrella's success criterion, not a green CI.

---

## §7 Phase -1 reviewer dispatch (between Stage 1 and Stage 2)

Before dispatching Stage 2, invoke:

```text
Agent: Phase -1 cold-review for aif-operator-asset-access Stage 1
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
```

Reviewer reads the research-patch diff + the umbrella kickoff's R-phase acceptance criteria, emits GO / REVISE / STOP. For DN-1 (operator-vs-shipped split) and any strategy fork: emits «DECISION-NEEDED: <one-line>. Option A → X. Option B → Y. Maintainer decides» — does NOT pick. See `reviewer-discipline.md §2`.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block naming sub-waves / AC items / file:line / verdict — NOT per-turn personal reasoning (that is the hook's job; duplicating it = `#two-prompts-drift`).

---

## §8 Stop conditions

- Any temptation to dispatch the Stage-1 R-phase to aif → STOP (input-blindness per §2.1).
- DN-1 unresolved but Stage 2 about to dispatch → STOP, surface to maintainer.
- Stage gate command empty AND Stage 1 expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface.
- Any temptation to commit `~/.claude/skills/orchestrator/` into the repo → T-AOA-A violation; stop.
- Any temptation to edit `~/.claude/skills/orchestrator/` → anti-scope violation; stop.

---

## §9 Anti-scope

- Do NOT commit/copy `~/.claude/skills/orchestrator/` into the repo (agent-uncommittable, owner=maintainer).
- Do NOT make consumer-side aif hard-require the operator's `~/.claude/` (shipped-axis agnostic invariant — DN-1).
- Do NOT add npm deps. Do NOT edit the global skills.
- Do NOT finalize the operator-vs-shipped split (DN-1) — surface it; the maintainer resolves.
- R-phase produces a research-patch + verdict ONLY — no implementation until verdict + DN-1 resolved.

---

## §10 See also

- `.claude/orchestrator-prompts/aif-operator-asset-access/kickoff.md` — the real umbrella scope (candidates A–E, DN-1/2/3).
- `docs/runtime-bridge-setup.md` — clone/mount model (why aif is blind to operator-home).
- `.claude/rules/build-first-reuse-default.md §1.1` — two-axis (operator vs shipped) doctrine, load-bearing for DN-1.
- `.claude/rules/dual-implementation-discipline.md` — CC-native + portable-fallback (relevant to B/D shipping).
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue · `.claude/rules/reviewer-discipline.md §2` — reviewer role.
- `packages/core/templates/shared/skill-context/` · `agents/*.md` · `scripts/link-coordination.sh` · `docs/meta-factory/prior-art-evaluations.md` (SSOT rows #67/#97).
