# KICKOFF — meta-orchestrator-refactor (meta-launch)

> **Type:** meta-launch wrapper (generated by `/meta-orchestrator meta-orchestrator-refactor` on 2026-06-03).
> **Origin:** plan-currency check + launch-table from the `/meta-orchestrator` skill.
> **Deliverable:** launch-table + stage sequencing + dispatch report for the 7-stage (0–6) audit-first → fixes → rename umbrella.
> **Base branch:** staging (NOT main — main is prod, manual promote only).
> **THIS IS A THIN WRAPPER.** The umbrella kickoff `.claude/orchestrator-prompts/meta-orchestrator-refactor/kickoff.md` is already fully self-contained (§3 gates, §4 park-contract, §4b PR-body mandate, §5 AI-traps, §6 recursive, §7 reviewer, §8 stop, §9 anti-scope, §4c runner analysis). Read it + `audit-plan.md` for everything. This file adds ONLY the launch-table + plan-currency snapshot + runner decision. No discipline is duplicated (avoids `#two-prompts-drift`).

---

## §0 Cold-start (read in order)

1. `README.md#why-this-exists` → `.claude/session-bootstrap.md` → `CLAUDE.md`.
2. **`.claude/orchestrator-prompts/meta-orchestrator-refactor/audit-plan.md`** — first-pass findings §1–§9 + **§10 re-verification (load-bearing — supersedes first-pass)**.
3. **`.claude/orchestrator-prompts/meta-orchestrator-refactor/kickoff.md`** — the rich umbrella kickoff (binding).
4. This wrapper (launch-table + sequencing only).

---

## §1 Plan-currency snapshot (as of 2026-06-03, HEAD `4e829ec`=#387)

- **Verdict: ПЛАН АКТУАЛЕН** for dispatching this umbrella.
- HEAD `4e829ec` (#387) fully synced with `origin/staging`: 0 ahead / 0 behind, 0 commits on origin not in HEAD.
- Zero open PRs → no in-flight collision on this umbrella.
- The ~100 `UNTRACKED-KICKOFF` lines from `plan-currency-check.sh` are by-design noise (CANON mirrors + this fresh 2026-06-03 umbrella not yet in `wave-sequencing-plan.md §0`) — NOT blocking drift.
- Stale remote branch `origin/research/meta-orchestrator-refactor-design-G` = the now-deleted authoring worktree's design branch (older context, superseded by the 2026-06-03 kickoff/audit-plan, no open PR).

---

## §2 Launch-table (manual — launch-table-generator found no auto-rows; kickoff §2 uses bullet-format stages, the F5 limitation)

| Stage | Name | Type | Mode | SDD? | Seq | Parallel sibling | Volume | DN-gated |
|---|---|---|---|---|---|---|---|---|
| 0 | Verify the plan | R-phase (cold-review) | Mode A — 1 read-only reviewer | No | 0 | — | S | — (DN-free) |
| 1 | Comprehensive audit (the real one) | R-phase (9 angles, full-coverage) | Mode A × N inline read-only Agents (fan-out by angle; docs not code → no worktrees) + adversarial re-verify sub-pass | No | 1 | — | L | — (DN-free) |
| 2 | Correctness fixes | I-phase-small × ~6 atomic (C1.1, C1.2, C1.3, M4, F2, F7) | Mode A — small atomic PRs (bundle-eligible: ≥3 independent same-type fixes) | borderline | 2 | — | M | — (DN-free) |
| 3 | Worktree hardening | I-phase (W3 batch-gh, W4 portable slug, M5 flock, F7 overlap) | Mode A / PAIR | No | 3 | — | M | **DN-5** (gh-batch semantics), **DN-6** (flock vs document) |
| 4 | Slim + hygiene | I-phase-large (multi-surface: SKILL.md compress, references split, dual-pair, helper dedup→lib) | Mode B × N worktrees OR PAIR | possibly | 4 | — | M–L | **DN-4** (mode-overrides wire vs defer), **DN-7** (cut depth), **DN-8** (assign-skill keep/gate/simplify) |
| 5 | Rename (DN-gated) | I-phase — atomic blast-radius (~35 LIVE files; leave 45 FROZEN) | Mode A — single session, atomic (C4.6 classify-rename folds in with P19 + SKILL.md §2.5) | No | 5 | — | M | **DN-1** (the name), **DN-2** (rename principle-test filenames?) |
| 6 | Eval/behavioral gate (NS1≡WS5) | I-phase — build `evals/evals.json` + run slimmed-vs-current | Mode A | No | 6 | — | M | **DN-9** (accept as exit-gate vs defer) |

**All stages sequential.** Stage N+1 waits on ALL Stage N PRs merged to staging (§3 gate in umbrella kickoff) + mandatory Phase -1 cold-review (§7). No intra-stage parallel siblings as written — though Stage 1's 9 audit angles fan out concurrently (read-only Agents), and Stage 2's ~6 atomic fixes are independent (bundle-eligible).

### Verified-truth bug list (build Stages 2–3 ONLY on these — §10 corrected the first pass)
**SOLID (CONFIRMED, re-verified file:line):** C1.1 · C1.2 · C1.3 · F2 · F7 · W3 · W4 · M4.
**DO NOT carry forward (refuted/downgraded):** WS1 «52 narrative lines» (~10× overcount, real ≈5–8) · §7 reviewer is ADAPT not thin-wrap · W2 symlinks by-design (real gap = no flock) · F3/F4/F5 minor/process-gap not severe.

---

## §3 Stage gates + §4 park-contract + §5 AI-traps + §7 reviewer

**All inherited verbatim from the umbrella kickoff** — do NOT duplicate here. The umbrella kickoff §3 (real `gh pr list --search is:merged` gates), §4 (9-DN park-don't-guess contract), §4b (§1.7 PR-body mandate), §5 (T1/T2/T3/T4/T11/T12/T15/T16/T19/T20 + T-MOR-A domain trap), §6 (recursive — keep P12/15/18/19/20 GREEN), §7 (Phase -1 cold-review between EVERY stage), §8 (stop conditions), §9 (anti-scope) are binding.

**The 9 DNs are genuine forks — PARK + surface, never guess.** They gate Stages 3–6 only. Stages 0/1/2 are DN-free and can run immediately. DN-1 (the name) and DN-2 (rename file scope) surface at Stage 5; DN-4/7/8 at Stage 4; DN-5/6 at Stage 3; DN-9 at Stage 6.

---

## §6 Runner decision

See `audit-plan` sibling kickoff §4c. **Recommended runner: a Claude Code session on the operator machine** — it has the global `orchestrator`/`reviewer`/Superpowers skills AND can read `audit-plan.md` (the load-bearing input, which is gitignored + CANON-symlinked = unreachable by aif-handoff dispatch without inlining/committing it). This audit-heavy, skill-leaning umbrella fits a CC session far better than aif. Surfaced to maintainer as a fork (aif autonomy is possible but needs audit-plan inlined into the dispatch payload).

---

## §7 See also

- `.claude/orchestrator-prompts/meta-orchestrator-refactor/kickoff.md` — the binding umbrella kickoff.
- `.claude/orchestrator-prompts/meta-orchestrator-refactor/audit-plan.md` — the substantive audit input (§10 = corrected truth).
- `.claude/skills/meta-orchestrator/SKILL.md` — the generating skill (also the subject of this refactor).
- `state.md` (this dir) — dispatch state log.
