# KICKOFF — aif-handoff-as-runtime-bridge (meta-launch)

> **Type:** R-phase umbrella (4 sub-waves: A/B parallel → C synthesis → D verdict). Generated by `/meta-orchestrator aif-handoff-as-runtime-bridge` on 2026-05-29.
> **Origin:** plan-currency check + launch-table from `/meta-orchestrator` skill.
> **Deliverable:** 4 markdown research-patches at `docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-{variant-a-mcp,variant-b-watcher,variant-c-minimal,synthesis}.md`. Each scored against §4 5-criteria + BFR-default verdict. Synthesis patch proposes SSOT additive «Trigger to revisit» updates to rows #30/#44/#67 and 0-3 new rows depending on verdict shape.
> **Base branch:** staging (NOT main — main is prod, manual promote only).

---

## §0 Cold-start context

Sub-waves and their mode decisions are in §2 launch-table below. Read in this order:

1. This meta-kickoff (binding scope + stage gates + admission-gate status).
2. `docs/meta-factory/wave-sequencing-plan.md §0` — plan-currency snapshot (acknowledged stale; see §1.1).
3. Umbrella kickoff: `.claude/orchestrator-prompts/aif-handoff-as-runtime-bridge/kickoff.md` — 338 LOC, authoritative for variant breakdown + criteria + sub-wave content.

---

## §1 Inputs

- **Plan-currency check:** **DRIFT** (4 items) as of 2026-05-29 `eab8eeb`.
  - DRIFT-1: `wave-sequencing-plan.md §0` Meta-orchestrator row references F.3 as «(this PR)» but PR #263 MERGED 2026-05-28T16:32:32Z.
  - DRIFT-2: 40+ UNTRACKED merged PRs (#218..#263) since last §0 reconciliation 2026-05-25.
  - DRIFT-3: This umbrella has a kickoff but is NOT in §0.
  - Cache OK by SHA; mechanical state wins.
- **Umbrella kickoff:** `.claude/orchestrator-prompts/aif-handoff-as-runtime-bridge/kickoff.md` (338 LOC, DRAFT 2026-05-28).
- **Admission gates:**
  - Gate 1 (F.3 merged): ✅ PR #263 merged 2026-05-28T16:32:32Z.
  - Gate 2 (maintainer prioritisation explicit): ✅ This `/meta-orchestrator aif-handoff-as-runtime-bridge` invocation IS the explicit selection.
  - Gate 3 (Phase -1 cold-review of THIS umbrella's kickoff): ❌ NOT DONE — required before Sub-wave A dispatch.
  - Gate 4 (no upstream capability commits in 30d): ❌ FAILED — `lee-to/aif-handoff` has 2 runtime-adapter capability PRs in last 30 days (verified via `gh api repos/lee-to/aif-handoff/pulls/{127,128}`): **PR #128** `feat(runtime): support proxy env across adapters` (merged 2026-05-26T08:07:12Z, merge SHA `51ce96a`) + **PR #127** `feat(runtime): per-profile environment overrides for Claude adapter` (merged 2026-05-15T09:05:00Z, source commit `b99807f` authored 2026-05-13T22:07:13Z, merge SHA `e1ffa70`). Sub-wave A and Sub-wave C scope MUST extend to cover the new proxy-runtime + per-profile-env adapter surface.
- **Stale items:** plan §0 reconciliation deferred — recordkeeping debt, not blocking this umbrella.

---

## §2 Launch-table

| Sub-wave | Type | Mode | SDD? | Stage | Parallel sibling | Volume |
|---|---|---|---|---|---|---|
| A | R-phase research (Variant A — MCP-consumer) | Mode A inline / fresh CC session / worktree | No | 1 | B | L (~400-600 LOC patch) ✅ MERGED PR #268 |
| B | R-phase research (Variant B — native fs-watcher) | Mode A inline / fresh CC session / worktree | No | 1 | A | M (~300-450 LOC patch) ✅ MERGED PR #267 |
| **B2** | **R-phase research (Variant B' — hybrid CC-hook + aif-handoff MCP wrapper)** | **Mode A inline / fresh CC session / worktree** | **No** | **2** | **C (parallel-safe)** | **M (~300-450 LOC patch) — added 2026-05-29 post-DN-2 reframing** |
| C | R-phase research (Variant C — minimal bridge) | Mode A inline / fresh CC session / worktree | No | 2 | B2 (parallel-safe) | M (~300-450 LOC patch) |
| D | R-phase synthesis + verdict (4 variants) | Mode A inline / fresh CC session | No | 3 | — | L (~600-800 LOC patch — expanded for 4 variants) |

**SDD?=No rationale:** All sub-waves produce markdown research-patches (no code). SDD's spec+code+quality-review pattern fits execution-build with ≥3 independent tasks; R-phase research = single-author deliverable per sub-wave.

**Parallel A/B in worktrees:** required per `.claude/rules/parallel-subwave-isolation.md §1` — separate worktrees so `git checkout -b` does not race on the shared `.git/index`. Mandatory first step in each Sub-wave A and B prompt.

---

## §3 Stage gates (real git checks — NOT in-memory FIFO)

### Stage 1 → Stage 2 ✅ CLEARED 2026-05-29

Both Stage 1 PRs merged:

```bash
gh pr list --search "is:merged head:research/aif-handoff-bridge-variant-a base:staging" --json number,title,mergedAt --limit 5
# → PR #268 merged 2026-05-29T08:58:51Z (squash 22ce0c2)
gh pr list --search "is:merged head:research/aif-handoff-bridge-variant-b base:staging" --json number,title,mergedAt --limit 5
# → PR #267 merged 2026-05-29T08:58:20Z
```

Phase -1 round 1 → REVISE → follow-up PR #269 merged 09:57:02Z. Decisions 8 (DN-1) + 9 (DN-2 refined as launch B2) recorded.

### Stage 2 → Stage 3

Sub-wave D dispatch waits for BOTH Sub-wave B2 AND Sub-wave C PRs to be merged:

```bash
gh pr list --search "is:merged head:research/aif-handoff-bridge-variant-b-prime base:staging" --json number,title,mergedAt --limit 5
gh pr list --search "is:merged head:research/aif-handoff-bridge-variant-c base:staging" --json number,title,mergedAt --limit 5
```

Expected: **BOTH** must return ≥1 row. If either is empty → halt.

### Stage 3 → end

Sub-wave D is the synthesis + verdict. After its PR merges, the umbrella closes — D's verdict either greenlights a future I-phase (separate umbrella) or DEFERs with updated SSOT triggers.

**Phase -1 cold-review between every stage transition is MANDATORY** (see §7 below). The first Phase -1 (gate 3) runs against THIS meta-kickoff + the umbrella kickoff itself BEFORE Stage 1 dispatch.

---

## §4 Sub-wave dispatch instructions

For each Sub-wave, the maintainer (or auto-dispatcher) sets up the worktree (when parallel) and opens a fresh CC session. The 1-liner blocks below are the exact paste prompts.

> **Self-setup convention (2026-05-29 maintainer directive):** each Sub-wave dispatch prompt is **self-contained** — paste into a fresh CC tab opened in **primary workdir** (`/Users/art/code/rules-as-tests-aif`), the orchestrator session itself runs the worktree setup as its first Bash invocations, then proceeds with the R-phase task. Maintainer does NOT pre-create worktrees by hand. Race-safety: parallel sub-wave tabs each `git worktree add` to **different** target directories; the brief `.git/worktrees/` index update is atomic per git.

### Sub-wave A — Variant A (MCP-consumer) ✅ MERGED PR #268

Paste into fresh CC session opened in **primary workdir**:

```
You are dispatched as Sub-wave A Worker for the aif-handoff-as-runtime-bridge umbrella. Your first action is to set up your own isolated worktree from origin/staging, then perform the R-phase work.

# Self-setup (run as first Bash invocations):
cd /Users/art/code/rules-as-tests-aif
git fetch origin staging
git worktree add ../rules-as-tests-aif-aif-handoff-sw-A origin/staging
cd ../rules-as-tests-aif-aif-handoff-sw-A
git checkout -b research/aif-handoff-bridge-variant-a
# Link node_modules to primary workdir (Gap-3 worktree pattern; see meta-launch §4a):
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
fi

# Then proceed with R-phase task:
/orchestrator aif-handoff-as-runtime-bridge §6 Sub-wave A — Mode A inline R-phase: Variant A (MCP-consumer) deep evaluation per umbrella kickoff §6 Sub-wave A. Output: docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-variant-a-mcp.md. AI traps active per §5: T1, T3, T7, T11, T12, T13, T15, T16, T17, T19, T20, T-AIF-BRIDGE-A. Gate-4 finding: extend MCP-schema sweep to cover lee-to/aif-handoff PR #128 `feat(runtime): support proxy env across adapters` (merged 2026-05-26, merge SHA 51ce96a) + PR #127 `feat(runtime): per-profile environment overrides for Claude adapter` (merged 2026-05-15, source commit b99807f authored 2026-05-13). PR base: staging. §1.7 PR-body mandate per §4b applies. **Important — §1.7 CI gate WILL NOT fire on this PR** (research-patches/** not in workflow trigger paths); pre-flight grep is the only programmatic check.
```

### Sub-wave B — Variant B (filesystem watcher) ✅ MERGED PR #267

Paste into fresh CC session opened in **primary workdir**:

```
You are dispatched as Sub-wave B Worker for the aif-handoff-as-runtime-bridge umbrella. Your first action is to set up your own isolated worktree from origin/staging, then perform the R-phase work.

# Self-setup (run as first Bash invocations):
cd /Users/art/code/rules-as-tests-aif
git fetch origin staging
git worktree add ../rules-as-tests-aif-aif-handoff-sw-B origin/staging
cd ../rules-as-tests-aif-aif-handoff-sw-B
git checkout -b research/aif-handoff-bridge-variant-b
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
fi

# Then proceed with R-phase task:
/orchestrator aif-handoff-as-runtime-bridge §6 Sub-wave B — Mode A inline R-phase: Variant B (fs-watcher) viability probe per umbrella kickoff §6 Sub-wave B. Output: docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-variant-b-watcher.md. AI traps active per §5: T1, T3, T7, T11, T12, T13, T15, T16, T17, T19, T20, T-AIF-BRIDGE-B. Adversarial counter-prompt mandatory for «no directory-watch mode exists» negative-existence claim. PR base: staging. §1.7 PR-body mandate per §4b applies. **Important — §1.7 CI gate WILL NOT fire on this PR** (research-patches/** not in workflow trigger paths); pre-flight grep is the only programmatic check.
```

### Sub-wave B2 — Variant B' (hybrid CC-hook + aif-handoff MCP wrapper) — Stage 2 (parallel-safe with C)

Paste into fresh CC session opened in **primary workdir** (parallel-safe with SW-C tab — they write to different worktree dirs):

```
You are dispatched as Sub-wave B2 Worker for the aif-handoff-as-runtime-bridge umbrella. Your first action is to set up your own isolated worktree from origin/staging, then perform the R-phase work.

# Self-setup (run as first Bash invocations):
cd /Users/art/code/rules-as-tests-aif
git fetch origin staging
git worktree add ../rules-as-tests-aif-aif-handoff-sw-B2 origin/staging
cd ../rules-as-tests-aif-aif-handoff-sw-B2
git checkout -b research/aif-handoff-bridge-variant-b-prime
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
fi

# Then proceed with R-phase task:
/orchestrator aif-handoff-as-runtime-bridge §6 Sub-wave B2 — Mode A inline R-phase (Opus per Decision #5): Variant B' (hybrid CC-hook + aif-handoff MCP wrapper) feasibility + scoring per umbrella kickoff §6 SW-B2 (added 2026-05-29). Output: docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-variant-b-prime-hybrid.md. AI traps active per §5: T1, T3, T7, T11, T12, T13, T15, T16, T17, T19, T20, T-AIF-BRIDGE-B'-1 through T-AIF-BRIDGE-B'-5 (umbrella kickoff §6 SW-B2). Origin: SW-B (PR #267) scoped Variant B narrowly to «aif-handoff has native fs-watcher»; SW-B2 explicitly probes the hybrid where WE build a small fs-watcher (CC PostToolUse hook or chokidar Node script or inotifywait/fswatch bash, ~20-100 LOC) that calls aif-handoff's EXISTING handoff_create_task MCP. Read SW-A patch (criterion 5 + ADOPT-blockers) and SW-B patch (native fs-watcher absence verdict) as baseline. Apply DN-1=B-constrained (Variant A's autoMode-Reviewer constraint) to B' as well — B' inherits A's Reviewer-cycle. Apply per-blocker walk against SW-A's 3 ADOPT-blockers. §1.7 PR-body mandate per §4b applies. **Important — §1.7 CI gate WILL NOT fire on this PR** (research-patches/** not in workflow trigger paths); pre-flight grep is the only programmatic check. PR base: staging.
```

### Sub-wave C — Variant C (minimal bridge) — Stage 2 (parallel-safe with B2)

Paste into fresh CC session opened in **primary workdir** (parallel-safe with SW-B2 tab — they write to different worktree dirs):

```
You are dispatched as Sub-wave C Worker for the aif-handoff-as-runtime-bridge umbrella. Your first action is to set up your own isolated worktree from origin/staging, then perform the R-phase work.

# Self-setup (run as first Bash invocations):
cd /Users/art/code/rules-as-tests-aif
git fetch origin staging
git worktree add ../rules-as-tests-aif-aif-handoff-sw-C origin/staging
cd ../rules-as-tests-aif-aif-handoff-sw-C
git checkout -b research/aif-handoff-bridge-variant-c
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
fi

# Then proceed with R-phase task:
/orchestrator aif-handoff-as-runtime-bridge §6 Sub-wave C — Mode A inline R-phase (Opus per Decision #5): Variant C (minimal bridge, Implementer-equivalent only) value-add audit per umbrella kickoff §6 Sub-wave C. Output: docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-variant-c-minimal.md. AI traps active per §5: T1, T3, T7, T11, T12, T13, T15, T16, T17, T19, T20, T-AIF-BRIDGE-C. BEFORE/AFTER maintainer-action count table MANDATORY. Cite findings from merged SW-A (#268) and SW-B (#267) patches + Stage 1→2 follow-up (#269). DN-1=B-constrained (Variant A is ADAPT-candidate with «aif-handoff Reviewer one-clear-pick/escalate-on-doubt» constraint) — use this in criterion 5 duplication analysis. Gate-4: address proxy-runtime PR #128 + per-profile env PR #127 surface impact on Variant C wrapper design. PR base: staging. §1.7 mandate per §4b applies. **§1.7 CI gate WILL NOT fire** (research-patches/** not in trigger paths); pre-flight grep is the only programmatic check.
```

### Sub-wave D — Synthesis + cross-variant verdict — Stage 3 (after B2 + C merged)

Paste into fresh CC session opened in **primary workdir**:

```
You are dispatched as Sub-wave D Worker for the aif-handoff-as-runtime-bridge umbrella. Your first action is to set up your own isolated worktree from origin/staging, then perform the synthesis R-phase work.

# Self-setup (run as first Bash invocations):
cd /Users/art/code/rules-as-tests-aif
git fetch origin staging
git worktree add ../rules-as-tests-aif-aif-handoff-sw-D origin/staging
cd ../rules-as-tests-aif-aif-handoff-sw-D
git checkout -b research/aif-handoff-bridge-synthesis
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
fi

# Then proceed with synthesis R-phase task:
/orchestrator aif-handoff-as-runtime-bridge §6 Sub-wave D — Mode A inline R-phase: Synthesis + cross-variant verdict per umbrella kickoff §6 Sub-wave D. Output: docs/meta-factory/research-patches/2026-05-29-aif-handoff-bridge-synthesis.md. AI traps active per §5: T1, T3, T7, T11, T12, T13, T15, T16, T17, T19, T20. Must include: **20-cell comparison table (5 criteria × 4 variants: A, B, B', C) with file:line citation per cell**; SSOT additive-note proposals for rows #30/#44/#67; recommended verdict per BFR-default §1 (GREENLIGHT one variant / DEFER all / REJECT all). DN-1 (B-constrained) and DN-2 (refined — Variant B' is the operative «B» pathway, original Variant B's native-fs-watcher remains «long-term ADAPT» with AND-trigger) MUST be reflected in the synthesis recommendation. For ties between variants: DECISION-NEEDED to maintainer per reviewer-discipline.md §2 — do NOT pick. Read all 4 sub-wave patches as input. PR base: staging. §1.7 PR-body mandate per §4b applies.
```

---

## §4b §1.7 PR-body authoring mandate (paths-triggered, applies to ALL Sub-waves above — self-evaluate)

All four sub-waves write to `docs/meta-factory/research-patches/`. Sub-wave D additionally proposes SSOT row additive notes under `docs/meta-factory/prior-art-evaluations.md` (additive only per umbrella §10 backward-check).

**CI backstop scope (Sub-wave-specific) — verified against `.github/workflows/discipline-self-check.yml:11-26`:** the §1.7 PR-body CI gate triggers ONLY on PRs touching `.claude/rules/**`, `packages/core/principles/**`, `docs/meta-factory/EXECUTION-PLAN.md`, `docs/meta-factory/prior-art-evaluations.md`, `CLAUDE.md`, `templates/**/*.md`, `packages/core/templates/**/*.md|*.template.md`.

- **Sub-waves A/B/C** write to `docs/meta-factory/research-patches/**` only — **NOT in trigger paths** → §1.7 CI gate WILL NOT fire on those PRs. §1.7 PR-body content for A/B/C is **prose-discipline only**, no mechanical backstop. The pre-flight grep below is the ONLY programmatic check for A/B/C.
- **Sub-wave D** additionally edits `prior-art-evaluations.md` → CI gate fires. The §1.7 PR-body content for D has both pre-flight grep AND CI backstop.

Workers MUST still produce the §1.7 sections (mandate is process-level; CI is one channel among several per `rule-enforcement-channel-selection.md §1` two-axis principle). The CI-fires-or-not difference affects who catches a missing section, NOT whether the section is required.



**Required PR body sections (verbatim shape — CI substance gate is strict):**

```markdown
### §1.7 Forward-check applied

<≥40 non-whitespace chars enumerating which existing disciplines were checked>

- `build-first-reuse-default.md` — <verdict + rationale per variant>. file:line evidence: `<path>:<line>`
- `phase-research-coverage.md §1` — <6-item search applied to each negative-existence claim>. file:line evidence: `<path>:<line>`
- `no-paid-llm-in-ci.md` — <verdict: probes used DeepWiki + WebSearch + file inspection only>
- `reviewer-discipline.md §2` — <verdict: no project-strategy decision made>
- `ai-laziness-traps.md §3` — <T-enumeration shipped + domain-specific traps active>

### §1.7 Backward-check applied

<≥40 non-whitespace chars enumerating the sweep of existing SSOT rows + rules + kickoffs under this patch's scope>

- `docs/meta-factory/prior-art-evaluations.md` rows #27/#28/#29/#30/#43/#44/#46/#67/#80 — <which were touched and how (additive only)>. file:line evidence: `<path>:<line>`
- <repeat for each touched artefact>
```

**Hard rules (each one has historically failed the gate):**

1. **Heading depth = H3 (`###`), NOT H2 (`##`).** CI regex matches `^### §1\.7 ...` — H2 silently fails.
2. **Word «applied» is required.**
3. **≥40 non-whitespace chars in EACH section body.**
4. **≥1 file:line citation per section** matching regex `[^[:space:]]+\.[a-z]+:[0-9]+`.

**Pre-flight grep — Worker MUST run BEFORE `gh pr create`:**

```bash
echo "$PR_BODY" | grep -cE '^### §1\.7 (Forward|Backward)-check applied'  # must be 2
echo "$PR_BODY" | awk '/^### §1\.7 Forward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | awk '/^### §1\.7 Backward-check applied/{c=1;next} /^###/{c=0} c' | tr -d '[:space:]' | wc -c  # must be ≥40
echo "$PR_BODY" | grep -cE '[^[:space:]]+\.[a-z]+:[0-9]+'  # must be ≥2
```

---

## §4a Worker worktree setup (FIRST step in any worker session — Gap-3 round-2 follow-up)

When dispatched into a `git worktree add`-created directory (Sub-waves A and B parallel, C and D fresh-cut), reconstitute `node_modules` plumbing BEFORE any `npm` / test / build:

```bash
GIT_DIR_REL=$(git rev-parse --git-dir)
GIT_COMMON_DIR=$(git rev-parse --git-common-dir)
if [[ "$GIT_DIR_REL" != "$GIT_COMMON_DIR" ]]; then
  PRIMARY_WORKDIR=$(dirname "$(cd "$GIT_COMMON_DIR" && pwd)")
  [[ ! -e node_modules ]] && ln -sfn "$PRIMARY_WORKDIR/node_modules" node_modules
  [[ ! -e packages/core/node_modules ]] && ln -sfn ../../node_modules packages/core/node_modules
  echo "worktree setup OK — node_modules + packages/core/node_modules linked to primary"
fi
```

Skip in primary workdir — the `if` is a no-op.

---

## §5 AI-traps active (per `.claude/rules/ai-laziness-traps.md §3`)

See `.claude/rules/ai-laziness-traps.md §2` for full catalogue. Per `ai-laziness-traps.md §3` blanket reference WITHOUT enumeration is itself T7.

**Active canonical traps for aif-handoff-as-runtime-bridge (all sub-waves):**

- **T1** — sampling floor ≥5. Each variant evaluation MUST cite ≥5 distinct DeepWiki probes / file inspections / command outputs.
- **T3** — every finding has file:line, command output, or explicit INCONCLUSIVE.
- **T7** — adversarial counter-prompts on negative-existence claims. Especially Variant B «no directory-watch mode» — counter-prompt: «if it had one, where would it live?»
- **T11** — search for prior bridge implementations BEFORE designing our own (oh-my-openagent? ai-factory itself?).
- **T12** — DeepWiki at R-phase time, not training-data recall on `lee-to/aif-handoff` (active upstream; gate-4 confirmed 3 capability commits in last 30 days).
- **T13** — re-verify ADOPTED items. SP `requesting-code-review` already adopted ([SKILL.md:404](../../skills/meta-orchestrator/SKILL.md#L404)) — confirm aif-handoff's Reviewer would not silently override it.
- **T15** — recursive self-application. R-phase patch must include its own §self-application audit.
- **T16** — explicit «Upstream problem class: X. Our problem class: Y. Match? Evidence: …» per variant. The reframed «optional layer below» framing risks repeating SSOT #67 muddied-scope mistake.
- **T17** — destructive delegation. R-phase does NOT delete or modify any meta-orchestrator code. Markdown only.
- **T19** — cold-QA before handoff. Reviewer-session reads each patch independently before PR open.
- **T20** — every verdict cites SSOT row + evidence + falsifier in same sentence.

**Domain-specific traps (aif-handoff-as-runtime-bridge-specific, NOT in canonical catalogue):**

- **T-AIF-BRIDGE-A** — «MCP body-format assumed compatible without inspection» (Sub-wave A scope). Specifically: assuming `handoff_create_task` accepts arbitrary markdown without verifying MCP schema. Counter: inspect `aif-handoff/packages/mcp/schemas/` or DeepWiki probe BEFORE proposing Variant A as bash-only.
- **T-AIF-BRIDGE-B** — «directory-watch mode existence assumed» (Sub-wave B scope). Specifically: assuming aif-handoff has fs-watcher because «modern tools usually do». Counter: explicit DeepWiki probe + GitHub issue search for `inotify` / `fsnotify` / `chokidar` in upstream codebase.
- **T-AIF-BRIDGE-C** — «automation theatre» (Sub-wave C scope). Specifically: declaring «Variant C automates dispatch» when it just adds UI tracking without changing maintainer-action count. Counter: BEFORE/AFTER maintainer-action count table MANDATORY.

> **Anti-pattern warning:** blanket «see ai-laziness-traps.md» without the enumeration above = T7 violation. Each Sub-wave dispatch prompt enumerates the active T-numbers + the relevant domain trap.

---

## §6 Recursive self-application

This kickoff was generated by `/meta-orchestrator` and must itself pass `principle 12` (AI-laziness-traps format validation):

```bash
npm --prefix packages/core run test:principles -- --testPathPattern=12 | tail -10
```

> **NB (round-2 reviewer note):** do NOT redirect stderr to `/dev/null` — a missing-test or malformed-pattern failure would silently look green. Stage 1 dispatcher MUST actually run this and read the output, not trust the kickoff says to.

Sub-wave outputs additionally satisfy §9 of the umbrella kickoff (BFR-default §3 6-layer search applied to themselves; T16 problem-class match per variant; Phase -1 cold-review of EACH Sub-wave patch BEFORE PR open).

---

## §7 Phase -1 reviewer dispatch (between stages — MANDATORY)

### Gate 3 — Phase -1 cold-review of THIS kickoff + umbrella kickoff (BEFORE Sub-wave A dispatch)

Per umbrella kickoff §2 gate 3 — the umbrella's own kickoff (338 LOC, DRAFT) needs a 1× Opus reviewer pass before any sub-wave fires. Dispatch:

```text
Agent: Phase -1 cold-review for aif-handoff-as-runtime-bridge umbrella admission
Role: reviewer (not orchestrator, not implementer)
Skill: requesting-code-review
Inputs:
  - .claude/orchestrator-prompts/aif-handoff-as-runtime-bridge/kickoff.md (umbrella, 338 LOC)
  - .claude/orchestrator-prompts/aif-handoff-as-runtime-bridge-meta-launch/kickoff.md (this meta-kickoff)
Required checks:
  - §1.7 forward+backward walks complete and accurate
  - DEFER-trigger citations in §1 (SSOT #30/#44/#67) are accurate
  - §3 variant analysis is genuinely 3 distinct options (NOT 1 dressed as 3 — T16 risk)
  - §5 5-criteria coverage adequate per ai-laziness-traps T1 (sampling floor ≥5)
  - Gate-4 finding (3 upstream capability commits) is addressed in dispatch prompts §4
Verdict: GO / REVISE / STOP
```

### Stages 1→2, 2→3 — Phase -1 between every stage transition

Same dispatch shape. Reviewer reads merged Stage N diff, verifies sub-wave acceptance criteria from umbrella kickoff §6, surfaces strategy forks as DECISION-NEEDED per reviewer-discipline.md §2 (does NOT pick).

**Verdict routing:**

- **GO** → proceed to §4 dispatch for Stage N+1.
- **REVISE** → surface findings to maintainer; worker fixes; repeat Phase -1.
- **STOP** → escalate to maintainer; halt Stage N+1.

---

## §7a Reporting format (mandatory plain-language tail at orchestrator checkpoints)

<!-- @dual-pair: plain-language-tail -->
<!-- spec: .claude/skills/meta-orchestrator/SKILL.md §10.3a + .claude/hooks/end-of-turn-reminder.sh -->

Any session message produced FROM this kickoff (Worker REPORT, Reviewer GO/REVISE verdict, mid-stage status, final handoff) ends with a `## 🟢 Простыми словами` block whose CONTENT follows the orchestrator-checkpoint substance shape (names sub-waves / AC items / file:line / REPORT-traces / dispatch-state). Distinct from the per-turn generic substance the end-of-turn hook already enforces — does NOT restate personal-reasoning prose («чем я сейчас занят», «на той ли цели»). Skip allowed only for one-line tool acknowledgements and pure `gh`/`git` outputs.

---

## §8 Stop conditions

- Any `gh` command returns error and stage-gate check produces ambiguous output → halt, escalate.
- Stage gate command returns empty AND Stage N was expected merged → halt, report.
- Phase -1 reviewer returns STOP → halt, surface to maintainer.
- Any sub-wave returns BLOCKER finding → halt Stage; surface before proceeding.
- Sub-wave A finds `handoff_create_task` requires structured fields (not free markdown body) → record finding, verdict «Variant A requires upstream feature OR meta-orchestrator output reshape», do NOT propose ADOPT.
- Sub-wave B finds no directory-watch capability AND no upstream PR feasible within reasonable effort → verdict «Variant B = long-term ADAPT».
- Sub-wave C BEFORE/AFTER maintainer-action count shows <30% reduction → verdict «Variant C value-add insufficient».
- Sub-wave D finds 2+ variants tie on §4 criteria → DECISION-NEEDED to maintainer (NOT picked).
- Any temptation to edit `~/.claude/skills/orchestrator/` → T17 / `#operational-doc-redefines-goal`; stop.

---

## §9 Anti-scope

- Do NOT write sub-wave implementation code in this meta-kickoff.
- Do NOT pick a variant verdict (that's Sub-wave D's job; if ties, DECISION-NEEDED to maintainer).
- Do NOT modify `~/.claude/skills/orchestrator/` (agent-uncommittable).
- Do NOT add npm deps (substrate-purity per `no-paid-llm-in-ci.md` + BFR-default).
- Do NOT silently expand scope to include I-phase work — if D recommends GREENLIGHT, the I-phase is a separate umbrella + separate maintainer decision.
- Do NOT modify SSOT rows #30/#44/#67 verdicts — only additive «Trigger to revisit» notes per umbrella §10 backward-check.

---

## §10 See also

- `.claude/orchestrator-prompts/aif-handoff-as-runtime-bridge/kickoff.md` — umbrella kickoff (binding scope).
- `docs/meta-factory/prior-art-evaluations.md` rows #27/#28/#29/#30/#43/#44/#46/#67/#80 — SSOT entries.
- `docs/meta-factory/wave-sequencing-plan.md` — plan-currency source (acknowledged stale; reconciliation deferred).
- `.claude/skills/meta-orchestrator/SKILL.md` — generating skill.
- `.claude/rules/ai-laziness-traps.md §2` — canonical trap catalogue.
- `.claude/rules/parallel-subwave-isolation.md §1` — worktree isolation (Sub-waves A and B mandatory).
- `.claude/rules/reviewer-discipline.md §2` — reviewer role boundaries (D synthesis ties).
- `.claude/rules/build-first-reuse-default.md` — verdict ladder applied in §6 per-variant verdicts.
- `.claude/rules/no-paid-llm-in-ci.md` — hard constraint on all probes.
- `docs/meta-factory/research-patches/2026-05-26-companion-reuse-aif-handoff-autoqueue.md` — predecessor sub-component evaluation (autoQueueMode).
- `docs/meta-factory/research-patches/2026-05-23-meta-orchestrator-prior-art.md` — meta-orchestrator BUILD verdict + SSOT #67 origin.
