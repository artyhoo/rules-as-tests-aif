# Claim-groundedness baseline — 2026-05-21

> Committed snapshot the §13.34 ≥+15pp promotion gate compares against. Inherits authority from [tests/eval/README.md](README.md). Re-run `claim-groundedness-scorer.py` on a future claim-scan-active window and diff against this.

**Population:** operator's local Claude Code transcripts for this project (`~/.claude/projects/-Users-art-code-rules-as-tests-aif/*.jsonl`). **Eval-unaware** — almost entirely *pre*-claim-scan sessions (hook landed 2026-05-21 16:26; the live claim-scan was not active during these). This is therefore the **without-mechanism baseline**.

## Headline (detector v2 — Q-E4 recall+precision regex, STRICT grounding)

| metric | value |
|---|---|
| transcripts scanned | 340 |
| sessions with ≥1 claim turn | 283 |
| claim-bearing turns (C) | 1995 |
| grounded claim turns (G) | 1498 |
| **grounded-rate G/C** | **0.751** |
| ungrounded-claim rate | 0.249 |

By modality (STRICT): numeric-count 1832 @ **0.753** · file:line 396 @ **0.813** · negative-existence 9 @ 0.889.

## Detector v1 → v2 change (validates the Q-E4 fix on real data)

| | v1 (adjacency regex, no cleaning) | v2 (≤2-token regex + cleaning) |
|---|---|---|
| claim-turns detected | 1537 *(337 transcripts, research-patch §6.1 run)* | 1995 *(340 transcripts)* |
| grounded-rate (STRICT) | 0.744 | 0.751 |

- **Recall ↑:** v2's intervening-token regex detects **~+37% more claim-turns** (1537 → 2100 on the no-cleaning `--raw` run) — surfacing natural phrasings v1 missed («6 discipline rules», «11 distinct principles»).
- **Precision ↑:** cleaning drops **~105 false-positive claim-turns** (2100 `--raw` → 1995 cleaned) that sat inside fenced code (drafted prompts), blockquotes, or markdown link targets. neg-existence flags fell 27 → 9 (the dropped ones were quoted-prompt meta-discussion, not real claims).
- **Headroom unchanged:** baseline grounded-rate stays ~0.75 → the hook's marginal-value question is unchanged by the detector fix (the fix improves *which* claims are flagged, not the underlying high baseline).

## How to reproduce

```bash
python3 tests/eval/claim-groundedness-scorer.py --strict        # v2 headline
python3 tests/eval/claim-groundedness-scorer.py --strict --raw  # v2 regex, no cleaning
```

Numbers will drift as more sessions accrue; this snapshot is the fixed comparison point. The clean Q1b A/B (claim-scan-active vs this baseline) is DEFERRED to longitudinal accrual (Q-E1) — there is ~no claim-scan-active data yet.
