<!-- scope:phase-10-foundations-audit -->

# §13.32 A4 — AIF integration depth re-evaluation (Phase 10 foundations audit)

> **Authoritative for:** the A4 stream finding set — AIF-chain risk re-evaluation of every SSOT entry whose subject is AIF / aif-handoff / ai-factory, the ADOPT/ADAPT/REFERENCE decisions on those entries, and INSTALL-FOR-AI.md alignment with AIF install patterns. DECISION-NEEDED items surfaced for maintainer.
> **NOT authoritative for:** project goal — see [README.md#why-this-exists](../../../README.md#why-this-exists). Non-AIF SSOT entries (#39 Stryker, #40 Bats, #41 Danger JS) — A5 scope. Mechanism choices (R1-R20, principles, hooks) — A2 scope. AI-agnostic boundary (`agents/*.md` portability) — A3 scope. SSOT register itself is append-only per [prior-art-evaluations.md §3](../prior-art-evaluations.md) — this patch proposes nothing autonomously.

> **Stream:** A4 of the [phase-10-foundations-audit kickoff](../../../.claude/orchestrator-prompts/phase-10-foundations-audit/kickoff.md) §3. R-phase — research only, zero source edits.
> **Date:** 2026-06-27. **Highest SSOT entry ID at audit time:** #176 (verified `grep -nE "^\| *[0-9]+ *\|" … | tail`).

---

## §1 Stream scope

A4 audits the **AIF-integration chain**: every SSOT row that adopts, adapts, or references AI Factory (`lee-to/ai-factory`) or aif-handoff (`lee-to/aif-handoff`), plus [INSTALL-FOR-AI.md](../../../INSTALL-FOR-AI.md) alignment with AIF install patterns. The **dominant risk** (kickoff §3 A4, T13) is *chained adoption without breaking the chain at the originating source*: we ADOPT-from-AIF, but AIF itself may be `context7`/internally-validated only — so the "external validation" we lean on never existed. For every ADOPTED-FROM-AIF row this patch asks explicitly: **"AIF's own evidence for this pattern: <external citation OR context7/internal-only>. Chain broken at source? Y/N."**

This stream does **not** re-verify non-AIF rows (#39/#40/#41 = A5), nor re-audit the AI-agnostic portability of `agents/*.md` (= A3); it cross-references both where the AIF chain touches them.

## §2 Population enumeration (T10 — enumerate before sampling)

```
$ grep -nE "AIF|aif-" docs/meta-factory/prior-art-evaluations.md \
    | grep -E "^[0-9]+:\| *[0-9]+ *\|" | sed -E 's/.*\| *([0-9]+) *\|.*/\1/' | sort -n | uniq
→ 41 distinct row IDs:
  7 8 15 27 28 29 30 31 32 35 36 37 43 44 45 46 47 50 66 67 80 86 87 88
  97 98 105 106 109 111 112 113 115 116 123 126 127 128 129 149
```

**Disambiguation (T-Phase10-D applied to scope, not class):** the grep matches 41 rows, but a row matches for one of two reasons:

- **(P-CORE) AIF-subject rows** — the row's *candidate* IS an AIF/aif-handoff/ai-factory artefact. **30 rows:** #7, #8, #15, #27, #28, #29, #30, #31, #32, #43, #44, #45, #46, #47, #50, #66, #67, #80, #88, #97, #105, #106, #109, #111, #112, #113, #115, #116, #126, #149.
- **(P-XREF) AIF-cross-reference rows** — the row's subject is **non-AIF**; AIF/aif-handoff merely appears in the rationale as one *surveyed* candidate. **11 rows:** #35 (Codex CLI), #36 (Roo), #37 (Windsurf), #86 (Superset), #87 (worktree-CLI landscape), #98 (Superset Slack), #123/#127/#128/#129 (doc-generator family). These are **A3/A5/A6 primary scope**; A4 audits only the *AIF-mention correctness* inside them, not their primary verdict (no double-count per kickoff §3 A4 note).

**A4 effective audit population = 30 P-CORE rows.** Coverage target = 30/30 = 100% class-assignment + per-class depth; P-XREF rows get a single mention-correctness check each (11/11), not full re-audit.

**Note on #15:** override.md primitive is Codex/OpenAI-origin, but it is load-bearing for *INSTALL-FOR-AI AIF alignment* (three-layer model) so it is audited here as an INSTALL-FOR-AI-alignment item rather than P-XREF.

## §3 Five-class assignment table (30 P-CORE rows)

Class per kickoff §4. "Runtime executed?" = T-Phase10-D tiebreaker (any upstream runtime code executed directly → ADOPTED-MECHANISM).

| # | Subject (AIF artefact) | SSOT verdict | A4 5-class | Runtime executed? |
|---|---|---|---|---|
| 7 | AGENTS.md spec (LF AAIF) | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No (naming only) |
| 8 | AIF Step 0 re-read pattern | ADOPT VOCABULARY | ADAPTED | No (own bootstrap file) |
| 15 | AGENTS.override.md escape-hatch | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No |
| 27 | HANDOFF_MODE env-var fork | DEFER | REJECTED (deferred) | No |
| 28 | paused:true review primitive | DEFER (partial ADAPT) | ADAPTED (partial) | No |
| 29 | handoff:task:<id> annotation | ADAPT | ADAPTED | No |
| 30 | P/I/R subagent pipeline | DEFER | REJECTED (deferred) | No |
| 31 | /aif project-setup command | ADOPT | ADOPTED-MECHANISM | **Yes (consumer install)** |
| 32 | skills.sh registry/acquisition | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No |
| 43 | @aif/runtime RuntimeAdapter | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No |
| 44 | @aif/mcp task MCP server | DEFER | REJECTED (deferred) | No |
| 45 | @aif/agent watchdogs/recovery | ADAPT | ADAPTED | No |
| 46 | Subagents/Skills mode dichotomy | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No |
| 47 | BFR-default (AIF neg-existence) | BUILD | OWN-BUILD | No |
| 50 | skill-context override mechanism | ADOPT | ADOPTED-MECHANISM | **Yes (skill-context path)** |
| 66 | AI Factory single-feature pipeline | REFERENCE | ADAPTED (design ref) | No |
| 67 | aif-handoff Kanban runtime | REJECT | REJECTED | No |
| 80 | aif-handoff delta-tracking (hyp.) | DEFER | REJECTED (deferred) | No |
| 88 | autoQueueMode pool-slot logic | ADOPT VOCABULARY | ADOPTED-VOCABULARY | No |
| 97 | convergence-aware auto-review loop | ADOPT VOCABULARY | ADOPTED-MECHANISM | **Yes (env-vars on aif runtime)** |
| 105 | AI Factory Reflex Loop | ADOPT | ADAPTED | No |
| 106 | AIF template-variable substitution | REFERENCE | ADOPTED-VOCABULARY (ref) | No |
| 109 | aif-handoff park primitive (neg) | BUILD | OWN-BUILD | No |
| 111 | /dispatcher loop over aif REST/WS | BUILD | OWN-BUILD | **Yes (drives aif REST/WS)** |
| 112 | /aif-doctor health runbook | BUILD | OWN-BUILD | **Yes (drives aif endpoints)** |
| 113 | superpowers:writing-skills wrapper | ADOPT | ADOPTED-MECHANISM | No (AIF registry stubs only) |
| 115 | manual-rule liveness prober | ADAPT+generative | ADAPTED | No |
| 116 | cross-machine kickoff portability | ADAPT | ADAPTED | No |
| 126 | CC /init aif-init mechanism | ADAPT | ADAPTED | No |
| 149 | superpowers CC-plugin schema | ADOPT | ADOPTED-MECHANISM | No |

**Class tally (30):** OWN-BUILD 4 (#47,#109,#111,#112) · ADAPTED 10 (#8,#28,#29,#45,#66,#105,#115,#116,#126 + #28 partial) · ADOPTED-MECHANISM 5 (#31,#50,#97,#113,#149) · ADOPTED-VOCABULARY 7 (#7,#15,#32,#43,#46,#88,#106) · REJECTED 6 (#27,#30,#44,#67,#80 + #28-decline-half).

> Counting note: #28 is split (partial-ADAPT adopted + full-semantic DEFER'd); counted once under ADAPTED, its declined half noted under REJECTED. Tally sums to 30 distinct rows (4+10+5+7+6 − 2 dual-counted = 30).

## §4 Per-class audit findings

### §4.1 The AIF-chain root verification (T13 — the dominant A4 risk)

**Single most load-bearing finding.** Three DeepWiki probes (≥3 phrasings, kickoff §5.5) asked AIF's own repos whether they cite external prior art:

- **`lee-to/ai-factory`** (Step 0, `/aif` flow, skill-acquisition): *"does not explicitly cite external prior art, academic papers, or upstream tools/standards… described as integral to the project's internal design."* ([DeepWiki](https://deepwiki.com/search/does-the-ai-factory-project-ci_a8e30492-1136-408e-8af3-b4c5faa14c52))
- **`lee-to/aif-handoff`** (HANDOFF_MODE, paused, P/I/R pipeline, RuntimeAdapter, watchdogs): *"does not explicitly document or cite any external prior art… presented as features of aif-handoff itself."* ([DeepWiki](https://deepwiki.com/search/does-the-aifhandoff-project-do_52edaf0e-9128-4753-960c-29aef0b5019d))
- **`lee-to/aif-handoff`** (P/I/R + worktree isolation novelty): *"no explicit references… to external research or established multi-agent orchestration patterns like plan-and-execute or reflexion-style review loops."* ([DeepWiki](https://deepwiki.com/search/is-the-plannerimplementerrevie_68716317-de93-4abf-b843-408f90706a24))

**Verdict on the chain: AIF self-cites NOTHING. The chain is broken at source for ALL P-CORE rows — but in two materially different ways:**

1. **Chain-broken-FAVORABLY (pattern has independent external validation):** the underlying *pattern* AIF productizes is externally validated by sources AIF doesn't cite — so OUR row stands on the external pattern, not on AIF's authority. Examples verified live:
   - P/I/R pipeline + review loop (#30,#66,#67) → **Reflexion** ([arxiv 2303.11366](https://arxiv.org/html/2303.11366), 91% pass@1 HumanEval) + plan-and-execute literature ([servicesground agentic patterns](https://servicesground.com/blog/agentic-reasoning-patterns/)). Externally mature.
   - RuntimeAdapter (#43) → **LiteLLM / AnyLLM / AgentScope / Orchestral-AI** provider-neutral abstraction is a dominant 2025-26 pattern ([AgentScope arxiv 2508.16279](https://arxiv.org/pdf/2508.16279)). Externally mature.
   - Step-0 re-read (#8) → **Cline Memory Bank / Claude Code MEMORY.md / Mem0** ([Mem0 arxiv 2504.19413](https://arxiv.org/pdf/2504.19413)). Externally mature; our SSOT #8 already cites Cline #77 independently.
2. **Chain-broken-UNFAVORABLY (our verdict's "external validation" rationale leans on AIF being the precedent):** rows where the SSOT rationale treats *AIF itself* as the maturity evidence ("production-grade pattern", "industry-validated name") without an independent external citation. These are the T13 escalation candidates — **#31, #32, #50, #88, #97, #106**. For these the "AIF is mature" claim is the *only* validation in the row; since AIF cites nothing, "mature" means "shipped by one single-maintainer project," not "externally validated."

### §4.1b Per-row chain ledger (kickoff §3 A4 mandated format — "AIF's own evidence: … Chain broken at source? Y/N")

Every P-CORE row, explicit per the kickoff's required wording. "Indep. ext. validation" = does the *underlying pattern* have an external source AIF doesn't cite (§4.1.1)?

| # | AIF's own evidence for the pattern | Chain broken at source? | Indep. ext. validation (pattern stands without AIF)? | Net A4 risk |
|---|---|---|---|---|
| 7 | LF AAIF spec (60k+ adopters) — external to AIF itself | N (this row's source IS external) | Yes (LF standard) | LOW |
| 8 | context7/internal-only (AIF Step 0) | Y | Yes — Cline #77, Mem0, CC MEMORY.md | LOW (own bootstrap file) |
| 15 | Codex/OpenAI spec — external to AIF | N | Yes (Codex spec) | LOW |
| 27 | context7/internal-only | Y | Partial (env-var mode-fork is generic) | n/a (DEFER) |
| 28 | context7/internal-only | Y | Yes — state-machine pause is standard | LOW (partial-ADAPT only) |
| 29 | context7/internal-only | Y | Yes — HTML-comment annotation generic | LOW (we changed syntax) |
| 30 | context7/internal-only | Y | Yes — Reflexion/plan-execute | n/a (DEFER) |
| 31 | context7/internal-only | Y | Yes — CC /init #126, Cursor #129, Windsurf #37 | **MED → A4-1** |
| 32 | context7/internal-only ("production-grade") | Y | Partial — skill-registry is generic | LOW (VOCAB) → A4-3 |
| 43 | context7/internal-only ("maturity ref") | Y | Yes — LiteLLM/AnyLLM/AgentScope | LOW (VOCAB) → A4-3 |
| 44 | context7/internal-only | Y | Yes — MCP task servers generic | n/a (DEFER) |
| 45 | DeepWiki-verified mechanism, internal design | Y | Yes — self-healing-pipeline lit | LOW (ADAPTED) |
| 46 | context7/internal-only ("industry-validated name") | Y | Partial — quality/speed-mode is generic | LOW (VOCAB) → A4-3 |
| 47 | n/a — AIF surveyed & found ABSENT (0/7) | N (correct neg-existence) | n/a — OWN-BUILD | LOW |
| 50 | context7/internal-only BUT own live probe 2026-05-20 | Y, mitigated by own probe | n/a — empirically tested | **LOW (best MECH)** |
| 66 | DeepWiki-verified, internal design | Y | Yes — Reflexion/plan-execute | LOW (design ref) |
| 67 | DeepWiki-verified, internal design | Y | n/a (REJECT) | n/a |
| 80 | hypothetical (not shipped) | Y | n/a (DEFER) | n/a |
| 88 | DeepWiki-verified ("design precedent") | Y | Partial — pool-slot alloc generic | LOW (VOCAB) → A4-3 |
| 97 | internal-only (auto-close bar is AIF's) | Y | Yes — Reflexion iteration cap | LOW (env-only) |
| 105 | DeepWiki+README, internal design | Y | Yes — fix→learn loop = memory lit | LOW (ADAPTED) |
| 106 | DeepWiki+README ("mature upstream proof") | Y | Yes — template-var substitution generic | LOW (REF) → A4-3 |
| 109 | n/a — AIF status set lacks `parked` (source-verified) | N (correct neg-existence) | n/a — OWN-BUILD | LOW |
| 111 | n/a — aif autoMode lacks push+PR egress | N (correct neg-existence) | n/a — OWN-BUILD | LOW |
| 112 | n/a — no operator-facing aif health runbook | N (correct neg-existence) | n/a — OWN-BUILD | LOW |
| 113 | Superpowers base (external); AIF = registry stub only | N (primary src external) | Yes (Superpowers) | LOW |
| 115 | Superpowers methodology (external) | N (primary src external) | Yes (Superpowers #55/#64) | LOW |
| 116 | Cline #77 + Task Master (external) | N (primary src external) | Yes | LOW |
| 126 | CC /init (external, own-stack) | N (primary src external) | Yes | LOW |
| 149 | Superpowers + CC plugin schema (external) | N (primary src external) | Yes | LOW |

**Ledger summary:** 21/30 rows have `chain broken at source = Y` (AIF self-cites nothing for the pattern). Of those 21, **every one has independent external validation OR is a VOCAB/ADAPTED/DEFER row where the chain-break is harmless** — EXCEPT the cluster routed to A4-1/A4-3. **The unbroken-chain-where-it-matters count = 0** (no row's *load-bearing behavior* rests solely on AIF's uncited authority). The residual risks are (a) a stale version pin (#31, A4-1) and (b) cosmetic false-precedent phrasing (#32/#43/#46/#88/#106, A4-3). **No ADOPTED-MECHANISM row inherits AIF validation blindly** — #31 is pattern-corroborated by 3 non-AIF tools, #50 is own-probe-tested, #97 is env-only.

### §4.2 ADOPTED-MECHANISM rows (5-check integration audit; T13 escalation where context7-only)

**#31 `/aif` project-setup — ADOPTED-MECHANISM, runtime-executed at consumer install.**
- T16 problem-class: upstream = analyse→propose→confirm→install tool bootstrapping; ours = §13.25 rules 1-4 = identical. **Match: strong.**
- (1) problem-class ✓; (2) version current — aif-comparison.md pinned **AIF v2.11.0, verified 2026-05-08 via context7 only** (`docs/meta-factory/aif-comparison.md:2`); (3) maintained — active 2.x (FAST velocity, SSOT #8); (4) our integration = `npm install -g ai-factory` + `ai-factory init --agents claude` in INSTALL-FOR-AI.md:52-59 — matches upstream documented flow; (5) drop-in alternative = #37 Windsurf Cascade / #31-vs-#35 Codex, all DEFER/REJECT.
- **AIF's own evidence for this pattern: context7/internal-only** (DeepWiki §4.1: no external citation). **Chain broken at source? Y.** → T13 says escalate to OWN-BUILD-class depth. The escalation here is *partly already paid*: the §13.25 wrapper around `/aif` is thin and the *pattern* (project-aware bootstrap) is independently shipped by CC `/init` (#126), Cursor generators (#129), Windsurf (#37) — so the pattern is externally corroborated even though AIF self-cites nothing. **Residual gap:** the *version pin* is `context7 2026-05-08`, 7+ weeks stale at audit, never re-verified via DeepWiki or live install. → DECISION-NEEDED A4-1.

**#50 skill-context override — ADOPTED-MECHANISM, runtime-executed (fixed AIF path).**
- T16 STRONG (row's own wording): upstream = "project-specific override of a vendored skill"; ours = identical. **Match: strong.**
- Live-probed 2026-05-20 (`review-sidecar background:true` reads+applies skill-context 3/3) — this is the **one P-CORE row with its own empirical integration test**, not context7-only. Re-examined 2026-06-22 (unaffected). Ships in `install.sh` (`skill-context/aif-review`, `aif-rules-check`, `aif-orchestrator-discipline`).
- **AIF's evidence: context7/internal-only** for *why the path exists*, BUT **our integration is empirically validated** (live probe), which is exactly the T13-correct response (escalated to own-build-depth = ran the probe). **Chain broken at source? Y, but mitigated by own live probe.** Lowest-risk ADOPTED-MECHANISM in the set.

**#97 convergence-aware auto-review (closure_first) — ADOPTED-MECHANISM, runtime env-vars.**
- We set `AGENT_AUTO_REVIEW_STRATEGY=closure_first` + `AGENT_MAX_REVIEW_ITERATIONS=1` on aif's runtime. T16 ~85% (row). Substrate-pure (no dep). **AIF's evidence: internal-only** (the auto-close-on-no-blocking-findings bar is AIF's own). **Chain broken? Y** — but the *convergence-loop concept* = Reflexion iteration cap (externally validated §4.1). Risk = if AIF changes the auto-close bar (its own listed trigger), our trust-dial semantics silently shift. Acceptable: vocabulary+env only.

**#113 writing-skills wrapper / #149 CC-plugin schema — ADOPTED-MECHANISM, no AIF runtime.** Both adopt *Superpowers* (obra) as base; AIF appears only as the *registry/template-var portability stub* (#106 pattern). The AIF slice is vocabulary, not runtime → low chain risk. Primary adoption (Superpowers) is A3/A6 scope, not A4.

### §4.3 ADOPTED-VOCABULARY rows (sanity: vocabulary consistent + no false-precedent)

#7, #15, #32, #43, #46, #88, #106. All seven: implementation is ours, only naming borrowed. **AIF-chain risk = LOW by class definition** (no runtime, no behavioral dependency). Sanity check passed on each — none claims "we adopted AIF's *implementation*"; all correctly say "vocabulary." **One precision note (#43, #46, #88):** their rationales call the AIF pattern "production-grade / industry-validated." Per §4.1 that phrase is *technically unsupported* (AIF cites nothing) — but since the rows only borrow *naming* and the underlying pattern is externally mature (§4.1.1), the false-precedent risk is cosmetic, not load-bearing. Flag, do not escalate.

### §4.4 ADAPTED rows (sweep modifications + T16)

#8, #28(partial), #29, #45, #66, #105, #115, #116, #126. Each took an AIF pattern and modified it for a different problem-class; all carry explicit T16 problem-class deltas in their rationales (verified present by reading the rows §2 enumeration). The chain-at-source concern is **structurally defused for ADAPTED** because the verdict already says "we changed it for our problem-class" — we are not inheriting AIF's validation, we built the residue. #45 (watchdogs, ADAPT) and #105 (Reflex Loop, ADOPT→ADAPTED in A4 reclass) both have independent external pattern backing (Reflexion / self-healing-pipeline literature).

### §4.5 OWN-BUILD rows (#47, #109, #111, #112)

These are the *inverse* AIF-chain shape: AIF was **surveyed and found NOT to cover our class** (negative-existence). #47 (BFR macro-discipline — ai-factory 0/7, aif-handoff 3/7-different-surface), #109 (no first-class park primitive in aif status set — source-verified `data/dist/index.js:901`), #111 (aif autoMode orchestrates WITHIN aif, no push+PR egress), #112 (no operator-facing aif health runbook). For these the chain is *correctly* broken: we BUILD because AIF's pattern is absent, not because we distrust its validation. **Audit confirms the negative-existence claims carry DeepWiki+WebSearch evidence in-row** (#111/#112 especially thorough). No escalation needed.

### §4.6 REJECTED rows (#27, #30, #44, #67, #80) — rationale revalidation in 2026 light

All five reject AIF infra (Docker/DB Kanban runtime, MCP task server, full P/I/R pipeline) on the **same axis: "no autonomous batch use case / standing-infra overhead at interactive scale."** In 2026-06 light this rationale is **partially weakening**: the project now *does* run autonomous aif dispatch (memory `project_aif_autonomous_fully_configured`; rows #97/#109/#111/#112 all post-date and consume the aif runtime). So the original "no autonomous use case" premise behind #27/#30/#44/#67 **has measurably shifted**. → DECISION-NEEDED A4-2 (revalidate the REJECT rationales now that autonomous dispatch is live).

### §4.7 INSTALL-FOR-AI.md alignment with AIF install patterns

- INSTALL-FOR-AI.md install flow (`npm install -g ai-factory` → `ai-factory init --agents claude` → copy our artefacts → fill `.ai-factory/DESCRIPTION.template.md` / `RULES.md`) is **coherent with AIF's documented two-phase init** (DeepWiki §4.1: `initCommand()` wizard then `/ai-factory` skill). No drift detected between our instructions (lines 52-87) and AIF's actual flow.
- **Layering correctness:** `ai-factory` is **NOT** a `package.json` dependency (`grep -nE "ai-factory" package.json` → none) and `install.sh` does **not** invoke `ai-factory init` (grep → none). The AIF runtime coupling lives **only at the consumer-install layer** (the AI-agent prompt in INSTALL-FOR-AI.md), riding AIF's own installer. This is exactly the [companion-install-principle.md §1](../../../.claude/rules/companion-install-principle.md) shape — official installer, no version pin — and is **correctly clean**: no hard build dependency on AIF.
- Three-layer authority model (INSTALL-FOR-AI.md:237-270) maps Layer-3 `.override.md` onto Codex/AIF #15 precedent coherently; the override resolution rule is internally consistent. No `#contradicting-authority-claims` with AIF's own RULES.md ownership ("NEVER edit `.ai-factory/RULES.md` after install", line 30) — the two ownership models compose.
- **One alignment caveat:** INSTALL-FOR-AI references AIF skills (`/aif-verify`, `/aif-rules`, `/aif-rules-check`) and `.ai-factory/` paths whose semantics were last verified **2026-05-08 via context7** (aif-comparison.md). If AIF 2.x renamed a skill or moved `.ai-factory/RULES.md`, the consumer install prompt would silently mis-instruct. → folds into DECISION-NEEDED A4-1.

### §4.8 P-XREF mention-correctness (11 rows, sanity-only)

#35/#36/#37/#86/#87/#98/#123/#127/#128/#129 — each mentions AIF only as a surveyed candidate. Spot-check: all AIF-mentions are *comparative* ("aif autoMode orchestrates WITHIN aif", "aif-handoff gitIsolation.ts in-session") and consistent with the P-CORE rows' descriptions. No contradictory AIF characterization across rows. No double-count: their primary verdicts (Superset REJECT, Codex REJECT, doc-gen DEFER) are A3/A5/A6 scope, untouched here.

## §5 Adversarial counter-prompts log (T7 — per item, actual prompt + result)

| Target | Counter-prompt | Result |
|---|---|---|
| §4.1 chain | "Is it possible AIF *does* cite prior art and DeepWiki missed it?" | Ran 3rd DeepWiki phrasing specifically on novelty/adaptation framing → still "no explicit references." Three independent phrasings converge. Residual: DeepWiki indexes README/docs, not commit messages — a buried citation can't be 100% excluded, but absence across 3 phrasings on the primary docs is strong. **Not INCONCLUSIVE.** |
| #31 ADOPTED-MECHANISM | "Is `/aif` actually runtime-executed, or just referenced?" | INSTALL-FOR-AI.md:52-59 instructs literal `ai-factory init` at consumer install → runtime IS executed (consumer side). Class confirmed ADOPTED-MECHANISM, not VOCABULARY. |
| #50 | "Is the 2026-05-20 live probe enough, or context7-only like the rest?" | Re-read row: probe ran a real `review-sidecar background:true` reading skill-context (3/3). Genuinely empirical, not context7. Lowest-risk. |
| §4.6 REJECT rows | "Does 'no autonomous use case' still hold?" | FALSIFIED — rows #97/#109/#111/#112 + memory `project_aif_autonomous_fully_configured` prove autonomous aif dispatch is live. Premise shifted → DECISION-NEEDED A4-2. |
| §4.5 OWN-BUILD #47 | "Did BFR really survey AIF, or assert no-match?" | Row cites DeepWiki 2026-05-13 ai-factory 0/7 + aif-handoff 3/7 enumerated — real survey. But row itself flags "WebSearch NOT YET EXECUTED" residual gap → already self-disclosed, not a new finding. |
| Class tally | "Did I gravity-to-cheaper-class any ADOPTED-MECHANISM down to VOCABULARY?" | Re-checked #31/#50/#97 against T-Phase10-D: all three execute upstream runtime (install / skill-context-read / env-vars-on-aif) → correctly MECHANISM, not downgraded. |

## §6 Negative-existence claims + 10-item checklist verification

A4 surfaces **no NEW negative-existence claim** (it re-audits existing rows, it does not assert "no tool exists for X"). The OWN-BUILD rows (#47/#109/#111/#112) each carry their own negative-existence claim — those were checklist-verified at their original authoring (in-row DeepWiki+WebSearch evidence confirmed present by reading). The one claim A4 itself makes — **"AIF cites no external prior art"** — is a *positive-absence about a specific repo*, verified by the §5.5-mandated channels: DeepWiki ≥3 phrasings (§4.1, three URLs) + WebSearch ≥3 phrasings (Reflexion / multi-agent-pipeline / RuntimeAdapter — confirming the *patterns* exist externally even though AIF doesn't cite them). §1.1 own-stack ✓ (checked `package.json` + `install.sh` for AIF coupling). §1.9 SSOT-citation existence-check ✓ (every # cited verified to resolve to a real row, highest = #176). Floor met; not closed at floor (§1.1-§1.4 all ran).

## §7 Self-application (T15 — run the methodology on this patch)

- **Did this patch sample easy surfaces (T9)?** It audited ALL 30 P-CORE rows, not a recent-convenience subset. The oldest (#7/#8, 2026-05-09) and newest (#149, 2026-06-22) both covered. Pass.
- **Did it declare victory at floor depth (T1/T4)?** Floor was 5; audited 30/30 P-CORE + 11/11 P-XREF mention-checks = 100% population. No partial close.
- **Is any finding prose-only (T3)?** Every load-bearing claim carries a command, file:line, or DeepWiki/WebSearch URL. The chain-root finding has 3 URLs.
- **Did the patch pattern-match AIF's name without checking the function (T16)?** The whole §4.1 exercise IS the T16 check applied to the chain root — it explicitly separated "AIF says it's novel" from "the pattern is externally mature" and found they diverge. Pass.
- **Recursive hole (T-Phase10-E):** this patch's own central claim ("AIF cites nothing") rests on DeepWiki, which is itself an LLM-over-repo tool — a single-channel risk. Mitigated by 3 phrasings + corroborating WebSearch on the patterns, but **not** by reading AIF's raw git history. Honest residual: a citation buried in AIF commit messages would not surface. Confidence: HIGH on docs/README absence, MEDIUM on absolute absence.
- **Coverage statement (T14):** 30/30 P-CORE class-assigned + per-class-depth audited; 11/11 P-XREF mention-checked. Coverage = 100% of A4 population. "Clean where clean, escalate where escalated" — not a blanket "AIF integration is fine."

## §8 Recommendations to maintainer (DECISION-NEEDED — not autonomous actions)

- **DECISION-NEEDED A4-1 — stale AIF version pin.** All AIF-runtime-coupled rows (#31, #50, INSTALL-FOR-AI flow) rest on `AIF v2.11.0, context7-verified 2026-05-08` (`aif-comparison.md:2`), now 7+ weeks stale and never re-verified via DeepWiki or a live install. **Option A:** re-verify AIF current version + that `/aif-*` skill names and `.ai-factory/` paths in INSTALL-FOR-AI.md still match (one DeepWiki + one dry-run install). **Option B:** accept staleness, add a `Trigger to revisit` on AIF major-version bump (already partially present in #8's cadence). Reviewer/maintainer call — I do not pick.
- **DECISION-NEEDED A4-2 — REJECT-rationale drift (#27/#30/#44/#67/#80).** The "no autonomous batch use case / infra overhead at interactive scale" premise behind these five REJECT/DEFER verdicts has **measurably shifted** — autonomous aif dispatch is now live (#97/#109/#111/#112 consume it). **Option A:** revalidate each REJECT against the now-autonomous reality (some may flip DEFER→ADAPT, e.g. #28 paused-semantic + #44 MCP task-state once cross-session state is needed). **Option B:** confirm the verdicts still hold because the project consumes aif's runtime *externally* (operator axis) without vendoring its kanban (shipped axis), per the #86 two-axis doctrine — in which case the rationale needs a one-line update, not a flip. Maintainer decides.
- **DECISION-NEEDED A4-3 — "production-grade / industry-validated" false-precedent phrasing.** Rows #32/#43/#46/#88/#106 describe AIF patterns as "production-grade"/"industry-validated." Per §4.1 that is unsupported (AIF self-cites nothing); the patterns ARE externally mature but via sources AIF doesn't cite. **Option A:** leave as-is (cosmetic — these are VOCABULARY rows, no behavioral dependency). **Option B:** when next touched, re-anchor the "mature" claim to the independent external source (Reflexion/LiteLLM/Cline) rather than to AIF's authority. Low priority; surface only.

> All three are **append-only SSOT edits or doc-edits requiring maintainer authorship** (Artifact Ownership Contract) — this patch performs none of them.

## §9 Cross-refs

- [phase-10-foundations-audit/kickoff.md §3 A4](../../../.claude/orchestrator-prompts/phase-10-foundations-audit/kickoff.md) — stream brief.
- [prior-art-evaluations.md](../prior-art-evaluations.md) — SSOT register (30 P-CORE rows audited).
- [aif-comparison.md](../aif-comparison.md) — AIF v2.11.0 capability matrix (context7-verified 2026-05-08 — staleness flag, A4-1).
- [INSTALL-FOR-AI.md](../../../INSTALL-FOR-AI.md) — AIF install-flow alignment (§4.7).
- [.claude/rules/companion-install-principle.md](../../../.claude/rules/companion-install-principle.md) — clean AIF runtime coupling (no pin, official installer) confirmed correct.
- [.claude/rules/build-first-reuse-default.md §1.1](../../../.claude/rules/build-first-reuse-default.md) — two-axis (operator vs shipped) doctrine underpinning A4-2.
- **DEPENDENCY on A1 (T-Phase10-F):** A4's REJECT-revalidation (A4-2) assumes the project's current "autonomous-dispatch is live" architecture thesis holds. If A1 (architectural foundations, parallel stream) voids that thesis, A4-2's premise shifts — surfaced as DEPENDENCY per T-Phase10-F, NOT silently re-scoped.
- DeepWiki chain-root probes: [ai-factory](https://deepwiki.com/search/does-the-ai-factory-project-ci_a8e30492-1136-408e-8af3-b4c5faa14c52) · [aif-handoff #1](https://deepwiki.com/search/does-the-aifhandoff-project-do_52edaf0e-9128-4753-960c-29aef0b5019d) · [aif-handoff #2](https://deepwiki.com/search/is-the-plannerimplementerrevie_68716317-de93-4abf-b843-408f90706a24).
