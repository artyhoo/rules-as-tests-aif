<!-- scope:§13.21 -->
# 2026-05-09 §13.21 L3 closure plan — self-review (Wave 0.5)

> Scope: walks [`2026-05-09-§13.21-l3-revision.md`](2026-05-09-§13.21-l3-revision.md) through [`phase-research-coverage.md §1.7`](../../../.claude/rules/phase-research-coverage.md) — forward (6 layers) + backward (6 steps) — and through [§2 self-reflection prompts (§2.1-§2.5)](../../../.claude/rules/phase-research-coverage.md). Companion to the revision patch; demonstrates the revised plan would pass §1.7 had §1.7 been applied at v1 draft time. Folder-level authority inherited from [research-patches/README.md](README.md); per-file Authoritative-for header not required.

## Problem

Revised L3 plan introduces / extends discipline (Wave 2 widens `principle 09` canonical-list surface; Wave 3 widens `install.sh` enforcement; Wave 4 introduces `<file>.override.md` consumer-side convention). Per [`§1.7` self-reflexive trigger](../../../.claude/rules/phase-research-coverage.md), every new discipline-bearing artefact ships with a self-review patch under [research-patches/](.) following the [bootstrap-exemplar template](2026-05-09-recommendation-skips-own-discipline.md) — walks the proposed change through §1.1-§1.7 + §2.1-§2.5, verifying it would have caught the gap that motivated its creation.

This file is that patch.

## Root Cause

v1 of the L3 plan was drafted without §1.7 application (despite §1.7 being active on `main` at draft time via PR #19). 7 reviewer findings surfaced in REVISE verdict — the revision patch closes them. The remaining question — does the revised plan itself pass §1.7? — is what this self-review answers.

## Forward-check applied

Walks revised plan against the 6 layers in §1.7 (full enumeration: [forward-checklist.md](../../../.claude/skills/self-reflection/references/forward-checklist.md)):

| Layer | Applied to revised plan | Result |
|---|---|---|
| 1. Code-level (R1-R20) | TS sketches in revision §5 (exemption tests) follow `principle 09` style — strict types, no `any`, vitest pattern, mirrors lines 124-149 positive+mutation shape. ESLint + dep-cruiser N/A at plan-stage; validate at Wave 2 commit. | **PASS (provisional pending Wave 2 commit run)** |
| 2. Principle-level (01-09) | Revision §7 principle 09 extension verified against current canonical list at [`09-doc-authority-hierarchy.test.ts:45-87`](../../../packages/core/principles/09-doc-authority-hierarchy.test.ts) — 11 new entries, **no duplicates** with `skills/rules-as-tests/*` (lines 82-87). Length sentinel (line 156): current **33** + 11 = 44, within `≤60` bound. (Verified by counting array entries between brackets at lines 45-88; reviewer's earlier «34» claim from `grep -cE "^\s*'[^']+',?\s*$"` was a false positive on line 162's test assertion.) Principle 08 capability-commit classification: Wave 0.5 commits are prose-only under existing `docs/meta-factory/research-patches/` directory → not capability commits → escape-hatch trailer per [CLAUDE.md `What is a capability commit`](../../../CLAUDE.md). | **PASS** |
| 3. Commit-level (capability-commit gate) | Wave 0.5 = multiple commits across reviewer cycles (run `git log babad0c..HEAD --oneline` for current SHAs and count — declarative ref avoids H8 sub-case (b) lag from hard-coded enumeration here). All prose-only, all under existing `docs/meta-factory/research-patches/` (no new subdirectory under `packages/core/`), 0 LOC under `packages/`. → NON-capability commits. Escape-hatch trailer required per CLAUDE.md on each: `Prior-art: skipped — <reason ≥20 chars>`. Wave 1-4 capability-commit classification deferred to each Wave's commit time. | **PASS** |
| 4. Build-vs-reuse SSOT | Revision §11-§15 blueprints register the 5 load-bearing precedents v1 left unregistered (ESLint extends, Tailwind presets, Cursor 4-tier, Continue Hub `uses:`, AGENTS.override.md). Each carries Verdict + Rationale (≤500 chars) + Trigger + Velocity per [`prior-art-evaluations.md §3`](../prior-art-evaluations.md) schema. Final wording lands in same commit as Wave 2 (principle 09 extension) per `§3` step 1 «add new entry in same commit as capability artifact». context7 query (≥3 phrasings) on the 5 precedents was performed during v1 (re-cited in revision blueprints); not re-run for revision since blueprints are vocabulary-adoption only, not new dep evaluation. | **PASS** |
| 5. Trigger sweep (§1.6) | Revision §14 classifies all 8 in-range entries §13.16-§13.23 explicitly (§13.16 / §13.17 / §13.18 / §13.19 / §13.20 STILL ARMED no cascade; §13.21 + §13.23 **FIRED-by-this-plan**; §13.22 STILL ARMED + **CASCADE-PARTIAL** via velocity tags on new SSOT entries) plus §13.1-§13.14 collectively as STILL ARMED no cascade (Phase 1-8 surfaces — granularity / marketing / multi-stack / AIF / LLM v2 / versioning / etc., none reference doc-authority / templates / install.sh). `grep -nE "^### 13\." docs/meta-factory/open-questions.md` re-runs at Wave 0.5 reviewer cycle for any post-§13.23 entries that may have landed concurrently. | **PASS** |
| 6. Doc-authority on artefacts produced | Wave 0.5's two artefacts (this file + revision file) live under `docs/meta-factory/research-patches/` — folder authority covers them per [research-patches/README.md](README.md) and [`doc-authority-hierarchy.md §5`](../../../.claude/rules/doc-authority-hierarchy.md); no per-file Authoritative-for header required. Both files **exist** post-commit (verified by `ls docs/meta-factory/research-patches/2026-05-09-§13.21-*` in commit-verify step). Cross-doc links resolve to real files (no «Research A — separate session, in flight» phantom-reference of v1 vintage). | **PASS** |

Layer 7 (User-value goal lens, supplementary per [forward-checklist.md](../../../.claude/skills/self-reflection/references/forward-checklist.md) full enumeration): every new rule (Wave 2 principle 09 extension) has executable test (vitest); every claimed enforcement (Wave 3 install.sh hook) has matching code in same Wave; no prose-only-with-no-enforcement drift. **PASS supplementary**.

**Forward result: 6/6 PASS.**

## Backward-check applied

Walks revised plan against the 6 steps in [backward-checklist.md](../../../.claude/skills/self-reflection/references/backward-checklist.md):

| Step | Applied to revised plan | Result |
|---|---|---|
| 1. Determine new rule's scope | Path globs for revised principle 09 surface: `packages/core/templates/shared/*.{md,md.template}`, `packages/preset-next-15-canonical/{RULES*.md, templates/ARCHITECTURE*.md}`, `agents/*.md`. Exclude: `*.json`, `*.sh`, `tsconfig*`, `*.config.*` (non-prose). Documented as path predicates, not vague scopes. | **PASS** |
| 2. Complete sweep (not §1.5 floor) | `ls packages/core/templates/shared/` → 5 prose entries (AGENTS.md.template, CLAUDE.md.template, DESCRIPTION.template.md, ARCHITECTURE.ts-server.md, integration-rules.md). `ls packages/preset-next-15-canonical/{,templates/}` → 3 prose entries (RULES.md, RULES.react-next.md, templates/ARCHITECTURE.react-next.md). `ls agents/` → 3 entries (best-practices-sidecar.md, review-sidecar.md, **docs-auditor.md** — verified existing via `git log --all --diff-filter=A`: present since initial commit `c12a892`/2026-05-06 across `main` and feature branches; handover note's absence claim reflects writer's transient working-tree state, not repo state). Total **11**. Matches revision §7 add-list count exactly; **no proposed-but-missing files** (was the v1 finding #1 + finding #2 failure mode). CLAUDE.md.template included in list despite not currently shipped by install.sh — flagged with 6-month removal trigger. | **PASS** |
| 3. Exemption mechanism explicit | Path glob: `packages/*/research/fixtures/**` + `packages/*/fixtures/**`. Specified in revision §«Wave 2». Sentinel-marker alternative (`<!-- fixture: with-drift -->`) declined per v1 §10 q.3 default (path glob; matches existing fixture layout). Exemption documented in rule body — Wave 2 commit includes `EXEMPT_PATTERNS` constant + comment explaining glob choice. | **PASS** |
| 4. Exemption meta-test (positive + mutation) | Code sketches in revision §5: positive (`expect(isExempt(fixturePath)).toBe(true)`) + mutation (`expect(hasAuthorityHeader(content)).toBe(false)` — confirms exemption is the only reason fixture passes). Mirrors existing principle 09 positive+mutation pair (lines 124-149) — same shape, same naming conventions. | **PASS** |
| 5. Cross-check against existing canonical lists | Diff revision §7 add-list vs current `REQUIRED_HEADER_DOCS` at lines 45-87: **no duplicates** (was the v1 finding #2 part B failure mode where original plan re-added 6 `skills/rules-as-tests/*` entries already present at lines 82-87; revision corrects the duplication). Length sentinel: 33 + 11 = 44, within `expect(...).toBeLessThanOrEqual(60)` line 156 (33 = manual count of array entries lines 45-88; not the imprecise grep value). Canonical roots (README.md, CLAUDE.md, .claude/session-bootstrap.md, EXECUTION-PLAN.md) preserved per lines 158-163. | **PASS** |
| 6. Reverse-cross-check (does new rule break existing artefacts unintentionally?) | For each of the 11 new path-list entries: would the file pass principle 09 *today* if Wave 1 hadn't run yet? **No** — none of the 11 currently carry `Authoritative-for` header (verified by `grep -L '^> \*\*Authoritative for:\*\*' <11 files>` at revision-draft time). **This is the intended breakage** — Wave 1 lands headers, Wave 2 lands canonical-list extension; if Wave 2 lands first, principle 09 fails-load. **Order constraint: Wave 1 must merge before Wave 2** — documented in revision §«Wave 1-4 deliverables breakdown». No collateral breakage on artefacts outside the new scope. | **PASS** |

**Backward result: 6/6 PASS.**

## §2 self-reflection prompts

| # | Prompt | Applied to v1 → revision evolution |
|---|---|---|
| 2.1 | **Когда ошибся — почему?** | Closing v1 `/tmp/generated-docs-discipline-research.md` §1 + §7 — frame loaded subject domain (doc-authority discipline) + prior art (5 industry precedents) + trade-offs (5 update mechanisms in §4 matrix); did **not** load «verify file paths exist via `ls`» / «diff §7 add-list against current canonical list» / «check SSOT for cited precedents» / «sweep §13.x for cascades». §0 anti-pattern self-audit caught defer-until-pain framing but missed the 7 listed findings. **Cognitive shortcut: pattern-match on plan-shape (familiar from prior research), not on §1.7-shape (active rule whose existence the draftsman knew about).** |
| 2.2 | **Мог ли пропускать раньше?** | Yes — same shape in PR #16 (EXECUTION-PLAN §1 silent goal-redefinition), 4-turn defer-until-pain session, and v1 of this plan. Three documented occurrences pre-§1.7; this revision is the 4th, post-§1.7. **§1.7 was active at v1 draft time but not consulted** — the failure mode shifts from «discipline doesn't exist» to «discipline exists but isn't loaded». Calibration: systemic, not one-off. |
| 2.3 | **Как не пропускать?** | Maps to §1.7 (already established). Two candidate observations surfaced during this work: `#candidate-h7-skill-consumption-blindness` (skill auto-trigger keywords matched but skill not engaged at draft time — sample H7 below) and `#candidate-h8-discipline-application-scope-blindness` (§1.7 applied to primary content but not to meta-commentary / self-claims / accepted collaborator claims — sample H8 reached threshold during this very Wave 0.5 work). Both pending §4 distillation in next-session work; current sample-size details in their respective candidate sections below. |
| 2.4 | **Какой урок?** | «§1.7 existence is necessary but not sufficient — auto-trigger description is a hint, not a hook. Before closing any L3-style plan touching ≥2 surfaces (templates + principles + install.sh + INSTALL-FOR-AI), explicitly walk §1.7 forward 6 layers + backward 6 steps against the plan with `find`/`grep`/`ls` evidence per layer — not against memory of project state.» Operationalisable; matches the «before X, also check Y» form per [`phase-research-coverage.md §3`](../../../.claude/rules/phase-research-coverage.md). |
| 2.5 | **Did the principle apply to its own design choices?** | **Yes — this self-review patch is the §2.5 application.** Revised plan is itself a discipline-bearing artefact (Wave 2 widens principle 09; Wave 3 widens install.sh enforcement; Wave 4 introduces `<file>.override.md` convention). §1.7 self-reflexive trigger demands self-review patch — this file is that patch. Recursive closure: §1.7 walks the plan that walks §1.7. |

## §2.5 candidate observation: H7 «skill consumption blindness»

**Pattern:** project-internal skill (e.g. [.claude/skills/self-reflection/](../../../.claude/skills/self-reflection/SKILL.md)) carries auto-trigger description that matches the current drafting context — keyword overlap is exact, scope overlap is exact — yet the skill is **not engaged** during drafting. The skill ships and lives, but the draftsman's attention frame doesn't load skill descriptions; auto-trigger requires the harness or operator to surface the skill, and absent that lift, the skill is invisible to the drafting session.

**Distinction from `#recommendation-skips-own-discipline` (parent):** parent says «discipline isn't applied to its own act of formation». H7 candidate says «discipline *is* materialised as a skill, *is* loadable, *has* matching keywords — yet doesn't load». Mechanism difference: parent is meta-cognitive (no representation of self-as-object); H7 is consumption-side (representation exists, retrieval fails).

**Distinction from `#recursive-self-application-gap` (sibling):** sibling is bottom-up vs top-down at the discipline-layer. H7 is at the skill-layer — a finer-grained mechanism within the broader pattern.

**Status:** 1/3 occurrences. Logging here; if a 2nd + 3rd occurrence surface in subsequent sessions, distill into [`phase-research-coverage.md §4` anti-pattern catalog](../../../.claude/rules/phase-research-coverage.md) per the standard 3-patch threshold (mirrors AIF `/aif-evolve`).

**Mitigation if promoted:** before drafting any plan/recommendation that introduces or extends discipline, run `ls .claude/skills/` + read first 5 lines of each `SKILL.md` `description:` field; if any description's auto-trigger keywords overlap with the current task's framing, **explicitly engage the skill** — do not rely on harness auto-trigger.

## §2.5 update — candidate H8 «discipline-application scope blindness»

**Pattern definition:** discipline-application scope ограничилась explicit object-under-review (the artefact under analysis — typically a plan, recommendation, or self-review patch), did **not** extend to (a) self-commentary about that object made in the same session, (b) meta-commentary about deliverables that lags behind primary-content updates, nor (c) claims accepted from collaborators (handover notes, reviewer findings) that propagate into edits. Below H7 strict reading — skill *was* loaded (§1.7 was actively applied to primary content); failure was selective scope, not consumption-side blindness. Closer to candidate **H8 «discipline-application scope blindness»** — sub-pattern of `#recursive-self-application-gap` at finer granularity than parent.

**H8 sample-size escalation within Wave 0.5 itself.** Three independent occurrences surfaced in this work — threshold for §4 distillation per [phase-research-coverage.md §3 aggregation](../../../.claude/rules/phase-research-coverage.md) (3-patch threshold mirroring AIF `/aif-evolve`):

1. **Original deliverable** (`eedaf12`): plan claimed «handover note was incorrect on this point» about `agents/docs-auditor.md` without `git log` adversarial-check — verify-before-stating applied to plan-listed file paths (via `ls`), not extended to meta-commentary about handover correctness. Corrected in `a3c3186`.
2. **Revision §14** (flagged in reviewer M1): §1.6 trigger sweep applied selectively to entries listed in finding #4 (§13.22 / §13.23) plus a few neighbours (§13.16, §13.20, §13.21) — not extended to the rule-prescribed «every non-cascade §13.x». Discipline-application scope = handover-finding scope, not §1.6-rule scope. Corrected in this revise-pass with explicit pre-L3 collective classification + §13.17/§13.18/§13.19 rows.
3. **Orchestrator accepting reviewer's m2 «count off-by-one» claim** during this very revise-pass: m2 was based on `grep -cE "^\s*'[^']+',?\s*$"` which captured a false positive on line 162's `expect(...).toContain('docs/meta-factory/EXECUTION-PLAN.md')` test assertion outside the array. Real REQUIRED_HEADER_DOCS count is 33 (manual count of lines 47-87 between array brackets), not 34. Orchestrator accepted reviewer's m2 fix and applied «33 → 34» edits before catching the false positive on second-look pre-commit verification. Reverted to 33 in same revise-pass. Discipline-application scope = «verify reviewer claims I accept into my workflow», not only «verify own claims I produce».

**Sample size for H8: 3/3 — threshold met.** Distillation into [§4 anti-pattern catalog](../../../.claude/rules/phase-research-coverage.md) is **out of Wave 0.5 scope** per acceptance criteria («Out of scope: Promotion H7 candidate в §4» — same exclusion logic applies to H8 by analogy). Recommended next-session work: separate research-patch + §4 entry promoting H8 from candidate to named anti-pattern, with all three occurrences as exemplars.

**Mitigation if promoted:** §1.7 forward-check Layer 6 «verify before stating, files exist» — extend scope to (a) first-order assertions about artefacts made in same session, (b) claims accepted from collaborators (reviewer findings, handover notes) before applying as fixes, (c) **declarative-not-literal phrasing in self-referencing meta-commentary** (use «see §X for current» forward-pointers instead of hard-coded counts/enumerations that go stale when primary content updates). Verify-before-stating becomes verify-before-stating-OR-accepting; literal-when-declarative-suffices = sub-case (b) lag generator.

**Process note:** closing this self-review required multiple reviewer cycles (full retrospective in PR description). Each cycle surfaced H8 sub-instances at finer granularity — H8 is a self-replicating class because each fix introduces fresh meta-commentary that can lag. Structural countermeasure (sub-case c above) was the late-cycle insight: literal-count phrasings («N commits», specific entry lists in narrative) self-deprecate as primary content extends; declarative phrasings (forward-pointers to authoritative §X) are stable. This insight applied retroactively in commit landing this note.

## Solution

The revised L3 plan + this self-review constitute the Wave 0.5 deliverable. Acceptance per handover criteria:

- 6/6 forward layers PASS (table above) ✓
- 6/6 backward steps PASS (table above) ✓
- All 7 reviewer findings closed in revision (§7, §11-§15, §14, §5, §6, §«Wave 1-4 deliverables breakdown», §1 paths) — none exempted ✓
- Both files exist under `docs/meta-factory/research-patches/`, folder-level authority inherited ✓
- Tags include `#candidate-h7-skill-consumption-blindness` + `#candidate-h8-discipline-application-scope-blindness` for distillation tracking ✓

Wave 1-4 implementation begins after Wave 0.5 reviewer GO.

## Prevention

PRIORITY CHECK rule, applied before closing any L3-style multi-surface discipline plan:

> Before closing the plan, walk §1.7 forward 6 layers + backward 6 steps **against the plan itself** with **explicit evidence** (`find`/`grep`/`ls` output) for each layer. If a layer's evidence is «memory of project state» — that layer fails. Re-check against current branch state.
>
> Additionally, before drafting: `ls .claude/skills/` + scan auto-trigger descriptions; if a skill's keywords overlap the task framing, engage the skill explicitly. Auto-trigger is a hint, not a hook.

The bootstrap exemplar [`2026-05-09-recommendation-skips-own-discipline.md`](2026-05-09-recommendation-skips-own-discipline.md) justified §1.7 existence by walking §1.7 over the gap that motivated it. This patch is the second-occurrence exemplar showing §1.7 catches the gap when applied — methodology validated under second observation.

## Tags

`#recommendation-skips-own-discipline` `#recursive-self-application-gap` `#methodology-validated` `#candidate-h7-skill-consumption-blindness` `#candidate-h8-discipline-application-scope-blindness`
