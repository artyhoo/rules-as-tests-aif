# Principles as Tests — Catalog

> **Status:** Phase 2 deliverable, 2026-05-07
> **Scope:** principles testable against `factory/rules-manifest.json` without Phase 3+ infrastructure
> **Test location:** `scripts/principles/` (vitest, picked up by `npm test` in scripts/)
> **Run:** `npm --prefix scripts run test:principles`

---

## Principle 1 — Every rule has executable check

**Source:** [overview.md](../../skills/rules-as-tests/references/overview.md) Layer 2
  + [ai-traps.md](../../skills/rules-as-tests/references/ai-traps.md) §8
  ("rules without measurable check decay")

**Test file:** [scripts/principles/01-executable-check.test.ts](../../scripts/principles/01-executable-check.test.ts)

**Manifest fields checked:** `check.type` ∈ {"eslint","command","script","manual"};
  `manual` type must have non-empty `rationale` field.

**Pass criterion:** Every rule has `check.type`. Rules with `check.type === "manual"` must
  document WHY automated enforcement is not feasible (prevents silent unenforceable rules).

**Mutation test:** removes `check` field → `assertPrinciple1` throws "missing check.type".
  Second mutation: sets `check.type = "manual"` with no rationale → throws "no rationale".

**Currently passing:** 26/26 rules

**Known exceptions:** R10, R13, R18, IR5, IR6 use `check.type === "manual"` with rationale.
  These ARE passing — the rationale requirement is what makes manual checks acceptable.

---

## Principle 2 — Paired negative test

**Source:** [overview.md](../../skills/rules-as-tests/references/overview.md) Layer 3
  anti-pattern ("Expected value generated by running the code → tautology farm") +
  [self-testing-docs.md](../../skills/rules-as-tests/references/self-testing-docs.md)
  ("Probe без negative test не считается реализованным")

**Test file:** [scripts/principles/02-paired-negative-test.test.ts](../../scripts/principles/02-paired-negative-test.test.ts)

**Manifest fields checked:** `examples.bad` (≥5 chars, non-trivial) and `examples.good`
  (≥5 chars); `examples.bad !== examples.good`.

**Pass criterion:** Every rule has distinct bad/good examples of non-trivial length.
  The negative example (`bad`) documents what violates the rule; without it, there is no
  documented violation scenario — the rule enforcement is unverifiable by inspection.

**Mutation test:** sets `examples.bad = "   "` → throws "absent or too short".
  Second mutation: sets `examples.bad === examples.good` → throws "tautology".

**Currently passing:** 26/26 rules

**Note on Phase 5 scope:** Phase 2 covers STRUCTURAL presence. Actually running `examples.bad`
  through the ESLint rule and verifying it produces a lint error → Phase 5 (validator runtime).

---

## Principle 3 — AST > grep (structural, PARTIAL)

**Source:** [overview.md](../../skills/rules-as-tests/references/overview.md) Layer 2
  ("AST scan" patterns) + [SKILL.md](../../skills/rules-as-tests/SKILL.md)
  ("Meta-tests — Implemented as AST scans")

**Test file:** [scripts/principles/03-ast-over-grep.test.ts](../../scripts/principles/03-ast-over-grep.test.ts)

**Manifest fields checked:** for rules with `check.type === "eslint"`: the `rule` field
  must be a non-empty, non-placeholder string referencing a concrete ESLint rule name.

**Pass criterion:** ESLint type rules reference concrete, non-placeholder rule names.
  ESLint rules use TSESTree/espree AST by default — asserting concrete rule name is the
  structural proxy for "AST-based enforcement is in place."

**Applicability:** 11/26 rules have `check.type === "eslint"`. Rules of type
  "command"/"script"/"manual" are exempt — they use other enforcement mechanisms.

**Mutation test:** sets `rule = ""` → throws "rule name is empty".
  Second mutation: sets `rule = "TODO: add rule name"` → throws "placeholder".

**Currently passing:** 11/11 eslint-type rules

**Known limitation (Phase 5 scope):** verifying that the ESLint rule's INTERNAL implementation
  uses AST traversal rather than regex requires running the rule. That is Phase 5 (validator
  runtime). Phase 2 covers only structural: "a concrete rule name exists."

---

## Principle 4 — No tautology

**Source:** [ai-traps.md](../../skills/rules-as-tests/references/ai-traps.md) §3
  ("Tautological tests") + [overview.md](../../skills/rules-as-tests/references/overview.md)
  Layer 3 anti-pattern ("Expected value generated by running the code → tautology farm")

**Test file:** [scripts/principles/04-no-tautology.test.ts](../../scripts/principles/04-no-tautology.test.ts)

**Manifest fields checked:**
  1. `check.command` must not be a no-op (`true`, `echo ok`, `:`, `exit 0`)
  2. `policy` must not be identical to `title`
  3. `examples.bad !== examples.good` (cross-check with P2)

**Pass criterion:** No rule has a structurally tautological manifest entry.

**Mutation tests:**
  - Sets `check.command = "true"` → throws "no-op"
  - Sets `policy = rule.title` → throws "policy field is identical to title"
  - Sets `examples.bad = examples.good` → throws "tautological example pair"

**Currently passing:** 26/26 rules

**Note on Guardrail 2:** This principle is the meta-level self-application of Guardrail 2.
  The mutation tests for principles 1-7 themselves verify that each meta-test is not
  tautological — they would fail if the assertions were trivially always-passing.

---

## Principle 5 — Manifest = SSOT (manifest ↔ RULES.md drift)

**Source:** [self-testing-docs.md](../../skills/rules-as-tests/references/self-testing-docs.md)
  (drift detection concept) + existing `render-rules.ts --check` +
  [overview.md](../../skills/rules-as-tests/references/overview.md) Layer 5
  ("Living Documentation — tests ARE the documentation")

**Test file:** [scripts/principles/05-manifest-ssot.test.ts](../../scripts/principles/05-manifest-ssot.test.ts)

**Manifest fields checked:**
  1. Every rule ID in `rules-manifest.json` must appear in `factory/RULES.md`
  2. `render-rules.ts --check` exits 0 (generated table is up-to-date)

**Pass criterion:** No orphan rules in manifest; RULES.md is regenerated from manifest
  (not hand-edited to drift).

**Mutation test:** removes first rule ID from RULES.md content string → throws
  "not found in factory/RULES.md".

**Currently passing:** 26/26 rules; render-rules.ts --check: exit 0

---

## Principle 6 — MUST not demoted to should

**Source:** [ai-traps.md](../../skills/rules-as-tests/references/ai-traps.md) §6
  ("AI agent silently demotes 'MUST' to 'should'")

**Test file:** [scripts/principles/06-must-not-demoted.test.ts](../../scripts/principles/06-must-not-demoted.test.ts)

**Manifest fields checked:** `policy` field of automated-check rules (`check.type` ≠ "manual")
  scanned for soft-only obligation language without hard counterpart.

**Applicability:** 21/26 rules have automated checks (not manual). Manual rules exempt —
  their enforcement is inherently advisory.

**Soft patterns detected:** `consider`, `should consider`, `if possible`, `where possible`,
  `recommended`, `ideally`, `preferably`.

**Hard counterparts (exempting soft):** `MUST`, `must not`, `forbidden`, `prohibited`,
  `required`, `No `, `never`, `always`, `only`.

**Pass criterion:** Automated-check rules with policy fields either (a) use hard obligation
  language, or (b) contain BOTH soft and hard language (soft word in context of hard rule
  is acceptable).

**Mutation tests:**
  - Sets policy = "Consider using safer alternatives where possible. Recommended if feasible."
    (no hard language) → throws "soft-obligation language"
  - Sets policy = "MUST use Zod. Recommended to also add logging where possible."
    (both) → does NOT throw (correct: hard counterpart exempts soft)

**Currently passing:** 26/26 rules (21 automated-check rules all use hard language)

---

## Principle 7 — Documents lie (examples are code, not prose)

**Source:** [self-testing-docs.md](../../skills/rules-as-tests/references/self-testing-docs.md)
  ("Documents lie; tests don't") + [SKILL.md](../../skills/rules-as-tests/SKILL.md)
  intro + [overview.md](../../skills/rules-as-tests/references/overview.md) Layer 5

**Test file:** [scripts/principles/07-documents-lie.test.ts](../../scripts/principles/07-documents-lie.test.ts)

**Manifest fields checked:** `examples.bad` and `examples.good` must contain code-like
  tokens (brackets, operators, string literals, JS/TS keywords, YAML structures, method
  calls) rather than pure prose descriptions.

**Pass criterion:** Examples contain executable code (any recognized code signal token),
  not just prose like "this is wrong because it violates the rule."

**Code signal patterns:** brackets `(){}[]`, operators `;,=`, string literals, JS/TS
  comments `//`, keywords (`import`/`export`/`const`/etc.), method calls `.foo(`,
  YAML patterns (`# `, `- `, `key: `), GitHub Actions keywords, `@version` refs.

**Mutation tests:**
  - Sets `examples.bad = ""` → throws "examples.bad is empty"
  - Sets `examples.bad = "This is wrong because it violates the rule"` → throws "prose"
  - Sets `examples.good = "This is the correct approach following best practices"` → throws "prose"

**Currently passing:** 26/26 rules

**Known limitation (Phase 5 scope):** Actually running `examples.bad` through the ESLint
  rule to verify it produces a lint finding (and `examples.good` produces none) → Phase 5.
  Phase 2 covers only structural: "example looks like code, not prose."

---

## Composite pass rate (Guardrail 3)

Total rules in manifest: **26** (R1–R20, IR1–IR6)

| Principle | Applicable rules | Passing | Rate |
|---|---|---|---|
| P1 — Executable check | 26/26 | 26 | 100% |
| P2 — Paired negative test | 26/26 | 26 | 100% |
| P3 — AST > grep (structural) | 11/26 (eslint-type only) | 11 | 100% |
| P4 — No tautology | 26/26 | 26 | 100% |
| P5 — Manifest = SSOT | 26/26 | 26 | 100% |
| P6 — MUST not demoted | 21/26 (automated-check only) | 21 | 100% |
| P7 — Documents lie | 26/26 | 26 | 100% |

**Composite applicable rate:** 26/26 rules pass all principles applicable to them = **100%**

Threshold 80% (EXECUTION-PLAN §6): **PASS**
REVISE trigger ≥30% fail (EXECUTION-PLAN §8): **NOT triggered** (0% fail)

---

## Known limitations — Phase 2 explicit deferrals

### Out of scope per Guardrail 1

| Principle | What Phase 2 covers | What is deferred | Defer to |
|---|---|---|---|
| AST > grep (validator runtime) | Structural: `check.type === "eslint"` implies AST-based enforcement; rule name non-empty | Verifying the ESLint rule's internal implementation uses AST traversal, not regex | Phase 5 (validator) |
| Mutation kill rate ≥70% | Anti-tautology of meta-tests themselves (mutation-style verification per Guardrail 2) | Stryker mutation testing on the underlying ESLint rule implementations | Phase 5 (validator) |
| Examples actually run | Structural: examples contain code-like tokens (not pure prose) | Running `examples.bad` through ESLint → expect lint error; `examples.good` → expect clean | Phase 5 (validator) |
| Every exported function has test | Not manifest scope | Filesystem traversal on consumer side | Phase 7+ (consumer integration) |
| rule-tester прогон runtime | Not feasible without `npm install` + ESLint runtime | Running @typescript-eslint/rule-tester on each rule | Phase 5 (validator) |

These deferrals are tracked in [open-questions.md](open-questions.md) and will be
addressed in Phase 5 (validator beyond meta-tests, per EXECUTION-PLAN §6).
